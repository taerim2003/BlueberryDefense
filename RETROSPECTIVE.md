# 블루베리 디펜스 — 회고 (1차 프로토타입)

**기간**: 2026-07-06 ~ 2026-07-09 (4일, 세션 4개)
**상태**: 플레이 가능 빌드 완성 (Must Have 전부 + Should Have 전부 완료, 스테이지 20개·몬스터 4종·스킬 5종·패시브 5종·젬 4종 전부 실동작, 빌드 검증 완료)
**핵심 루프**: 스킬을 강화해 밀려오는 블루베리를 시원하게 쓸어버리는 벙커 디펜스

---

## 구현된 피쳐

- 스폰/이동/데미지/게임오버 (몬스터 4종: 기본 블루베리, 리젠트블루베리(엘리트), 종이비행기블루베리(고속 저체력), 보물상자블루베리(스테이지당 1마리 확정 스폰))
- 액티브 스킬 5종(기본공격/회오리/오브/낙뢰/독수리투하), 획득 순서대로 QWER 슬롯 배정
- 레벨업 시스템: 3레벨 주기로 데미지/쿨다운/스킬별 고유 스탯(투사체 속도·발동 확률·크기) 중 하나씩 순환 강화
- 패시브 5종, 스킬 젬 4종 (장착 젬 HUD 아이콘 표시)
- 스테이지 20개 데이터 테이블 (지속시간/스폰간격/난이도 배율 전부 튜닝 가능)
- HUD 전체 (스테이지/HP/레벨/EXP/패시브·액티브 슬롯, 쿨타임 마스크, 데미지 숫자 팝업)
- 파티클/이펙트 오브젝트 풀링
- 렌더 순서 체계 (동적 X축 정렬 + 고정 상위 레이어 이펙트)

---

## 기술 스택

- 엔진: Unity 6 (6000.4.4f1), URP 2D
- UI: uGUI + TextMeshPro (Galmuri11 한글 폰트)
- 데이터: ScriptableObject (`StageTable`) — 코드 수정 없이 밸런스 튜닝 가능
- 기타: Unity-MCP(IvanMurzak)로 Claude가 에디터를 직접 조작, Pixel Craft VFX URP(1회성 임팩트 이펙트 전용)

---

## What Went Well

> 재현하고 싶은 것 — 코드 수준이 아니라 **워크플로·협업 수준**으로.

1. **Unity-MCP 기반 병렬 워크플로가 실제로 작동함.** 사용자가 스프라이트를 그리는 동안 Claude가 씬 배치·프리팹·코드를 동시에 진행하는 방식이 매 세션 안정적으로 반복됨 — "다른 거 하고 있어봐" 요청이 실제로 성립.
2. **도트 스프라이트 임포트 체크리스트를 절차화한 것.** spriteMode Single, PPU 32, Point 필터, 무압축, 풀캔버스 rect+중앙/하단 피벗 — 신규 몬스터 3종을 추가하며 매번 동일하게 적용해서 프레임 간 지터 같은 재발 버그가 단 한 번도 안 남.
3. **함정을 그때그때 CLAUDE.md에 기록하는 습관.** Collider 자동맞춤 버그, RigidbodyType enum, Canvas WorldSpace 기본값 등 — 세션이 바뀌어도 같은 실수를 반복 안 함.
4. **빌드 전용 버그를 추측 대신 로그로 확정한 것.** "에디터는 되는데 빌드는 안 된다"는 보고를 받았을 때, 코드를 이리저리 찔러보는 대신 `Player.log` 확인 방법을 안내해서 실제 스택 트레이스를 확보 → 한 번에 원인 확정.

---

## What Went Wrong

> 반복된 마찰 — **워크플로 수준**으로, "왜 일어났나"까지.

1. **가장 큰 사고: 잘못된 Kit 기록이 몇 세션 동안 못 알아챈 빌드 크래시로 이어짐.** "새 .cs 파일을 컴파일러가 안 잡는" 문제의 우회책으로 여러 MonoBehaviour 클래스를 한 파일에 병합했었는데, 이 패턴을 CLAUDE.md에 "유효하다"고 잘못 기록해뒀음. 실제로는 에디터의 관대한 fallback 때문에 에디터·플레이테스트에서는 멀쩡히 동작하고 **빌드에서만** 컴포넌트가 안 붙어 크래시하는 패턴이었음 — 코드 리뷰나 에디터 플레이로는 원리적으로 발견 불가능했고, 실제 빌드를 뽑아보고서야 드러남.
2. **같은 형태의 버그(HashSet 순회 중 파괴)가 두 스크립트에 중복 작성됨.** 회오리와 오브의 틱 데미지 로직을 유사하게 구현하면서, `foreach`로 순회 중인 컬렉션 안에서 `Destroy()`를 호출하는 패턴을 두 곳 모두에 넣음 — 첫 번째에서 걸렸을 때 즉시 두 번째도 점검했어야 했는데 사용자가 따로 알려줄 때까지 몰랐음.
3. **설계 선택지가 있는 지점에서 먼저 구현하고 나중에 피드백으로 고침.** 스폰 방식(확률 vs 결정론), 신규 몬스터 스탯 초안 등 — 사전에 선택지를 제시하거나 "임시값"이라고 명시했으면 왕복이 줄었을 상황이 여러 번 있었음.

---

## 과정 개선안

> Kit에 반영할 거면 **[→ Kit]** 표시.

1. **[→ Kit] 완료** — "여러 MonoBehaviour를 한 파일에 병합" 우회책 금지, 대신 assets-refresh 재시도 → 그래도 안 되면 재시작 요청. (CLAUDE.md §6 갱신)
2. **[→ Kit] 완료** — 에디터에서 재현 안 되는 버그는 추측하지 말고 Player.log부터 확인. (CLAUDE.md §6 신규)
3. **[→ Kit] 완료** — 트리거 추적용 컬렉션(HashSet 등)을 순회하며 그 안의 항목을 파괴하는 메서드를 호출할 때는 스냅샷 복사본을 순회할 것. (CLAUDE.md §6 신규)
4. 새 콘텐츠(몬스터/스킬) 스탯 초안은 "임시값, 플레이 후 조정 예정"이라고 매번 명시.
5. 확률 기반 vs 결정론적 스폰처럼 뚜렷한 설계 분기점은 구현 전에 먼저 사용자에게 선택지로 물어보기.

---

## 게임 개선방안

> 플레이어 피드백 수집 후 채울 것.

*미작성*

---

## 다음 프로토타입에 전달할 것

- Unity-MCP 기반 "그림은 사용자, 나머지는 Claude가 에디터까지 직접" 워크플로는 검증된 전제로 다음 SESSION_ZERO에 기본값으로 깔 것.
- 스프라이트 임포트 체크리스트(spriteMode/PPU/필터/압축/rect/피벗)를 SESSION_ZERO 템플릿에도 짧게 링크해두면 좋을 것 같음 — 지금은 CLAUDE.md에만 있어서 매번 찾아야 함.
- 멀티클래스 파일 병합 금지, Player.log 우선 확인, 컬렉션 순회 중 파괴 금지 — 전부 [→ Kit] 반영 완료.
