# HANDOFF.md — 세션 인계 문서

<!--
📋 유지 가이드 — 이 문서는 "다음 세션이 지금 뭘 해야 하는가"만 담는다. 길어지지 않게 유지할 것.
- 담을 것: 현재 빌드 상태, 미해결 이슈, 다음 할 일. 그것뿐.
- 지울 것: 완료·검증된 피쳐/버그/이슈는 즉시 삭제 (git 히스토리에 이미 남음).
- 세션 로그를 누적하지 말 것. 매 세션 새로 쓰는 게 아니라, 끝난 항목은 지우고 새 항목만 갱신.
- 게임 스펙(스킬 수치·시스템 규칙 등)은 여기 말고 GDD.md에. 여기엔 "무엇을 확인/수정해야 하는지"만.
- 목표: 항상 한 화면 안에 읽힐 것.
-->

## 🧭 North Star
> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 액티브 4 + 패시브 4를 진화 트리로 강화해 빌드를 만드는 벙커 디펜스. 상세 스펙은 `GDD.md` §4 참고.

---

## 현재 상태 (2026-07-12)

**빌드**: 1차 프로토타입 완성 후 대규모 밸런스/기능 패스까지 반영. 컴파일 에러 없음. **직전 대형 패스 + 이번 밸런스 패스는 실제 플레이 검증이 안 됐음** — 이 프로젝트 컨벤션상 동적 결과(파티클·애니·밸런스 체감)는 스크린샷으로 검증하지 않으므로, 아래 항목은 다음 세션에 실제 플레이로 확인 필요.

### 이번 세션 패스 요약 (2026-07-12, 검증 대기)
- **🔴 빌드 버그 수정**: `StageTable.asset`의 `m_Script`가 `{fileID: 0}`(null)이라, 에디터 전용 `m_EditorClassIdentifier` 폴백으로 에디터에선 되지만 **빌드에선 SO 타입 미해결 → stageTable=null → 전 스테이지가 기본값으로 동일하게 나오는 버그**. `StageTable`/`StageData`를 `GameManager.cs`에서 독립 파일 `StageTable.cs`(guid 080904fd…)로 분리하고 asset의 `m_Script`를 정상 참조로 교체. **→ 실제 재빌드로 스테이지 차등 확인 필수** (에디터/MCP 끊긴 상태로 수정해 미검증)
- **밸런스**: 스테이지 1~10 스폰 간격 ~20% 단축(초반 난이도↑) / 기본공격 레벨업 관통 +2→+1 / 낙뢰 기본피해 15→12, 지속 8→10초 / 경험치 획득 스테이지1=100%→20=50% 선형 감소 / 11스테이지+ 종료 보물상자 블루베리 2마리
- **기본공격 진화 비행 타격**: 비행 적(y≈1.1)이 안 맞던 문제 → 진화 시 발사점 +0.65 상향 + 발사체 히트박스 세로 1→1.6으로 지상·비행 동시 커버(수평 발사 유지, 사거리 손해 없음)

### 이전 대형 패스 요약 (검증 대기)
- **UFO 블루베리** 신규 몬스터(비행/저속/탱키, 스테이지 11+ 등장)
- **딜미터기 + 게임 클리어**: 20스테이지 클리어 시 클리어 처리, 게임오버/클리어 시 스킬별 누적피해 패널
- **하트 드랍 힐**: 처치 시 6% 확률 하트 드랍 → 흡수 시 체력 회복
- **패시브 진화 시스템**: 패시브 5종에도 액티브와 동일한 레벨업 + 3path 진화 트리 도입
- **진화 Lv.5 게이트**: path1/path2(연계) 진화는 연계 대상이 Lv.5 이상이어야 진행 가능
- 스테이지 전면 개편, 회오리 path0(미니 회오리) 재설계, 큰 초록 오브(BigOrb) 진화 비주얼 등 다수 밸런스 조정

---

## ⚠️ 미해결 이슈

### 0. 🔴 StageTable 빌드 수정 검증 (최우선)
Unity/MCP가 끊긴 채 파일만 수정함. 다음 세션에 **반드시**:
1. Unity 재포커스 → 새 `StageTable.cs` + asset 재임포트, 콘솔 컴파일 에러 확인 (StageTable/StageData 미인식 에러 시 CLAUDE.md §6 "새 .cs 미인식" 우회책 참고 — 단, GameManager.cs로 되돌리지 말 것. 그게 원인이었음)
2. `GameManager`의 Stage Table 필드에 asset 물려있는지 확인 (비면 재드래그)
3. **재빌드** 후 스테이지 1→5가 실제로 달라지는지 확인 ← 진짜 검증

### 1. 플레이 검증 필요 항목
이번/직전 패스에서 코드만 확인하고 실제 플레이는 못 한 것들:
- 이번 밸런스 패스 체감(초반 난이도, 낙뢰·관통 너프, 경험치 곡선, 보물상자 2마리), 기본공격 비행 타격 실제 명중 여부
- UFO 스폰/체력 밸런스, 딜미터·게임클리어 패널 노출, 하트 드랍·흡수 애니
- 진화 패널에서 Lv.5 잠금 문구 표시, 패시브 5종 각 3path 진화가 의도대로 적용되는지(특히 건강 반격·리프레쉬×낙뢰 쿨감 등 새 훅)
- 큰 오브(BigOrb) 020 진화에서 등장, 회오리 미니회오리 재설계 밸런스, 스테이지 곡선 전반
- 체인 라이트닝 연결선이 실제로 잘 보이는지, 오브 설치기 위치/쿨타임 체감

---

### 2. 사운드 시스템 (나중으로 미룸)
볼륨 소스가 여러 군데(개별 프리팹 `m_Volume`, `fireSfxVolume`/`castSfxVolume`, `SfxPlayer.MasterVolume`)로 분산돼 이중 곱해지며 예측 불가한 상태 — 볼륨 급증/씹힘 증상이 남아있음. 재설계 권장하나 **우선순위 낮음, 한참 뒤로 미룸.** 손댈 때 통합 대상: `SfxPlayer.cs`, `AudioThrottle.cs`, `ObjectPool.cs`, 각 Vefects VFX 프리팹 AudioSource.

---

## ★ 다음 세션 체크리스트
- [ ] **🔴 StageTable 빌드 수정 검증** (위 미해결 #0) — 재임포트 → 재빌드 → 스테이지 차등 확인
- [ ] 위 플레이 검증 항목 실제 플레이로 확인 → 발견된 밸런스/버그 조정
  - (검증 툴: `Window > Blueberry Defense > Cheat Window` — Play 모드에서 레벨업/획득/진화/스테이지 스킵)

---

## 🎨 스킬 이펙트 픽셀 크기 컨벤션
스킬 이펙트 스프라이트는 배경과 픽셀 밀도가 안 맞아, **그림은 작게 그리고 엔진에서 1.5배로 표시**한다(플레이어 딸기 프리팹 `localScale=1.5`에 맞춤).
- 새 이펙트 프리팹은 기존값에 곱하지 말고 `localScale`을 **직접 (1.5,1.5,1.5)로** 세팅.
- PPU **32**, Main Camera Orthographic Size 5(화면 세로 = 10유닛).
- 공식: `원본 그림 px = (목표 월드 유닛 × 32) ÷ 1.5`
  - 예) 화면 세로를 꽉 채우는 세로형 이펙트 = 64 × 213px.
- 카메라 Size나 플레이어 localScale이 바뀌면 이 배율을 전부 다시 계산할 것.
- 1.5 적용된 프리팹: `Whirlwind_Skill`, `Orb_Skill`, `BigOrb_Skill`, `Whirlwind_BigTornado`, `OrbAltar_Skill`, `SmallOrb_Skill`, `Projectile_BasicAttack`, `EagleDrop_Visual`. (`HitParticle`는 소품이라 예외로 0.7)

---

## 보류 중인 결정 / 백로그
- UI 시스템: uGUI + TextMeshPro 확정
- 배포(itch.io 등 실제 공개)는 아직 진행 안 함
- 진화 시스템 도입 후 신규 수치(오버힐 캡, 미니 회오리 데미지 분배 등)는 감으로 잡은 값 — 플레이테스트로 조정 필요
