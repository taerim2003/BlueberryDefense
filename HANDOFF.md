# HANDOFF.md — 세션 인계 문서

## 현재 상태 (2026-07-08)

**빌드 상태**: 1차 프로토타입 완성. Must Have + Should Have 전 항목 구현 및 플레이테스트 완료. 스탠드얼론 빌드까지 검증 완료 (빌드 전용 크래시 버그 발견 후 수정, Player.log로 확인).
**마지막 작업**: 스테이지 20개 밸런스 3차 조정, 신규 몬스터 3종(리젠트/종이비행기/보물상자블루베리) 추가, 스킬 레벨업 재설계, 오브젝트 풀링 도입, 렌더 순서 체계 정리, 치명적 버그 2건 수정(HashSet 순회 크래시, 빌드 전용 스크립트 참조 크래시). 자세한 내용은 `d:\unity\prototyping-kit\journal\2026-07-08.md` 참고. **1차 프로토타이핑 종료 — `RETROSPECTIVE.md` 작성 완료.**

> **다음 세션 = 회고 기반 개선 또는 2차 프로토타이핑 시작.** 아래 "다음 할 일" 참고.

---

## 프리징 버그 근본 원인 발견/수정 + 체인라이트닝 비주얼 개편 + 사운드 렉 완화 + 에디터 치트창 (2026-07-11)

**"게임하다 멈춘다" 버그 — 근본 원인 찾아서 수정 완료.**
- **원인**: `Enemy.TakeDamage`가 한 프레임 안에 같은 적에게 두 번 호출되면(예: 회오리가 여러 개 동시에 겹쳐서 오버킬) `Destroy()`가 프레임 끝에야 실행되는 특성 때문에 사망 처리 블록(XP 지급/`ShowTreasureReward`/`Destroy`)이 **두 번** 돌 수 있었음. 트레저 블루베리가 이렇게 오버킬되면 `EvolutionTreeUI.Show()`(또는 `LevelUpUI.ShowOptions()`)가 같은 프레임에 두 번 호출되어 `ModalPause.Push()`가 두 번 쌓이는데, 플레이어는 패널을 한 번만 닫으므로 `Pop()`은 한 번만 일어남 → 참조 카운트가 0으로 안 내려가서 `Time.timeScale=0`이 영구 고정. 에러 로그가 전혀 안 남는 이유도 이거(예외가 아니라 그냥 카운트 불일치).
- **수정**: `Enemy.cs`에 `isDead` 플래그 추가해서 사망 처리가 한 프레임에 두 번 안 돌도록 가드. `EvolutionTreeUI.Show()`/`LevelUpUI.ShowOptions()`도 이미 패널이 열려있으면 `Push()`를 건너뛰도록 2중 방어 추가.
- 사용자가 "회오리 업글했더니 또 멈추던데"라고 재현 조건을 정확히 짚어줘서 원인 특정이 빨랐음.

**체인 라이트닝 비주얼 전면 개편**: 기존엔 하늘에서 내리치는 낙뢰 VFX(`VFX_2D_Projectile_Lightning_01`)를 회전만 시켜서 재활용했는데, 이 에셋 자체가 세로로 떨어지는 모양이라 아무리 회전해도 "두 대상을 잇는 선"처럼 안 보이는 근본적 한계가 있었음. → `LineRenderer` 기반 신규 `ChainLightningBeam.cs`로 교체, 두 좌표 사이에 지그재그 번개선을 실제로 그림(세그먼트 6개, 랜덤 지터). 신규 프리팹 `ChainLightning_Beam.prefab` + 머티리얼 `M_ChainLightningLine.mat`(URP 2D Unlit), 4종 몬스터 프리팹 전부 참조 교체.
- 1차 수정 후에도 "그냥 세로로 떨어진다"는 피드백 재발 — 원인은 체인 전이 대상에게 `TakeDamage(isLightningProc:true)` 호출 시 **기존 낙뢰 VFX가 추가로 또 뜨고 있었음**(연결선 + 세로 낙뢰가 동시에 재생되는데 세로 낙뢰가 더 시각적으로 강해서 연결선이 묻힘). `TakeDamage`에 `suppressLightningStrikeVfx` 파라미터 추가해서 체인 전이 타격에서만 세로 낙뢰를 끄도록 수정, 빔 자체도 두께(0.08→0.14)/지속시간(0.2→0.35초)/정렬순서(10→50) 올려서 더 잘 보이게 함.
- ⚠️ 이 프로젝트 컨벤션상 동적 VFX는 스크린샷 검증 안 함(신뢰도 낮고 불안정) — **다음 세션에서 실제 플레이로 육안 확인 필요.**

**사운드 겹침 렉 완화**: AoE 스킬(독수리투하/낙뢰/회오리 체인)이 한 프레임에 적 수십 마리를 때리면 각자 붙어있는 하나의 동일한 효과음이 동시에 수십 번 재생되어 렉 발생. `AudioThrottle.cs`(정적 클래스, 클립별 마지막 재생 시각 기록) 신규 작성 후 `ObjectPool.Spawn()`에 훅 — 같은 클립이 80ms 안에 다시 재생 요청되면 새로 트리거된 쪽을 즉시 `Stop()`. 히트/사망/임팩트 VFX는 전부 `ObjectPool` 경유라 이걸로 커버됨. (스킬 발사체 자체(`Projectile`/`Whirlwind`/`Orb`/`EagleDrop`/`OrbAltar`)는 아직 풀링 안 됨 — 캐스트 빈도가 낮아서 우선순위 낮게 스코프 아웃, 필요하면 다음에.)

**오브 설치기(altar) 밸런스 조정**: 위치 살짝 위로(맵 중앙 y=-0.5→-0.2→0.1, 총 3차례 조정), 쿨타임 30→20초, 낙뢰 주기 4→2.5초, 크기 추가 축소(altar 자체에 ×0.75 배율). **낙뢰 연계(path2) 진화 재설계**: "설치기로 대체" 기믹을 T1에서 T2로 이동(개큰 회오리와 동일 패턴 맞춤, T1은 선행 스탯 버프만), T1/T3 효과를 서로 교체(T1=취약부여, T3=설치기 피해량 증가) 후 사용자 요청으로 T3 수치를 20%→50%로 상향. T3는 원래 "설치기 크기 증가"였는데 "필요 없다, 데미지가 낫다"는 피드백으로 데미지 배율(`altarDamageMult`)로 교체.

**개큰 회오리 미세 위치 조정**: "공중에 떠있는 것 같다"는 피드백으로 스폰 좌표에 `Vector3.down*0.3f` 추가.

**레벨업 패널 위치**: 화면 중앙에서 위로 60px 이동.

**에디터 도구 2종 신규 구축**:
1. **데이터 기반 선택지 시스템** — `LevelUpStatOptionSO`(레벨업 범용 스탯 강화 선택지, 에셋 추가만으로 새 선택지 생김, `Assets/Data/LevelUpOptions/`에 기본 3종 배치) + `EvolutionTierTextTableSO`(진화 트리 각 path/tier 제목·설명 텍스트를 코드 수정 없이 override하는 테이블, `Assets/Data/EvolutionTierTextTable.asset`, 현재 빈 상태 — 항목 없으면 기존 하드코딩 텍스트로 폴백이라 동작은 그대로). 사용자가 "너무 어렵다"며 이 방식 대신 에디터 창(치트 UI) 방식을 요청해서 아래 2번으로 대체 제안됨 — 이 시스템 자체는 유지하되 주력 워크플로는 아님.
2. **`CheatWindow.cs`(`Assets/Editor/`)** — `Window > Blueberry Defense > Cheat Window`. Play 모드 전용, 버튼 클릭으로 레벨업 강제 발동, 풀피/최대체력 증가, 스킬·패시브 즉시 획득, 스킬 레벨업/진화, 스테이지 스킵 가능. `PlayerHealth.FullHeal()`/`GameManager.SkipToNextStage()` 치트 전용으로 신규 추가.

**남은 작업**:
- 체인 라이트닝 연결선이 실제로 잘 보이는지 플레이로 확인 (동적 VFX라 스크린샷 검증 불가했음)
- 오브 설치기 최종 위치/크기/쿨타임 체감 밸런스 플레이 확인
- `CheatWindow`/`EvolutionTierTextTableSO` 둘 다 이번 세션엔 실사용 검증 안 됨 — 다음 세션에 에디터에서 직접 열어서 확인
- 이번 커밋에는 이전 세션(07-10)에 문서화만 되고 커밋 안 된 변경분도 함께 포함됨 (StageTable, 폰트, EagleDrop/Enemy 프리팹 등)

---

## UI/사운드 버그 수정 + 레벨업 UI 뱀서(Vampire Survivors) 스타일 전환 (2026-07-10 밤)

- **암살 크리티컬 데미지 텍스트**: 빨간색 + "!" 접미사로 표시. `Enemy.TakeDamage`/`DamageNumber.Init`에 `isCrit` 플래그 추가, `Projectile`/`Whirlwind`/`Orb`/`SmallOrb`/`OrbAltar`까지 전부 크리티컬 여부를 끝까지 전달하도록 배선.
- **모달 패널 시간정지 버그**: `EvolutionTreeUI`/`LevelUpUI`가 각각 독립적으로 `Time.timeScale`을 0/1로 직접 건드리고 있어서, 트레저 처치 시 "먼저 진화시킬 스킬 선택" LevelUpUI가 EvolutionTreeUI를 연 직후 자기 자신의 Close()가 timeScale을 1로 되돌려버리는 버그가 있었음 — 새 `ModalPause` 정적 클래스(참조 카운트)로 교체, 여러 모달이 겹쳐도 전부 닫혀야 재개되도록 수정.
- **진화 패널 제목 텍스트 밀림 버그**: Window를 풀스크린으로 늘렸을 때 헤더(아이콘/이름/서브텍스트) 좌표를 안 옮겨서 텍스트 박스 왼쪽 절반이 화면 왼쪽 경계 밖으로 나가 잘려 보이던 문제 — 헤더 좌표 재계산해서 화면 안쪽으로.
- **레벨업 UI를 뱀파이어 서바이벌 스타일로 전면 개편**: 기존 가로 3분할 카드(제목+설명 뭉쳐진 텍스트 하나)를 → 세로 스택 3행 레이아웃으로 교체. 각 행: 좌측 아이콘, 상단에 제목+레벨(또는 "신규!" 노란 태그), 하단에 설명. 헤더 "레벨 업!" 텍스트도 추가. `LevelUpUI.Option`을 Title/LevelText/IsNew/Description으로 분리.
- **HUD 스킬 아이콘에 레벨 표시**: QWER 슬롯 아이콘 위에 "Lv.N" 뱃지 추가 (`HUDController.ActiveSlot.levelLabel`).
- **몬스터 죽음 사운드가 이상하게 반복되던 버그 원인 발견 및 수정**: `deathVfxPrefab`(Pixel Craft VFX "Vanish" 프리팹)의 AudioSource가 `Loop=true`로 설정되어 있어서, 원샷 효과음인데도 계속 루프 재생되고 있었음(ObjectPool이 SetActive만 하고 AudioSource는 따로 안 멈춤). **레벨업 보상 고를 때 사운드가 겹쳐 들리던 것도 같은 원인**(직전에 죽은 몬스터의 루프 사운드가 안 멎은 채로 남아있었음). `Loop=false`로 수정하고 클립도 `SFX_Vefects_Vanish_02` → `SFX_Vefects_Vanish_01`로 교체.

**남은 작업**: 이번에도 script-execute 반복 Show() 호출 시 불안정 이슈로 실제 플레이 검증은 못함 — 레벨업 UI 새 레이아웃, HUD 레벨 뱃지, 크리티컬 빨간 텍스트, 사운드 수정 전부 다음 세션에서 직접 플레이로 확인 필요.

---

## 스테이지 시스템 킬 클리어제 전환 + 대량 밸런스/UI 수정 (2026-07-10 저녁)

- **스테이지 종료 조건 변경**: 타이머만으로 끝나던 걸 "시간 다 되면 스폰 정지 → 남은 적 전부 처치해야 스테이지 종료"로 변경 (`GameManager.waitingForClear`). 스테이지 넘어갈 때 플레이어 전체 스킬 쿨타임 자동 초기화(`PlayerSkills.ResetAllCooldowns()`).
- **스테이지 전환 텀**: 2.5초 → 4.5초.
- **스테이지 7 = 종이비행기 올스타**: 그 스테이지는 트레저 제외 전부 종이비행기로만 스폰.
- **5스테이지마다 스탭형 보너스 배율**: 기존 StageTable 곡선 위에 `EnemySpawner.HpStepBonus`/`SpeedStepBonus` 추가 적용 (스테이지5=+3%/+2%, 10=+8%/+5%, 15=+15%/+9%, 20=+25%/+15%) — 초반엔 거의 안 느껴지고 후반으로 갈수록 체감 커짐.
- **후반 난이도 재상향**: `StageTable.asset` 전 스테이지 spawnInterval ×0.6, eliteChance ×1.7+0.05(최대 0.7) — 리젠트 훨씬 자주 나오고 스폰 훨씬 빡빡해짐.
- **암살 패시브**: 치명타 확률 4% → 15%.
- **기본공격 위치**: 살짝 아래로. **기본 오브**: 스폰 위치 계속 낮춤(최종 player.pos - 0.1 아래).
- **오브 스프라이트 좌우반전 버그** 수정 (예전 오브 그림 기준 flipX=true였던 게 새 작은 오브 그림엔 안 맞았음).
- **비행형(종이비행기) 타격 불가**: 기본공격은 무조건, 기본 오브는 path0(공중 추가피해) 진화 전까지 비행형을 아예 못 맞힘 — `Enemy.IsFlying` 체크 추가. 종이비행기 스폰 높이 0.9→1.1.
- **오브 설치기(altar) 튜닝**:
  - 스폰 위치를 플레이어가 아니라 **맵 중앙 고정 좌표**로 변경 (`PlayerSkills.MapCenter`)
  - 크기 1.5배 → **1배**로 축소
  - 설치기 발동 쿨타임을 **30초**로 대폭 상향 (일반 오브 쿨타임 공식 무시하고 하드코딩 오버라이드)
  - 지속시간 8초 → **15초**
  - 미니 오브 크기 절반 축소(기존 요청 반영 유지)
  - **버그 수정**: 설치기 낙뢰가 `isLightningProc: true` 없이 데미지만 주고 있어서 번개 이펙트가 사실상 한 번도 안 뜨고 있었음(가끔 뜬 건 플레이어가 실제 낙뢰 스킬도 같이 켜놨을 때 우연히 겹친 것) — 플래그 추가해서 항상 이펙트 나오게 수정.
- **개큰 회오리 피벗 버그**: 스프라이트가 center pivot이라 절반이 바닥 밑으로 파묻혀 있었음 — BigTornado 1~4 전부 pivot을 bottom-center로 재임포트하고, 스폰 좌표에서 별도 위쪽 오프셋 제거. 콜라이더 offset도 위로 재조정.
- **진화 패널 카드 리디자인**: 가로 230→380으로 확장, 아이콘/제목/설명이 안 겹치게 3단 배치(제목 상단·아이콘 중단·설명 하단)로 재배치, **카드별 짧은 업그레이드 제목**을 새로 추가(`PlayerSkills.GetPathTierTitle`, 45개 타이틀 전부 작성), 헤더(스킬 아이콘/이름/서브텍스트) 위치도 다시 잡음.

**남은 작업**: 이번 라운드도 script-execute 반복 호출 시의 "destroyed object" 오탐 이슈 때문에 플레이모드 Show() 직접 검증은 스킵하고 컴파일 확인 + 좌표/스케일 값 정적 계산으로만 검증함. 다음 세션에서 실제 플레이로 스테이지 클리어 흐름, 오브 설치기, 개큰 회오리, 진화 패널 카드를 전부 육안 확인할 것.

---

## 마지막 남은 "아트 필요" 진화 3종 구현 완료 (2026-07-10)

이전에 "아트 없어서 스탯으로만 임시 대체" 상태였던 3개 진화 경로를 실제 아트+기믹으로 교체:

- **오브 → 낙뢰 연계 (path2) T1**: "오브 설치기" — 날아가는 오브 대신 `OrbAltar.cs`가 제자리에 설치됨. 2초마다 8방향으로 `SmallOrb.cs` 발사, 4초마다 화면 내 모든 적에게 낙뢰 데미지. T2는 설치기 크기+20%, T3는 취약 부여(기존 유지). 신규 프리팹: `SmallOrb_Skill.prefab`, `OrbAltar_Skill.prefab`.
- **회오리 → 오브 연계 (path2) T2**: "개큰 회오리" — 기존엔 T2가 "둔화 지속시간 증가"였는데, 이제 T2부터 회오리가 여러 개로 안 쪼개지고 `Whirlwind_BigTornado.prefab` 하나로 대체됨 (Effect_BigTornado2~4 애니메이션, 1번 프레임은 안 씀 — 사용자가 "002부터" 지정). T1(둔화)·T3(취약)는 유지.
- **낙뢰 → 힘 연계 (path1) T1**: "체인 라이트닝" — 낙뢰가 첫 발동할 때(자기 자신에게 재귀 프록 되기 직전 시점) 반경 4유닛 내 가장 가까운 적 최대 3마리에게 전이. `Enemy.ChainLightningToNearby()`, 빔 이펙트는 기존 VFX팩의 `VFX_2D_Projectile_Lightning_01_Color_Static` 재사용(따로 안 그려도 됨). T2/T3는 쿨감으로 재배치.

**오브 기본 스프라이트 교체**: `Orb_Skill.prefab`이 이제 `Effect_SmallOrb1/2`(32×32, 2프레임)를 씀 — 기존 `Effect_Orb1`(64×64)은 더 이상 기본 오브에 안 쓰임. 유저가 나중에 그릴 "큰 오브"(기존 스프라이트 1.5배)는 오브 path0(기본) 진화 전용으로 예약됨, 아직 미적용.

**스킬 이펙트 전역 1.5배 스케일 적용**: `Whirlwind_Skill.prefab`(1.35→2.03), `Orb_Skill.prefab`(1.328→1.99) — 픽셀 밀도가 배경과 안 맞는 문제 해결 위해 원본 그림은 작게, 엔진에서 1.5배로 표시하는 방식으로 전환 (§아래 컨벤션 참고).

**남은 작업**: 위 3개 경로 다 실제 인게임 플레이로 검증 안 됨(스크립트 실행 도구가 이 세션에서 Show()/컴포넌트 반복 호출 시 불안정해서 컴파일 확인만 함) — 다음 세션에서 꼭 플레이모드로 QWER 눌러서 오브 설치기/개큰회오리/체인라이트닝 실제로 나오는지 확인할 것.

---

## 🎨 스킬 이펙트 픽셀 크기 컨벤션 (2026-07-10 확정, 같은 날 한번 정정됨)

스킬 이펙트 스프라이트(회오리/오브 등)는 배경과 픽셀 밀도가 안 맞아서, **그림은 원래 크기로 그리고 엔진에서 배율을 키워서 표시**하기로 함. 이 배율은 **플레이어(딸기) 프리팹의 `localScale` = 1.5**를 그대로 따라간 것 — 딸기 스프라이트가 32×48px(PPU32 기준 네이티브 1×1.5유닛) × 1.5배로 표시되고 있어서, 스킬 이펙트도 정확히 이 1.5배에 맞춤.

- ⚠️ **정정**: 처음엔 각 프리팹의 기존 scale에 1.5를 "곱하는" 식으로 처리했다가(회오리 1.35→2.03, 오브 1.328→1.99) 결과적으로 이펙트가 플레이어보다 훨씬 커 보이는 문제가 생김. → 전부 기존값 무시하고 **`localScale`을 (1.5,1.5,1.5)로 직접 세팅**하는 걸로 정정. 새 이펙트 프리팹 만들 때도 곱하지 말고 그냥 1.5로 고정할 것.
- 현재 1.5로 맞춰진 프리팹: `Whirlwind_Skill`, `Orb_Skill`, `Whirlwind_BigTornado`, `OrbAltar_Skill`, `SmallOrb_Skill`, `Projectile_BasicAttack`, `EagleDrop_Visual`. (`HitParticle`는 타격 스파크용 소품이라 의도적으로 0.7 유지, 플레이어 배율 안 따름)
- PPU(Pixels Per Unit): **32** (기존 회오리/오브 원본 64×64px 기준)
- Main Camera: Orthographic Size 5, 16:9 비율 → 화면 세로 = **10유닛** (바닥~천장)
- 공식: `원본 그림 px = (목표 월드 유닛 × 32) ÷ 1.5`
- 예시: "개큰 회오리"(회오리→오브 진화, 화면 꽉 채우는 세로로 긴 토네이도)는 **64 × 213px**로 그리면 엔진 1.5배 적용 후 정확히 화면 세로(320px 상당)를 채움

카메라 Orthographic Size나 플레이어 자체의 localScale이 바뀌면 이 배율(1.5)과 213px 계산을 전부 다시 해야 함.

---

## 2026-07-09 밸런싱 패스 (2차 프로토타입, 진화 시스템 이후)

- **스킬 기본 데미지**: 회오리 9→6 너프, 낙뢰 ProcDamage 10→15 버프, 기본공격 10→14 버프.
- **스테이지 후반 난이도**: 11~20스테이지 spawnInterval을 끝까지 계속 감소하도록 재조정(기존엔 18~19스테이지에서 0.18로 평탄화됐음), eliteChance도 후반부에 더 가파르게 증가(20스테이지 기준 42%→52%), paperPlaneChance도 초반 8% 고정에서 후반 15%까지 증가하도록 변경. `Assets/Data/StageTable.asset` 참고.
- **레벨업 성장치 하향 / 진화 성장치 상향**: 레벨업 3종 효과(피해량 배율 1.3→1.2, 쿨감 0.85→0.9, 3번째 효과 수치도 전부 하향)를 낮추고, 진화 트리 쪽 수치(피해량 +25%, 쿨감 -15% 등)는 상대적으로 크게 유지 — 진화 시점의 체감 성장폭이 레벨업보다 훨씬 크게 느껴지도록.
- **진화 UI 수치 표기**: `LevelUpUI`/`EvolutionTreeUI`에 노출되는 모든 업그레이드 설명에 구체적 수치(%, 초, 회) 명시. `DescribeUpgradeEffect`/`DescribePathEffect` 전부 갱신.
- **진화 트리 "한 티어 = 한 효과" 원칙 적용**: 기존엔 `ApplyPathTierEffect`가 path0/1에서 "피해량(또는 쿨감) % + 스킬별 flavor 기믹"을 매 티어 동시에 부여해서 한 업그레이드가 실질적으로 2개 효과를 주는 문제가 있었음(예: 회오리 path0은 매 티어 피해량 15%와 분열소환 개수가 동시에 올라감). 전면 재설계해서 **티어마다 정확히 하나의 효과만** 나가도록 분리 — 예: 회오리 path0은 T1=분열2개, T2=피해량25%, T3=분열3개. `PlayerSkills.cs`의 `ApplyPathTierEffect`/`DescribePathEffect`와 `FireBasicAttack`/`FireWhirlwind`/`FireOrb`/`EagleDropRoutine`의 라이브 PathTier 읽기 공식을 전부 이 원칙에 맞춰 다시 짬. 이 과정에서 기존 버그도 발견: Orb의 path2(낙뢰 연계)는 T1/T2에 아무 효과도 없이 T3 취약만 부여하던 상태였음 — T1=피해량15%, T2=오브 크기20%로 채워 넣음. BasicAttack path2도 설명 텍스트가 "적 둔화"로 잘못 표기돼 있었음(실제 효과는 미니 독수리) — 텍스트만 있고 실제 슬로우 효과는 없는 상태였던 버그, 수정함.
- 컴파일 에러 없음 확인(`console-get-logs`), `DescribePathEffect` 전체 45개 조합 중 일부 샘플을 playmode에서 직접 호출해 텍스트 출력 검증 완료.
- **아직 안 함**: 실제 플레이 밸런스 테스트(수치는 감으로 잡은 값), 신규 몬스터 종류 추가는 이번 패스에 포함 안 됨(스폰 빈도/확률만 조정).

---

## 🧭 North Star — 게임 비전

> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 스킬 + 스킬 젬 강화로 빌드를 만드는 벙커 디펜스. 핵심 루프는 GDD.md §2 참고.

---

## ★ 다음 세션 시작 시 체크리스트

- [ ] `RETROSPECTIVE.md` 먼저 읽고 시작 (프로토타입 종료 회고, Kit에 이미 반영된 항목 확인)
- [ ] 다음 방향 결정: (a) 플레이어 피드백 수집 후 밸런스/UX 개선, (b) 2차 프로토타입(다른 컨셉) 시작, (c) 이 프로토타입 계속 확장
- [ ] (b)라면 SESSION_ZERO 새로 작성, (a)/(c)라면 이 문서 체크리스트 이어서 사용

---

## 🎨 도트 그래픽 제작 리스트

| 항목 | 상태 | 비고 |
|---|---|---|
| 플레이어 캐릭터 | ✅ 완료 | `Player.png` + 공격 애니 3프레임 |
| 기본 블루베리 | ✅ 완료 | 걷기 애니 4프레임 |
| 리젠트블루베리(엘리트) | ✅ 완료 | 걷기 애니 4프레임, 항상 뒷레이어 |
| 종이비행기블루베리(고속) | ✅ 완료 | 날갯짓 애니 2프레임 |
| 보물상자블루베리(황금) | ✅ 완료 | 4프레임, 스테이지당 1마리 확정 스폰 |
| 배경 | ✅ 완료 | PPU 18 |
| 스킬 실제 인게임 스프라이트 (기본공격/회오리/오브/독수리) | ✅ 완료 | 전부 실제 아트로 교체됨 |
| 스킬 아이콘 (QWER UI 등, 14종) | ✅ 완료 | HUD·레벨업 카드 전부 연결 |

---

## 코드 변경 요약 (2026-07-08 세션 — 대규모)

### 신규 스크립트
- `ObjectPool.cs` — 파티클/이펙트 재사용 풀링 (HitParticle, DamageNumber, 각종 VFX 임팩트)
- `DamageNumber.cs`, `HitParticle.cs` — **`Enemy.cs`에서 분리** (병합 패턴이 빌드 크래시 원인이었음, 아래 참고)

### 버그 수정 (중요)
- **빌드 전용 NullReferenceException**: `DamageNumber`/`HitParticle`이 `Enemy.cs`에 병합되어 있어 `m_Script: {fileID: 0}` + 클래스이름표로 직렬화됨 — 에디터는 관대하게 재연결해주지만 빌드는 컴포넌트를 아예 못 붙임. 별도 파일로 분리 + `MonoScript` 정식 참조로 재연결해서 해결. **CLAUDE.md §6에 경고 반영 완료, 앞으로 절대 이 패턴 쓰지 말 것.**
- **HashSet 순회 중 컬렉션 수정 크래시**: `Whirlwind`/`Orb`의 틱 데미지 로직이 `overlappingEnemies`를 순회하며 `Destroy()`를 호출 → `OnTriggerExit2D`가 동기 발생해 같은 HashSet을 수정 → `InvalidOperationException`. 스냅샷 복사본 순회로 해결.

### 스킬 시스템
- `PlayerSkills.cs`: 레벨업 시 데미지(30%)/쿨다운 감소/스킬별 고유 스탯(기본공격=투사체 속도, 낙뢰=발동 확률, 나머지=크기) 중 하나씩 3레벨 주기로 순환 적용. 글로벌 쿨다운 0.2→0.4초.
- `LightningStorm.cs`: 발동 확률 60%(고정값→가변 필드로 변경, 레벨업으로 증가 가능), 데미지 10.

### 스테이지/몬스터
- `StageTable.asset`: 20스테이지 전부 재조정 (지속시간 20~60초 점증, 스폰 간격 대폭 단축, `eliteChance`/`paperPlaneChance` 필드 추가, `treasureChance`는 제거하고 확정 스폰 방식으로 전환).
- `EnemySpawner.cs`: 보물 블루베리를 확률 롤 대신 "스테이지 전환 후 첫 스폰에 확정 등장"으로 변경.
- `Enemy.cs`: `spawnYOffset`(공중 유닛용), `alwaysBackLayer`(대형 유닛용) 필드 추가. 렌더 순서를 X좌표 기반 동적 정렬로 변경(적끼리 겹칠 때 앞뒤 관계 안정화).
- 신규 프리팹: `Enemy_RegentBlueberry`, `Enemy_PaperPlaneBlueberry`, `Enemy_TreasureBlueberry`(신규 아트로 전면 교체).

### UI
- `HUDController.cs`: 액티브 스킬 슬롯에 장착 젬 아이콘(최대 3개) 표시 추가.
- EXP 바 폭 확장(700→900).

### 렌더 순서 체계
- 적: `100 + X*10` 동적 정렬 (배경 -100보다 항상 위, `alwaysBackLayer`면 고정 1)
- 파티클/스킬이펙트: 300~330 고정 상위 레이어 (적보다 항상 위)

---

## 2차 라운드 계획 (2026-07-09 수립)

`RETROSPECTIVE.md` 기반, 같은 게임을 확장하는 방향(HANDOFF (c)) — SESSION_ZERO 새로 안 씀.

**최우선**
1. **스킬 강화 서브트리("진화") 시스템 — 스킬 젬 시스템 완전 대체. 구조 구현 완료 (2026-07-09).**
   - 구조: 액티브 스킬 1개당 **3개 path × 3티어 = 9노드**. path0=기본(무의존), path1=패시브 연계, path2=액티브 연계.
     - 티어1: 3개 path 중 **2개까지만** 투자 가능 — 2번째 path에 티어1을 찍는 순간 남은 1개 path는 영구 잠김.
     - 티어2: 티어1 찍은 2개 path 중 **하나만** 진행 가능(다른 하나는 티어1에서 영구 캡) — named path(1/2)는 여기서부터 **선행조건**(path1=해당 패시브 보유, path2=해당 액티브 보유) 필요. 무의존 path0는 선행조건 없음.
     - 티어3: 티어2 찍은 그 path만 진행. 최종형은 `310`/`031`류(합계 4포인트 투자).
   - 선행조건 매핑(Notion 분기명 그대로 사용): 기본공격→(패시브)암살/(액티브)독수리투하, 회오리→리프레쉬/오브, 오브→지식/낙뢰, 낙뢰→힘/회오리, 독수리투하→건강/회오리.
   - `PlayerSkills.cs`: `EquippedSkill.PathTier[3]`로 상태 관리, `CanEvolvePath`/`EvolveSkill`로 게이팅+적용. path0=피해량 누적 증가, path1=쿨타임 누적 감소, path2=제어기(티어1=둔화, 티어3=둔화+취약) — 기존 4종 젬(에메랄드/토파즈/자수정/가넷) 효과를 이 3개 path에 재배치해 흡수함.
   - 발동 트리거는 기존 젬과 동일(스킬 레벨 임계치 + 보물상자 처치), 2개 이상 스킬이 진화 대기 중이면 `LevelUpUI.ShowTreasureReward()`가 먼저 "어떤 스킬부터 진화할지" 3택1을 보여줌.
   - `GemType.cs` 삭제, HUD 스킬 슬롯 아이콘도 path 투자 여부 표시로 교체.
   - **진화 UI를 BTD6 스타일 카드 트리로 전면 교체 (2026-07-19).** 처음엔 젬 시스템처럼 "3중 택일 카드"로 구현했었는데, 사용자 피드백으로 폐기 — BTD6처럼 스킬당 3행(path)×3열(tier, 왼쪽=Tier1) 카드 그리드를 항상 그대로 보여주고, 그중 지금 투자 가능한 카드만 클릭 가능하게(잠긴 카드는 회색, 보유한 카드는 연두색) 바꿈. 신규 `EvolutionTreeUI.cs` + 씬에 `Canvas/EvolutionPanel`(3×3 카드 그리드, 패널 1400×820, 카드 300×160, UITransition Pop) 추가.
     - **함정 1**: 처음 세로 배치(path=열, tier=행)로 만들었는데 BTD6은 가로(path=행, tier=열) — 다시 만듦.
     - **함정 2**: 스크립트를 패널(비활성 오브젝트) 위에 얹었더니 `Awake()`가 전혀 안 불려서 `Instance`가 null이었음 — Unity는 "씬 시작 시 비활성인 오브젝트는 활성화되기 전까지 Awake 호출 안 함". `LevelUpUI`처럼 항상 활성 상태인 `Canvas` 루트로 옮겨서 해결(패널 GameObject는 필드로만 참조).
     - **함정 3**: 카드 안 설명 텍스트를 오른쪽-앵커(`anchorMin/Max=(1,0.5)`) + 큰 width로 배치했더니 앵커 기준점 계산을 잘못해서 카드 밖으로 67px 삐져나감 — stretch 앵커(`anchorMin=(0,0), anchorMax=(1,1)` + `offsetMin/Max`)로 교체해 해결.
     - 첫 버전은 배경을 완전 불투명 검정(alpha=1)으로 만들어 "너무 까맣다"는 피드백을 받음 — `LevelUpPanel`과 동일한 alpha 0.75로 낮춤.
     - 카드에 프레임 스프라이트(`DamageButton/Frame`과 동일) + path색 아이콘 + `Tier N\n{효과 설명}` 텍스트를 항상 표시(호버 없이 바로 보임).
     - 플레이모드에서 배경색·카드 월드 좌표(설명 텍스트가 카드 경계 안에 들어오는지)·보유/잠김 색상·인터랙터블 상태·클릭 시 투자까지 전부 코드로 직접 검증 완료.
   - 플레이모드에서 직접 `EvolveSkill` 호출해 게이팅 규칙(2-path 잠금, 선행조건 없으면 티어2 차단, 데미지 스케일링) 전부 검증 완료.
   - **플레이버 메커닉 — "새 아트 불필요" 항목 구현 완료 (2026-07-09):**
     - 기본공격: path0=관통(`Projectile.PierceRemaining`), path2=명중 시 미니 독수리 투하(`Projectile.OnHitBonus` → `MiniEagleBonus` 코루틴, 기존 독수리 스프라이트 축소 재사용)
     - 회오리: path0=분열 소환(`1+PathTier[0]`개, 데미지/스케일 분배), path1=리프레쉬 발동 시 전체 스킬 쿨타임 -1초(`ReduceAllCooldowns`)
     - 오브: path0=공중 적 추가피해(`Enemy.IsFlying`), path1=슬로우 강화(`SlowMultiplierBonus`/`SlowDurationBonus`)
     - 낙뢰: path0=재귀 유발(`LightningStorm.RecursiveProcEnabled`, 체인 최대 4회 안전장치), path2=회오리 소환 시 지속시간 증가. **부수 수정**: `LightningStorm.ProcDamage`가 원래 상수 10 고정이라 path0 데미지 스탯 증가가 낙뢰에 전혀 반영 안 되던 기존 버그 발견 → mutable static으로 변경해 활성화마다 갱신되도록 수정.
     - 독수리투하: path0=투하 횟수/간격 파라미터화, path1=**초과체력(오버힐) 시스템 신규 구현**(`PlayerHealth.Overheal`, 데미지 우선 흡수 + 50 캡) + HUD에 노란 오버레이 바 추가(기존 HealthFill 스프라이트 복제·색상만 변경, 새 아트 아님), path2=착탄마다 미니 회오리 생성(**10회 피격 시 소멸** — `Whirlwind.MaxHitCount`, 지속시간 대신 히트카운트 기준)
     - **`Enemy.IsFlying`을 `spawnYOffset > 0` 대신 별도 `isFlying` 필드로 분리 (2026-07-19).** 원래 spawnYOffset 재사용 방식이었는데, 보물상자 블루베리를 "살짝 위로" 올리려고 spawnYOffset을 쓰면 오브의 공중 추가피해 대상으로 잘못 판정되는 부작용 발견 → `isFlying` bool 필드 신설, 종이비행기 블루베리 프리팹에만 `true` 설정. 보물상자는 `spawnYOffset=0.1`(처음 0.25로 했다가 너무 높다는 피드백에 낮춤)로 순수 비주얼 오프셋만 적용.
     - **여전히 미구현("새 아트 필요", 대기 중)**: 회오리→오브 path("개큰 회오리"), 오브→낙뢰 path("오브 설치기"), 낙뢰→힘 path("체인 라이트닝") — 이 3개는 Whirlwind/Orb의 path2, Lightning의 path1에서 기존 범용 CC/쿨감 스탯으로 유지 중. 아트 나오면 교체.
     - 플레이모드에서 오버힐 흡수 연산(데미지 우선 흡수 → 초과분만 HP 차감 → 50 캡) 직접 검증 완료.
   - 패시브 자체는 서브트리 대상 아님(현행 유지, 사용자 확정).

**차순위**
2. 몬스터 1~2종 추가 (기존 4종에 특수 패턴형 추가, 플레이테스트 코멘트: "적 종류가 다양해서 재밌었다, 더 있으면 좋겠다")
3. 밸런싱 재조정 — 서브트리 시스템 + 신규 몬스터 반영해 스테이지 20개 난이도 곡선 다시 튜닝
4. UI 비주얼 강화 — 팀원(진하)이 만든 "Juicy UI" 리소스 적용. **1차 적용 완료 (2026-07-09):**
   - DOTween(HOTween v2, Asset Store 무료판) 설치 완료. JuicyUI의 asmdef가 참조하는 `DOTween.Modules` 어셈블리가 실제로 없어서 `Assets/Plugins/Demigiant/DOTween/DOTween.Modules.asmdef`를 직접 생성해 연결 (원인: 이 DOTween 배포판은 asmdef를 자동 생성하지 않음).
   - `DOTween.defaultTimeScaleIndependent = true` 전역 설정(`GameManager.Awake()`) — 레벨업 패널이 `Time.timeScale=0`으로 게임을 멈춘 채 뜨기 때문에, 이거 없으면 그 상태에서 모든 JuicyUI 애니메이션이 얼어붙음.
   - LevelUpPanel: `CanvasGroup` + `UITransition`(Pop) 추가, `LevelUpUI.Close()`가 즉시 SetActive(false) 대신 `UITransition.Hide()` 애니메이션 재생 후 꺼지도록 수정.
   - 레벨업 선택지 버튼 3개(DamageButton/CooldownButton/HealthButton): `JuicyButton` 추가 (hover/press 스쿼시). 기존 `Button.onClick` 로직은 그대로 유지, 시각효과만 얹음.
   - HP/EXP 바: JuicyHealthBar 컴포넌트는 앵커 리사이즈 방식이라 기존 `Image.Type=Filled`(둥근 모서리 스프라이트, fillAmount 클리핑) 방식과 전제가 달라 스프라이트가 찌그러질 위험 있음 — **컴포넌트 스왑 대신 DOTween `DOFillAmount`로 직접 구현.** `HUDController.cs`에 부드러운 fill 트윈 + 데미지 시 `DOShakeAnchorPos` 흔들림 추가.
   - 패시브/액티브 스킬 슬롯: 신규 스킬 획득 시 아이콘 `DOPunchScale` 팝 효과 추가. **스킬 쿨타임이 다 돌아서 사용 가능해지는 순간에도 같은 팝 효과 추가**(`activeSlotWasOnCooldown` 상태 추적, on→off 전환 시 `PunchIcon` 호출).
   - 플레이모드 진입해 실제 데미지 이벤트로 검증 완료, 런타임 에러 없음.
   - **남은 것**: Tooltip 시스템(스킬 슬롯에 호버 시 설명 표시)은 슬롯 내용이 매 프레임 동적으로 바뀌는 구조라 `TooltipTrigger`의 정적 텍스트 필드로는 안 맞음 — HUDController에 직접 `IPointerEnterHandler` 붙이는 추가 작업 필요, 이번엔 스코프 아웃.

**Known Unknowns (2차 라운드)**
- 회오리→오브, 오브→낙뢰, 낙뢰→힘 3개 path의 고유 플레이버("개큰 회오리"/"오브 설치기"/"체인 라이트닝") — 새 아트 리소스 필요, 아트 나오면 구현
- 진화 시스템 도입 후 스테이지 난이도/스킬 레벨 임계치 재밸런싱 필요 (아직 미조정) — 특히 오버힐 캡(50)·회오리 분열 데미지 분배·독수리투하 오버힐 지급량 등 신규 수치는 감으로 잡은 값이라 플레이테스트로 조정 필요

---

## 보류 중인 결정 / 백로그

- UI 시스템: uGUI + TextMeshPro 확정 사용 중
- 스킬 젬 호환 매핑: 임시로 전체 호환 처리 — 밸런싱 단계에서 스킬별로 제한할지 결정 필요
- 배포(itch.io 등 실제 공개)는 아직 진행 안 함
