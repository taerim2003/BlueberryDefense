# HANDOFF.md — 세션 인계 문서

<!--
📋 유지 가이드 — 이 문서는 "다음 세션이 지금 뭘 해야 하는가"만 담는다.
- 담을 것: 현재 빌드 상태, 미해결 이슈, 다음 할 일. 그것뿐.
- 지울 것: 완료·검증된 피쳐/버그/이슈는 즉시 삭제 (git 히스토리에 이미 남음).
- 세션 로그를 누적하지 말 것. 매 세션 "추가 + 삭제" 둘 다 한다.
- 옮길 것: 구조적 함정 → ARCHITECTURE.md / 게임 스펙·설계 철학 → GDD.md / 씬 배선 → SCENE_MAP.md.
  여기엔 "무엇을 확인·수정해야 하는지"만 남긴다.
- 목표: 한 화면 안에 읽힐 것. (497줄까지 불어난 적 있음 — 그렇게 두지 말 것.)
-->

## 🧭 North Star
> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 액티브 4 + 패시브 4를 진화로 강화해 빌드를 만드는 벙커 디펜스. 스펙은 `GDD.md`.

**세션 시작 시**: 이 문서 → `ARCHITECTURE.md`(코드 작업 시) → `SCENE_MAP.md`(씬 작업 시). `GDD.md`는 grep으로 필요한 절만.

---

## 현재 상태 (2026-07-30, 세션 21 종료)

**빌드**: 컴파일 에러 없음(경고 CS0618 기존 잔존, 무해). 세션21 변경은 전부 **에디터에서 되읽어 값 확인 완료 · 플레이 손맛은 미검증**.

### 세션21에 들어간 것 — 세션20 플레이 피드백 16건 반영

| | 내용 | 어디 |
|---|---|---|
| 🎁 **보물상자 재설계** | 3지선다 폐지 → **뱀서식 자동 보너스**. 1개(운 좋으면 3개, 더 좋으면 5개)의 랜덤 레벨업이 순서대로 떨어진다. 만렙은 다음 시행에서 자동 제외. **레벨업 카드가 아니라 전용 `TreasurePanel`** — 획득 아이콘만 쌓이고 클릭으로 닫는다 | `LevelUpUI.TreasureRollRoutine` · 씬 `Canvas/TreasurePanel` |
| 🐉 **보스 판 = 15·20·25** | 최종 판이 아니어도 그 판엔 보스가 선다(20판 승천에서도 15판은 보스전). **중간 보스 판은 종료 보물상자 + 진화 아이템 1개를 그대로 받는다** — 최종 판만 스킵 | `AscensionTable.IsBossStage` · `EnemySpawner`의 `reservedTail` |
| ✍️ **진화 설명 전면 교체** | BTD6 파라곤식 — **수치를 안 쓴다.** "모든 것을 갈아버리는 하나의 거대한 소용돌이" 식으로 호기심만 남긴다 | `PlayerSkills`/`PlayerPassives`의 `DescribePathEffect`·`GetPathTierTitle` |
| 🚀 **호밍 미사일 강화** | 레벨업이 미사일 **+1/+2씩** 붙고 **쿨감 2회**가 커브에 들어옴. Lv10 = 12발·쿨 4.49s (DPS +59%) | `Prog_Homing.asset` |
| 🏖️ **해안가 전면 재저작** | 위쪽 공간을 쓰는 유닛 비중 **13% → 59%**, 중반·후반 체력 대폭 상향, 15~20판은 `spawnInterval` 0.17→0.09로 **짧고 빽빽하게** | `StageTable_Coast.asset` |
| 🛡️ **방패(벽) 완화** | 두 맵 공통으로 `shieldChance` 약 절반, 대신 `ambushCount` 상향(여기저기서 튀어나오게) | 두 `StageTable` |
| 🥊 **접촉 모델** | 대기 위치를 딸기 한 칸(1.2유닛) 뒤로, **도착 즉시 첫 박치기** | `BalanceConstants` · `Enemy.InitializeSpawn` |

> 그 외: 보스 체력 −30%(1200→840) · 콩콩이 도약 2.4→3.3·체력 22→34 · 패시브 성장률 상향(리프레시 제외) · 스나이핑 최종 진화 피해 너프(×3.4→×2.1) · 스킬트리 노드 가격 전부 절반 · 대회오리 스폰 y −0.7.

### 🔴 다음 세션 테스트 체크리스트

> 세션21 변경 16건은 **전부 숫자만 맞춰 놓은 상태**다. 손맛은 하나도 검증 안 됐다.
> 판 3개로 나눠 놨다. 🅰️만 해도 12건이 걸러진다.

#### 🅰️ 해안가 승천1 완주 (필수 · 이번 세션 대부분이 여기 걸려 있음)

`Map_Wide15` + 승천1(15판). 치트로 스킵하지 말고 **처음부터** — 초반 템포는 안 건드렸는지도 같이 본다.

| 볼 것 | 언제 | 실패 신호 → 손잡이 |
|---|---|---|
| **중반이 안 쉬운지** ⭐최대 가설 | 7~14판 | 한 번도 안 쫄았다 → `ambushCount` 먼저 올릴 것 (체력 아님) |
| **화면 밀도가 과하지 않은지** ⭐ | 8판~ | 뭐가 뭔지 안 읽힘 → `paperPlaneChance`부터 내릴 것 |
| **콩콩이가 더 높이 뛰는 게 보이는지** | **11판**(90%) | 체감 안 됨 → `hopHeight` 3.3에서 더 |
| **서핑 무리** | **5판**(85%) | — |
| **보스가 15판에 나오는지** ⭐신규 | 15판 마지막 | **안 나오면 버그** — `AscensionTable.IsBossStage` 확인 |
| 승천1 15판(=최종)엔 **상자·진화 아이템이 없어야** 정상 | 15판 | 나오면 버그 |
| **보스 체력 −30%가 적당한지** | 15판 | 너무 물렁 → `EnemyDef_Boss.maxHealth` 840에서 상향 |
| **적이 딸기 한 칸 뒤에서 때리는 그림** | 상시 | 돌진이 튄다 → `HeadbuttLungeDuration` 0.32 늘릴 것 |
| **도착 즉시 첫 박치기**가 억울하지 않은지 | 상시 | 억울함 → `InitializeSpawn`의 `headbuttTimer` 초기값 되돌리기 |
| **보물상자가 보너스로 느껴지는지** ⭐신규 | 매 판 끝 | 밋밋/과함 → `TreasureEscalateChance`(0.45)·`TreasureMaxRolls`(5) |
| ↳ 아이콘이 뜨는 속도·크기가 적당한지 | 매 판 끝 | `TreasureRevealInterval`(0.55초) / `treasureIconSize`(130) |
| **진화 설명이 고르고 싶게 만드는지** ⭐신규 | 진화 아이템 먹을 때 | 감이 아예 안 잡힘 → **제목만** 기능어로 되돌리는 절충안 있음 |
| **호밍이 이제 셀 만한지** | 호밍 찍었을 때 | Lv10 12발·쿨 4.49s·DPS 18.1(개편 전 11.4) |
| **스킬트리 진행 속도** | 판 끝나고 트리 | 노드 가격이 절반(15/20/25…)이라 **너무 빨리 풀릴 수 있음** |

#### 🅱️ 해안가 승천2 (15~20판 확인용 · 필수)

15~20판만 보면 되므로 치트로 승천2 세팅 후 진행해도 된다.

- ⭐ **15~20판이 이제 안 늘어지는지** — 물량은 비슷한데 `spawnInterval`이 0.17→0.09라 **같은 물량이 절반 시간에** 쏟아진다. 여전히 길면 `spawnCount`를 깎을 것(간격은 이미 하한).
- ⭐ **후반 난이도가 확 뛰는지** — 체력 곡선을 15판 21 → 20판 85로 올렸다(이전 14.5 → 49.8).
- **20판에도 보스가 나오는지** (승천2의 최종 판이자 보스 판).
- ⭐ **15판(중간 보스 판)에 보물상자와 진화 아이템이 같이 나오는지** — 상자가 먼저 뜨고(파워 스파이크) 그 뒤 보스가 등장하는 순서. 진화 아이템은 판 중반쯤.
- **UFO 판(13·22)에서 프레임이 버티는지** — 일반 판 `ufoChance`도 0.08~0.11로 남겨 뒀다. 렉이 나면 **여기부터 의심.**

#### 🅲 기본 맵 (짧게 · 벽 완화만 확인)

기본 맵은 **방패 빈도 ↓ + 중간 소환 ↑ 두 가지만** 바뀌었다.

- **방패 블루베리가 덜 답답한지** (일반 판 `shieldChance` 약 절반, 벽 판 9·24는 1.0 → 0.7)
- **중간 소환이 늘어난 게 재밌는지, 정신없는지** (`ambushCount` 전반 +2)
- 나머지 판정은 🅰️와 공통

#### ⚠️ 테스트 중 "버그로 오해하지 말 것"

- **승천2 20판을 못 깨는 것** — `extendedHpGrowth = 1.28`은 사용자가 의도적으로 고른 가파른 값. 못 깨면 1.08 방향으로 내리는 것이지 버그가 아니다.
- **최종 판에만 상자·진화 아이템이 없는 것** — 의도된 동작. 그 판을 깨면 게임이 끝나 보상을 쓸 데가 없다. **중간 보스 판(15·20)에는 둘 다 나온다.**
- **광활한 밭 가장자리가 비는 것** — 배경 미제작. 아래 표 참고.

### 🔧 손잡이 (감이 안 맞을 때 만질 곳)

| 바꾸고 싶은 것 | 어디 |
|---|---|
| 해안가 난이도 | `StageTable_Coast.asset` (`ambushCount` → 체력 → 물량 순) |
| 화면 밀도(위쪽 공간) | `paperPlaneChance`·`hopperChance`·`surferChance`. ⚠️ **else-if 사슬이라 앞 확률이 뒤를 갉아먹는다** — `eliteChance`를 올리면 나머지 전부가 같이 줄어든다 |
| 보물상자 후함 | `LevelUpUI.TreasureEscalateChance`(0.45) / `TreasureMaxRolls`(5) |
| 웨이브 리듬 | `StageData.burstSize`/`burstRest`. ⚠️ 무리 스테이지에선 `spawnInterval`이 **무리 안의 간격**이라 0.09~0.13까지 낮춰야 "좌라락"이 된다 |
| 콩콩이 도약 | 프리팹 인스펙터 `hopHeight` 3.3 / `hopDuration` 0.62 / `hopGroundPause` 0.12 |
| 서핑 높이·무리감 | `diveSpawnHeightRatio` 0.03~0.14(물결 위) / `diveAimYSpread` 0.75(무리 두께) / `diveBobAmplitude` 0.25(너울) / `speedVariance` 0.15 |
| 맵의 레인 높이 | `MapDefinition.cameraYLift` (해안가 2.2). ⚠️ **서핑 강하 띠와 한 세트** — 리프트를 바꾸면 서핑 높이도 같이 움직인다 |
| 적 접촉 거리 | `BalanceConstants.ContactStopDistance`(2.55). ⚠️ **`HeadbuttLungeDistance`·`AmbushBandMaxX`와 3종 세트** — 하나만 바꾸면 박치기가 허공을 치거나 중간 소환이 대응 불가가 된다 |
| 후반 난이도 | `StageTable.extendedHpGrowth` → `ambushCount` → 적 체력 순 |

---

## ⚠️ 미해결 이슈 / 주의

- 🔴 **맵 3종이 서로 다른 단계에 있다.** 밸런스를 고치기 전에 **어느 맵인지 먼저 확인할 것.**

  | 맵 | fieldScale | 스테이지 테이블 | 배경 | cameraYLift | 콩콩이·서핑 |
  |---|---|---|---|---|---|
  | 블루베리 밭 | 1 | `StageTable` | ✅ 320×180 | 0 | ✖ |
  | 해안가 | 1.5 | **`StageTable_Coast`** | ✅ 480×270 | 2.2 | ✅ |
  | 광활한 밭 | 2 | `StageTable`(공유) | 🔴 **640×360 미제작** | 🔴 0(미설정) | ✖ |

  → **광활한 밭은 배경 그림이 나와야 나머지가 따라온다.** 배경 없이 실행하면 가장자리가 빈다(버그 아님).
- ⚠️ **13·22스테이지(UFO 벽)는 투하물이 물량 쿼터에 안 잡힌다** — UFO 1대당 12~15마리를 쏟는데 `spawnCount`엔 UFO 수만 잡힌다. 게임에서 제일 무거운 판이 여기서 나온다. 무거우면 **여기부터 깎을 것.**
  🔴 세션21에 해안가 일반 판의 `ufoChance`도 0.08~0.11로 남겨 뒀다 — **프레임이 떨어지면 제일 먼저 의심할 곳.**
- ⚠️ **보스 판이 15·20·25 셋으로 늘었다** — 그 판은 스테이지 종료 보물상자가 안 나온다(`treasureStage = !isBossStage`). 25판 승천이면 상자 3개를 손해 본다. 신경 쓰이면 보스 판에도 상자를 주도록 `EnemySpawner`를 고칠 것.
- ⚠️ **`GetActiveSkillDescription`(산탄)이 아직 실제와 다르다** — "5초 동안 공격 횟수 +1"만 적혀 있는데 실제로는 산탄 알도 같이 발사한다. 세션21에 호밍·스나이핑·독수리의 개수 표기는 고쳤고 산탄만 남았다.
- ⚠️ **판 전환/게임오버 시 비행 중인 경험치 보석의 XP는 유실**(timeScale 0이면 멈춘 채 씬이 끝남). 무해하다고 판단했으나 신경 쓰이면 도착 강제 처리.
- ⚠️ **ESC의 "타이틀로 돌아가기"는 확인창 없이 한 번 클릭**에 게임오버 흐름을 탄다. 오발이 신경 쓰이면 2단계 확인.
- ⚠️ **Windows 빌드 세이브는 파일이 아니라 레지스트리** — `HKCU\Software\DefaultCompany\BlueberryDefense`. 삭제: `reg delete "HKCU\Software\DefaultCompany\BlueberryDefense" /f`(게임 종료 상태에서).
- **에디터 인스펙터 예외(무해)**: 플레이 진입 시 `ObjectPreview.DrawPreview ... Image destroyed`.
- **노션 토큰 노출**(기존): 폐기·재발급 권장.
- (기존) **사운드 볼륨 이중곱** — 우선순위 낮음.

> ✅ **되살리지 말 것 (틀린 것으로 확인된 옛 메모)**
> - "시작 캐릭터 풀을 줄여야 루프가 성립" — **틀림.** 스킬 해금 게이팅은 스킬트리 해금 노드가 이미 담당한다(`MetaBonuses.SkillUnlockedForRun`). `CharacterDefinition.allowedActivePool`은 캐릭터 개성용이고 둘은 독립.
> - 세션13~19의 "실플레이 확인 대기" 항목 — 전부 확인 완료(보석·의성어·라이더 방향·UFO 수송선·회오리 중첩·트리 안개·벽 스테이지·중간 소환·오브 관통 예산·적 풀링).

---

## 다음 할 일

1. 🔴 **테스트 체크리스트 🅰️🅱️🅲 소화** (위 참조). 세션21 변경 16건의 정답 확인이 전부 여기 걸려 있다. **결과를 받고 나서** 아래 2~5번을 시작할 것.
2. **공중 유닛 2종 추가** — 세션19 넓은 맵 피드백 중 마지막 남은 항목. 하늘을 채울 게 종이비행기·UFO뿐이다. 필요한 것: **위아래 지그재그**로 오는 유닛 / **직선으로 공중을 가로지르는** 유닛.
   → 세션21에 하늘 비중을 4~5배로 올려 놓아서 **종류가 부족한 게 더 두드러진다.** 절차는 세션20의 콩콩이가 최신 예시(ARCHITECTURE "새 적 타입" 행 + `isHopper` 한 벌).
3. 🔴 **`Map_Wide20`(광활한 밭) 배경 640×360** — PPU **18**로 임포트 후 `background`에 배정. 그 뒤 `cameraYLift`·전용 스테이지 테이블·신규 적 로스터를 한 번에 정리.
4. **승천 루프 검증** — 승천1 클리어 → 승천2 해금 → 못 깸 → 트리 해금 → 재도전. (노드 가격이 절반이 되어 트리 진행이 훨씬 빨라졌다 — 너무 빠른지도 같이 볼 것.)
5. 트리 UI 레이아웃 손보기.

### 남은 청소거리 (급하지 않음)
- **`OrbCanHitFlying` 스킬트리 노드** — 히트박스 판정 도입 후 **UFO에만** 의미가 있다. 다른 효과로 용도 변경 필요(예: 오브 공전 반경 확대).
- **화살 발사점 `FlyingArrowSpawnRaise = 0.65` 땜빵** — 대공 진화를 "발사각/히트박스 확장"으로 재설계할 때 정리.
- **`EvolutionTierTextTableSO`** — 여전히 (path, tier) 좌표계라 새 루트/티어와 어긋난다. 지금은 설명 폴백이 코드라 무해하지만, 에디터에서 텍스트를 덮어쓸 거면 루트 좌표계로 바꿔야 함.
- **보스 박치기 피해** — `HeadbuttDamageScale`(0.35) 하나로 전 적을 곱해서 보스는 1회 70. 보스만 별도 배율이 필요할 수 있음.
- **경험치 보석이 임시 노란 사각형**(`UI_SolidFill` 44px) — 전용 도트를 그리면 `XpGemFlight.gemSprite`만 교체.
- **딸기 전용 초상화 미완**(`Player.png` 임시) — 그리면 `Char_Strawberry.portrait`만 교체.
- **`EvolutionPanel` 씬 노드가 아직 3×3** — 진화가 2×2로 바뀌어 런타임에 5칸을 끈다. 정리하려면 SCENE_MAP 참고.

---

## 🧩 콘텐츠 확장 = 순수 데이터
- **밸런스 편집**: `Window > Blueberry Defense > Balance Dashboard` 또는 인스펙터 — `Prog_*`·`Passive_*`·`EnemyDefinition`·`MapDefinition`·`StageTable`·`ScalingTable`·`CharacterDefinition`·`AscensionTable`. 전역 상수는 `BalanceConstants.cs`.
- **스킬트리 편집**: `Blueberry Defense > Skill Tree Editor`. 백업본 `MainSkillTree_Backup.asset`.
  **새 강화 노드**: 에디터에서 노드를 만들고 그 **id**를 `SkillEffects.Compute`의 case에 추가(효과는 id 기준 레지스트리 — 에셋 `effect` 필드는 신뢰 안 함).
- **새 맵/캐릭터**: `MapDefinition`/`CharacterDefinition`을 만들어 Title 씬의 각 Select UI 배열에 드래그.
- **치트**: `Window > Blueberry Defense > Cheat Window` — 정수 지급, 만렙, 스킬트리 초기화, 승천 등급 설정.

## 🎨 스킬 이펙트 픽셀 크기 컨벤션
이펙트 스프라이트는 배경과 픽셀 밀도가 안 맞아, **그림은 작게 그리고 엔진에서 1.5배로 표시**(플레이어 프리팹 `localScale=1.5`에 맞춤).
- 새 이펙트 프리팹은 기존값에 곱하지 말고 `localScale`을 **직접 (1.5,1.5,1.5)로** 세팅.
- PPU **32**, 기본 맵 카메라 ortho 5(화면 세로 10유닛).
- 공식: `원본 그림 px = (목표 월드 유닛 × 32) ÷ 1.5`
- **배경만 PPU 18** — 스프라이트가 자기 px/PPU만큼 커져 화면을 정확히 덮는다(1배=320×180px, 1.5배=480×270, 2배=640×360).
