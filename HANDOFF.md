# HANDOFF.md — 세션 인계 문서

<!--
📋 유지 가이드 — 이 문서는 "다음 세션이 지금 뭘 해야 하는가"만 담는다. 길어지지 않게 유지할 것.
- 담을 것: 현재 빌드 상태, 미해결 이슈, 다음 할 일. 그것뿐.
- 지울 것: 완료·검증된 피쳐/버그/이슈는 즉시 삭제 (git 히스토리에 이미 남음).
- 세션 로그를 누적하지 말 것. 매 세션 새로 쓰는 게 아니라, 끝난 항목은 지우고 새 항목만 갱신.
- 게임 스펙(스킬 수치·시스템 규칙 등)은 여기 말고 GDD.md에. 여기엔 "무엇을 확인/수정해야 하는지"만.
- 목표: 항상 한 화면 안에 읽힐 것.
-->

## 🧭 North Star
> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 액티브 4 + 패시브 4를 진화 트리로 강화해 빌드를 만드는 벙커 디펜스. 상세 스펙은 `GDD.md` §4 참고.

---

## 현재 상태 (2026-07-29, 세션 16)

**빌드**: 컴파일 에러 없음(경고 CS0618 기존 잔존, 무해).
**세션16 = 세션15 진화 시스템의 플레이테스트 피드백 4건 수정 + 후반 밸런스 상향.**
에디트모드 로직 테스트 12항목 통과(만렙 상한·진화 게이트·루트 잠금·2차 진화). **실플레이 검증은 아직.**

> 🔴 **다음 세션 시작하자마자 확인할 것**:
> 1. **진화 모달이 만렙(Lv.10)에서만 뜨는지.** 그리고 만렙 스킬이 레벨업 3지선다에서 빠지는지.
> 2. **판당 진화 아이템 3개(7/9/13)가 실제로 쓸모 있는지.** ⚠️ 게이트가 10이 되면서 **스테이지 7 아이템은 거의 항상 폴백(3레벨업)으로 빠질 것**이다(그 시점 주력 스킬이 ~5레벨). 설계상 폴백이 "만렙에 3레벨 더 가까워짐"이라 낭비는 아니지만, **3개 중 2개 이상이 폴백이면** 아이템 드랍 스테이지를 뒤로 옮기거나(7→8·10·13) 개수를 늘릴 것 (`StageData.evolutionItemDrops`).
> 3. **레벨업 선택지가 마르는지** — 8슬롯 × 만렙 10 = 판당 최대 80레벨. 그 뒤로는 '정수 +10' 대체 보상만 뜬다. 후반에 정수만 계속 뜨면 아이템(=진화 리셋)을 더 줘야 한다는 신호.
> 4. **후반 난이도** — 아래 밸런스 절 참고. 적이 한 방에 안 죽는지, 대신 15스테이지가 지루한 스펀지가 되진 않는지.
> 5. **2×2 트리 레이아웃**이 보기 괜찮은지 — 3×3 패널에서 5칸을 끈 거라 오른쪽/아래가 텅 빈다. 어색하면 씬에서 2×2로 재배치(§SCENE_MAP).
> 6. **진화 후 체감** — 피해 ×1.5 + 옛 티어 2개 동시 적용이 "확 세졌다"인지, 과한지.

### 세션16 변경 (플레이테스트 피드백)
- **만렙 = 10 신설** (`BalanceConstants.MaxSkillLevel`). 액티브·패시브 모두 Lv.10에서 레벨업이 막히고 레벨업 3지선다 후보에서 빠진다. 진화하면 Lv.1로 리셋되어 다시 10까지 큰다. → 이제 `Prog_*` 커브(정확히 10레벨 저작)를 벗어나는 일이 없다. **11+ 옛 폴백 커브는 사실상 죽은 코드가 됨.**
- **진화 게이트: 누적 레벨 5/10 → 표시 레벨 10(만렙) 하나로 통일** (`EvolutionRoutes.RequiredLevel`). `TotalLevel`은 이제 표시용일 뿐 게이트가 아니다. "만렙 찍고 진화 → Lv.1 → 다시 만렙 → 2차 진화".
- **루트 잠금(연계 조건) 부활** — 세션15에 폐지했던 걸 되살렸다. 기존 path 좌표계가 그대로 조건: **path1 = 패시브 연계 / path2 = 액티브 연계 / path0 = 무조건**. 연계 스킬을 **보유**하면 열린다(Lv.5 조건은 안 걸었다 — 아이템이 희소해서 레벨까지 걸면 아이템이 버려지는 판이 생김). 두 루트가 다 잠긴 스킬은 진화 목록에 안 뜨고, 진화 카드 설명은 **열린 루트 이름만** 나열한다. 트리에서 잠긴 노드는 `[잠김 — '건강' 필요]`로 표시되고 클릭 불가. (예: 독수리 투하 route0=건강 / route1=회오리)
- **스테이지 종료 보물상자 = 전 스테이지 1마리로 통일** (11+ 2마리였음).
- **기본공격 추가 발사 = 동시 발사 → "두두두둑" 연사** ([PlayerSkills.cs `BasicAttackBurst`](Assets/Scripts/PlayerSkills.cs)). 예전엔 추가 발사체가 **같은 프레임에 0.4씩 위로 쌓여** 한 덩어리로 보여 여러 발 맞는 게 안 보였다. 이제 첫 발만 즉시, 나머지는 **0.07초 간격**으로 **위·아래 번갈아 ±0.13씩** 어긋나게 나간다(만렙 4발 = 0.21초). 수치는 `BalanceConstants.BasicAttackBurst*`. **세로 오프셋은 순수 연출** — 화살 히트박스가 세로 2.4유닛이라 명중 판정은 그대로다.
- **치트창에 "만렙" 버튼** 추가 — 진화 게이트까지 한 번에 올려 테스트용.

### 세션14 변경 (요약)
- **라이더 스프라이트 수정**: 좌우반전(`m_FlipX 1→0`, 라이더 원본이 반대 방향) + 화질(3프레임 `filterMode` Bilinear→Point, DefaultTexturePlatform 압축 off). → *방향은 원본 그림 기준 추론, 플레이 확인 필요.*
- **UFO 수송선 튜닝**([Enemy.cs](Assets/Scripts/Enemy.cs)): 등장 x를 우측 1/3 회피(좌측~중앙, `-halfW*0.6~+halfW*0.3`) / 호버 고도 40%→28% / 단발 투하 → **1초 간격 3회 투하**(`carrierDropCount`,`carrierDropInterval`) / 투하당 3~4→**4~5마리, 그중 1마리 리젠트**(`carrierRegentPrefab`). 총 12~15마리/캐리어라 과하면 `carrierDropCount`·`carrierDropMin/Max`로 조절.
- **거대 회오리 + 미니 회오리 동시 발동**([PlayerSkills.cs:1229](Assets/Scripts/PlayerSkills.cs#L1229)): `FireWhirlwind`에서 거대 회오리(path2 T2)일 때 미니 회오리 스폰(path0)을 건너뛰던 버그 → 미니 스폰을 조건 밖으로 빼 독립 발동.
- **보스 사망 UFO 위치**([Enemy.cs](Assets/Scripts/Enemy.cs)): 분출 캐리어가 화면 위로 순간이동 후 플레이어 위로 하강하던 문제 → `EmergeAsBurstCarrier`로 **보스 죽은 자리에서 등장·투하 없이 상승 퇴장**.
- **스킬트리 안개 재설계**([SkillTreeUI.cs](Assets/Scripts/SkillTreeUI.cs)): 해금 노드 바로 옆(1링크) 노드는 `?` 마스킹 걷고 **이름·효과·비용 전부 공개**(미보유는 어둡게). 2링크 이상은 기존대로 존재 은폐. `ColMasked` 상수 제거.

### 세션14 추가 — 연출(juice) 3종
- **💎 경험치 보석 → 경험치 바로 흡입** (`XpGemFlight.cs` 신규, 씬 `Canvas/XpGemLayer`): 처치 지점에서 보석이 툭 튀어나와 **경험치 바의 "현재 차 있는 끝 지점"**(fillAmount 위치 — 30% 차 있으면 바의 30% 지점)으로 빨려든다. **XP는 처치 순간이 아니라 보석 도착 순간에 적립** — "보석이 꽂히니까 바가 오른다"는 인과가 화면에 보이게. 도착 시 바가 세로로 튕김. 풀링·동시 상한(120) 있음. 부작용으로 보물상자 킬의 `AddXP`/`ShowTreasureReward` 순서 경합([LevelUpUI.cs:355](Assets/Scripts/LevelUpUI.cs#L355) 주석 참고)도 자연히 사라짐.
- **💥 만화 의성어 BAM!/BOOM!/SMASH!** (`ComicBurst.cs` 신규, `GameManager`에 부착): ①**멀티킬** — 0.6초 창 안 누적 처치 **3/6/10**에서 한 단계씩 승급하며 무리 한가운데서 터짐(한 콤보에 최대 3번 = 대규모 쓸이가 그대로 연출이 됨). ②**보스 처치** = 무조건 SMASH. ③**UFO 수송선을 투하 전 격추** = BOOM(대공 보상).
- **🤜 적 접촉 모델 교체 — "닿으면 자폭" → "앞에 쌓여서 계속 박치기"** ([Enemy.cs](Assets/Scripts/Enemy.cs), 수치는 [BalanceConstants.cs](Assets/Scripts/BalanceConstants.cs)): `OnTriggerEnter2D`(1회 피해 후 `Destroy`) **삭제**. 이제 적은 플레이어 앞 `ContactStopDistance`(0.9)에서 멈춰 `HeadbuttInterval`(1초)마다 `damage × HeadbuttDamageScale`(0.35)만큼 때리고, **죽여야만 사라진다**. 뒤 적은 `BlockedAhead`로 막혀 **중심간격 0.083으로 겹쳐 쌓임**(스프라이트 폭 1.13유닛 대비 거의 완전히 포개짐 — 삐져나온 두께로만 물량감). 스폰 시 **y를 ±0.13 랜덤**(`EnemySpawnYJitter`)하게 흔들어 자로 잰 듯한 일렬을 깬다.
  - **돌진 박치기**: 제자리에서 멈춰 있다가 주기마다 `HeadbuttLungeDistance`(0.5)만큼 튀어나갔다 sin 곡선으로 복귀. **피해는 최전방 도달 순간**(k=0.5)에 들어간다. 돌진 여유를 만들려고 `ContactStopDistance` 0.9→**1.35**로 물러세움.
  - **멈춰 있는 동안 걷기 애니메이션 정지**(기절과 같은 `SetAnimatorFrozen` 스위치 공유).
  - **추월**: 자기보다 **느린 적은 통과**한다(라이더가 일반 블루베리에 막히지 않음). 단 **이미 멈춰 선 적(`isHolding`)은 속도 무관하게 막는다** — 안 그러면 추월한 적이 선두를 통과해 같은 자리에 겹쳐 서고 **선두가 여러 마리가 되어 피해가 배로** 들어간다.
  - ⚠️ **간격 판정은 반드시 "중심 간 x거리"로 할 것.** Physics2D 프로브 충돌을 그대로 정지 조건으로 쓰면 앞 적의 **콜라이더 가장자리**에서 멈춰 간격이 `spacing + probeRadius + 반폭`(≈1.0)이 되고, 스프라이트 폭과 비슷해져 **줄 서 있는 것처럼 보인다**(한 번 이 함정에 빠졌음). 프로브는 후보 탐색용(`EnemyStackSearchRadius`)일 뿐. `EnemyLaneTolerance`(0.6)로 **지상 줄과 공중 줄이 서로 안 막음**. 실제 피해를 주는 건 각 레인의 **선두 1마리뿐**(예: 기본 블루베리 8→3/초).
  - ⚠️ **적이 더 이상 스스로 사라지지 않음** → GameManager의 "잔몹 0" 클리어 조건상 **못 죽이면 스테이지가 안 넘어간다**(천천히 깎이며 버티는 상태). 의도된 실패 형태인지 플레이로 확인 필요.
  - ⚠️ **보스 damage=200** → 박치기 1회 70. 여전히 즉사급이라 **보스만 별도 배율이 필요할 수 있음**.
- **✈️ 대각선 강하 비행 (종이비행기만)** — 종이비행기가 좌진 행진을 그만두고 **화면 위에서 스폰해 플레이어로 직선 수렴**한다. 방향은 **스폰 시 1회만 결정**(유도 아님)이라 여러 마리가 부채꼴로 모여들고, 기수도 진행 방향으로 돌아간다. ✅ **검증**: 높이 2.16~4.43으로 넓게 흩어져 스폰, 플레이어 높이(y≈0)로 내려와 박치기 선(x≈6.5)에 정확히 정지.
  - **스폰 높이는 비율**(`diveSpawnHeightRatioMin/Max` = 0.4~0.95) × 화면 절반 높이 → ortho 5 기준 **y 2.0~4.75**(화면 위쪽 중간 ~ 거의 최상단). 비율이라 나중에 카메라 크기를 바꿔도 "화면 어디쯤"이 유지된다. 완만한 강하와 급강하가 섞여 매번 다른 각도로 들어온다.
  - **`isFlying`(분류)과 `requiresAntiAir`(대공 필요) 분리**가 핵심. 판정용 게이트를 `RequiresAntiAir`로 바꿔서:
    - **UFO = `requiresAntiAir` true → 지금과 완전히 동일**(13스테이지 대공 벽 유지).
    - **종이비행기 = false → 오직 히트박스로만 판정.** 높이 있는 동안은 지상 스킬이 안 닿고, 가까워질수록 대부분의 스킬에 맞기 시작한다(이진 판정 → 그라디언트).
    - `isFlying`은 그대로 남아서 **비행 추가피해·호밍 우선타겟은 종이비행기에도 계속 적용**된다.
  - 게이트 교체 지점: `Projectile`·`Orb`·`Whirlwind`(타격 + 추적 대상 선정 2곳).
  - 🐞 **한 번 크게 터진 버그**: `requiresAntiAir` 기본값을 `true`로 뒀더니 **이 필드가 직렬화 안 된 기존 프리팹 전부**가 대공 전용으로 잡혀 **지상 적이 아무 스킬에도 안 맞았다**(화살이 그냥 통과). 기본값을 `false`로 바꾸고, **모든 적 프리팹에 값을 명시적으로 기록**해 해결(UFO만 1). ⚠️ *스크립트 기본값만 바꾸면 이미 임포트된 에셋의 캐시값은 안 바뀐다 — 반드시 프리팹에 써 넣을 것.*
  - ⚠️ 스킬트리 `OrbCanHitFlying` 노드는 **이제 UFO에만 의미가 있다**. 논의대로 "오브 공전 반경 확대" 같은 걸로 갈아끼울지 재검토 필요.
- **🎯 관통 판정 버그 수정 (적이 겹치면서 드러난 잠복 버그)** — `Destroy(gameObject)`는 **프레임 끝에 처리**되므로, 같은 물리 스텝에 이미 잡힌 나머지 `OnTriggerEnter2D` 콜백이 그대로 실행된다. 그래서 **관통 예산이 0이어도 그 프레임에 닿은 적을 전부 때렸다**(기본공격 1발이 뭉친 무리를 통째로 쓸어버림). 적이 흩어져 있던 시절엔 안 드러났을 뿐 원래 있던 버그.
  - [Projectile.cs](Assets/Scripts/Projectile.cs): `consumed` 플래그 + 소멸 시 콜라이더 즉시 비활성(`Consume()`). ✅ **검증**: 0.05 간격으로 완전히 겹친 적 5마리에 관통0 화살 → 정확히 1마리, 관통2 화살 → 정확히 3마리.
  - [Orb.cs](Assets/Scripts/Orb.cs): 방패에 막혀 소멸하는 경로에 같은 `consumed` 가드(방패 2마리와 동시에 겹쳤을 때 중복 타격 방지).
  - `HomingMissile`·`SmallOrb`는 이미 `hit`/`hasHit` 가드가 있어 정상. `Whirlwind`·`Orb`의 지속 타격은 원래 광역이라 다수 타격이 정상.
  - ⚠️ **새 투사체를 만들 때 이 함정을 반드시 기억할 것** — 단일/제한 타격이면 `Destroy` 호출만으로는 부족하고 **플래그 가드가 필수**.
- **🎨 신규 아이콘 배선**: `태양정수`→EssencePickup, `체력회복`→HeartPickup 스프라이트 교체. 신규 스프라이트 5종 임포트 교정(PPU 100→**32**, Bilinear→**Point**, 기본 플랫폼 압축 off) — 세션14 라이더 수정과 같은 처방.

> ⚠️ **연출 수치는 전부 인스펙터 노출** — 보석은 `Canvas/XpGemLayer`의 XpGemFlight(흩어짐 거리·속도·가속·보석 크기·색), 의성어는 `GameManager`의 ComicBurst(단계별 크기·수명·전역 간격·멀티킬 기준 3/6/10). **감이 안 맞으면 코드 말고 여기서 조절.** 단, **적 접촉 모델 수치만 `BalanceConstants.cs`(const)** — 인스펙터 아님.
>
> 🟡 **경험치 보석은 지금 임시 노란 사각형**(`UI_SolidFill` + 노랑, 44px). 원래 `Icon_Emerald`였는데 30px·고속이라 안 보였음 → 크게·느리게 바꿔 확인 후 다시 빠르게(420/2400) 조정. **전용 보석 도트를 그리면 `gemSprite`만 교체**하면 됨.

### 세션14 추가 — 레벨업 커브 전면 재작성
> **진단**: 9개 스킬이 전부 같은 3슬롯 순환(`쿨×0.95`/고유/`피해×1.2`)이라 **보이는 스탯이 0~33%**였고, 모든 스킬의 L2가 `쿨×0.95`(5% 빨라짐=체감 불가)라 **레벨업 3번을 해야 뭔가 느껴졌다.** 게다가 10레벨 성장이 피해 1.73배인데 15스테이지 적은 체력 8.6배 — **5배 갭**이라 레벨업 자체가 무의미했다.

- **설계 원칙**: *레벨업 = 정체성을 "양"으로 키움 / 진화 = 정체성을 "바꿈".* 둘이 같은 손잡이를 잡으면 진화가 심심해진다.
- **슬롯 패턴(L2~L10, 9칸)**: `보이는축① / 피해×1.3 / 보이는축② / 피해 / ①  / 피해 / ② / 피해 / ①(크게)` → **보이는 스탯 5/9(56%)**, L2부터 즉시 보임.
- **레벨업에서 쿨감 제거** — 안 보이는데 슬롯만 먹었다. 쿨감은 진화·스킬트리가 담당. (되감기만 예외: 피해가 없는 유틸이라 쿨감이 곧 성장)
- **성장 구간을 1~10레벨에 몰빵** — 진화 시 LV1로 리셋되므로 11+는 도달하지 않는다. 저작 레벨 49개 → **9개**.
- **시작값 전부 50% 하향** + 개수형 시작값도 하향(호밍 5→3, 독수리 투하 3→2, 스나이핑 연사 5→3).
- **신규 `SkillStat` 2개**(enum **맨 뒤에만** 추가할 것 — 중간에 끼우면 기존 에셋의 정수 직렬화가 밀림):
  - `TickRate` = 반복 타격 간격 배율 → 회오리 피해 주기, 독수리 투하 간격
  - `MaxTargets` = 동시 상대 적 수 → 오브 동시 타격, 스나이핑 저격 대상
  - `ProjectileCount`는 "**발사/투하 수**"로 재해석해 기본공격·스나이핑 연사·호밍·독수리·산탄이 공유.
- **오브에 동시 타격 제한 신설**([Orb.cs](Assets/Scripts/Orb.cs)) — 겹친 적을 전부 갈지 않고 **가까운 순 N마리까지만**. 2→5마리가 오브의 주 성장축.
- **🔫 산탄 재설계** — 예전엔 버프만 걸어서 **시전해도 화면에 아무 일도 안 일어나는 유일한 스킬**이었다. 이제 왼쪽으로 **부채꼴 산탄을 실제로 발사**(`SmallOrb_Skill` 재사용, 반각 22°) + 기존 타수 버프 유지. 알 개수 3→8발이 주 성장축.
- **낙뢰 하향** — 확률 60%→**25%**(헤드룸이 40%뿐이라 +3%p가 안 보였음), 피해 12→**6**. ⚠️ **낙뢰만 `Prog_Lightning.baseDamage`(=0)가 무시되고 `LightningStorm.BaseProcDamage`가 실제 시작 피해**다(`GetDefaultDamage`가 낙뢰만 특수 처리).
- **1~2스테이지 물량 감소**(14→9, 18→13) — 시작 스킬이 약해진 만큼 극초반이 과해지지 않게. **간격은 그대로**(밀도가 아니라 총량만).

> ⚠️ **11레벨 이상은 옛 코드 폴백**(`SkillProgression.DefaultStep`의 3슬롯 순환)을 탄다. 진화로 LV1 리셋이 되니 보통은 안 닿지만, 진화 아이템을 못 먹으면 도달 가능하다.

### 세션15 — 진화 재설계 (3경로×3티어 → **2루트×2티어**, 아이템 기반)

> **핵심 트릭**: 저장 구조를 안 바꿨다. `PathTier[3]`을 **그대로 두고** [EvolutionRoutes.cs](Assets/Scripts/EvolutionRoutes.cs)가 좌표만 번역한다 →
> `Fire*`/`TryUseSkill`이 `PathTier[n] >= t`를 읽는 **115군데를 한 줄도 안 고쳤다.**
> ```
> 새 루트 r(0/1) → 기존 path 인덱스 (스킬마다 다름, RoutePath 표)
> 새 티어 1      → 기존 T1+T2 동시 적용  → PathTier[path] = 2
> 새 티어 2      → 기존 T3 적용          → PathTier[path] = 3
> ```
> 한 번의 진화가 옛 티어 2개를 한꺼번에 줘서 **진화 1회 체감이 예전의 2배**가 됐다.

- **열리는 조건이 레벨 → 아이템으로 바뀜.** (세션16: 여기에 **만렙 도달** 조건이 다시 붙었다 — 아이템 + Lv.10 둘 다 필요.)
- **진화 아이템**: 벽 스테이지(7/9/13)에 **확정 등장**하는 엘리트가 드랍. `StageData.evolutionItemDrops`(신규 필드)가 0보다 크면 그 수만큼 엘리트가 물량 균등 분할 지점에 나오고, `EnemySpawner`가 `Enemy.MarkEvolutionItemCarrier()`로 표식을 찍는다. **판당 3개.**
- ~~**게이트**: 1차 = 누적 레벨 5, 2차 = 누적 레벨 10~~ → **세션16에 만렙(표시 레벨 10) 하나로 교체.**
- **진화하면**: 이름 변경(`DisplayName`) + **표시 레벨 1로 리셋** + **피해 ×1.5 / 쿨 ×0.9 즉시 도약**. 레벨업 커브를 처음부터 다시 타므로 진화형 전용 커브 에셋은 **필요 없었다**(`Prog_*` 재사용).
- **2차는 1차에서 고른 루트 고정** — 다른 루트 시도는 `EvolveSkill`이 거부.
- **버린 path**: 각 스킬에서 "레벨업 축과 겹치거나 밋밋한" 1개를 뺐다(스나이핑 타겟수·호밍 미사일수·독수리 투하수·산탄 타수·되감기 시간 = 전부 이제 레벨업 축 / 회오리 순수 쿨감 / 오브 공중추가피해 / 낙뢰 회오리연계). 패시브는 전부 path0(같은 스탯 더 주기)를 뺐다.
- ~~**연계 조건(연계 스킬 Lv.5) 폐지**~~ → **세션16에 "연계 스킬 보유"로 부활**(레벨 조건은 없음).
- **UI는 씬 수정 없이 재사용**: 진화 트리 9칸(3×3) 중 **왼쪽 위 2×2만 쓰고** 나머지는 `SetActive(false)`. 노드 제목 = **진화 후 이름**, 설명 = 옛 티어 설명들을 이어붙인 것 + 피해 도약 표기.
- **아이템 사용 흐름**: 픽업 흡수 → `LevelUpUI.ShowEvolutionReward()`(진화 가능 목록 3지선다) → 고르면 진화 트리(루트 선택) → 적용.
- **폴백**: 진화 가능한 게 하나도 없으면 "**랜덤 스킬 3레벨업**"이 된다. 고를 게 없는 보상이라 3지선다가 아니라 **가운데 슬롯에 카드 한 장**만 떠서 누르기만 하면 된다(세션16). ⚠️ 씬의 옵션 카드는 y=+190/0/-190에 **고정 배치**라 레이아웃 그룹이 없다 — 그래서 선택지가 1개일 때만 `LevelUpUI.SlotForOption`이 옵션0을 슬롯1(가운데)로 보내고 `OptionForSlot`이 클릭을 되돌린다.
- **HUD 슬롯 보석** = 이제 투자한 path 수가 아니라 **진화 횟수(0~2)**.

> 신규 파일: [EvolutionRoutes.cs](Assets/Scripts/EvolutionRoutes.cs)(루트 매핑 + 진화 이름표 전체), [EvolutionItemPickup.cs](Assets/Scripts/EvolutionItemPickup.cs), `Assets/Prefabs/EvolutionItemPickup.prefab`(Icon_Amethyst).
> ⚠️ **진화 이름 36종 + 패시브 20종은 코드(`EvolutionRoutes.EvolvedName`)에 있다.** 마음에 안 들면 거기만 고치면 됨.
> ⚠️ **루트 배정도 `RoutePath` 표 한 곳**에서 바꿀 수 있다 — 버린 path를 되살리고 싶으면 그 줄만 수정.

### 루프 설계 (확정)
> 스킬트리는 **자원 1개·되돌리기 불가**의 영구 성장. **승천 = 슬더식 난이도 등급**(맵을 난이도별로 다시 도전).
> "지금 빌드론 못 깸 → 정수 벌어 스킬 해금·강화 → 클리어 → 다음 승천" 반복.

### 이번 세션 변경 (요약)
- **🛸 UFO 리워크 = 수송선(carrier)**: 좌진 행진 유닛 → **화면 위에서 하강→호버하며 부대 투하→다시 상승 퇴장**. `Enemy.cs`에 `isCarrier` 모드 추가(하강/호버/상승 상태기계, Y좌표는 `Camera.main` 기준 런타임 계산). **투하 전 격추하면 부대 안 나옴**(대공 보상). 투하물=기본 블루베리 3~4마리, 캐리어가 받은 스테이지 배율을 물려받음(`ApplyStageMultipliers` 기억). UFO HP 40→**45**(요격 대상이라 낮게), 하강속도 3. `EnemySpawner`의 `currentStage>=11` UFO 게이트 제거(이제 `ufoChance` 데이터가 배치 제어).
- **🏇 라이더 블루베리 신규 적**: 종이비행기의 지상판 — 빠른 지상 돌진·저HP. `EnemyDef_Rider`(속도4.5/피해10/HP28/xp7), `Enemy_RiderBlueberry.prefab`(Enemy_Blueberry 복제), 3프레임 워크 애니(`RiderBlueberry_Walk.anim`+`.controller`). `StageData.riderChance`+`MapDefinition.riderEnemyPrefab`+스포너 분기 추가. 라이더 스프라이트는 PPU 100→**32**·Single·중앙피벗으로 재임포트(다른 적과 정렬).
- **🧱 벽 스테이지 + 11+ 밸런싱**(StageTable): **9=방패 벽**(shield 1.0, 관통/광역 시험), **13=UFO 벽**(ufo 1.0, 대공 순간화력 시험) 신규. 7=종이비행기 벽 유지. 라이더는 5·6·8·10·11·14에 뿌림. 11~15 HP/피해/물량 상향(예: 11 hp3.8→4.3, 15 hp7.5→8.6, spawn 90→102).

> **벽 스테이지 설계 철학**: BTD6식 — 난이도가 평탄히 오르는 게 아니라 **특정 능력을 크게 시험하는 near-pure 스테이지**를 중간중간 배치. 7=대공/9=관통·광역/13=대공순간화력. 새 벽 추가 시 이 언어를 따를 것.

> ⚠️ **Windows 빌드 세이브는 파일이 아니라 레지스트리** — `HKCU\Software\DefaultCompany\BlueberryDefense`. `AppData/LocalLow`엔 로그만 있음. 수동 삭제: `reg delete "HKCU\Software\DefaultCompany\BlueberryDefense" /f` (게임 종료 상태에서).

---

## 🧩 콘텐츠 확장 = 순수 데이터
- **밸런스 편집**: `Window > Blueberry Defense > Balance Dashboard` 또는 인스펙터 — `Prog_*`·`Passive_*`·`EnemyDefinition`·`MapDefinition`·`StageTable`·`ScalingTable`·`CharacterDefinition`·**`AscensionTable`**. 전역 상수는 `BalanceConstants.cs`.
- **스킬트리 편집**: `Blueberry Defense > Skill Tree Editor` — 노드는 id/이름/**타입**/**등급(비용 자동표시)**/(해금·강화면)스킬/설명/선행. 백업본 `MainSkillTree_Backup.asset` 있음.
- **새 강화 노드 추가법**: 에디터에서 노드 만들고, 그 **id**를 `SkillEffects.Compute`의 case에 추가(효과는 id 기준 레지스트리 — 에셋 `effect` 필드는 신뢰 안 함).
- **새 맵/캐릭터**: `MapDefinition`/`CharacterDefinition` 만들어 각 Select UI 배열에 드래그.
- **치트**: `Window > Blueberry Defense > Cheat Window` — 정수 지급, 스킬트리 초기화, **승천 등급 설정·해금 최대로**.

---

## ⚠️ 미해결 이슈 / 주의
- **실플레이 확인 대기(연출 3종)** — *코드 동작은 플레이모드에서 검증됨(보석 도착 시 XP 적립·바 상승, 13마리 동시 처치 시 SMASH 1회). 남은 건 **감각 튜닝**:*
  - **보석** — 크기(30px)·속도·흩어짐이 적절한지. 물량 몰릴 때 화면이 보석으로 지저분하지 않은지. 도착 지점이 정말 "바의 찬 끝"으로 읽히는지.
  - **의성어** — 크기(BAM 1.1 / BOOM 1.35 / SMASH 1.7 월드스케일)가 과한지. 멀티킬 기준 3/6/10이 실제 플레이 빈도에 맞는지(너무 자주 뜨면 기준↑ 또는 `minInterval`↑).
  - ⚠️ **판 전환/게임오버 시 비행 중인 보석의 XP는 유실**됨(timeScale 0이면 멈춘 채 씬이 끝남). 실사용상 무해하다고 판단했으나 신경 쓰이면 도착 강제 처리 필요.
- **실플레이 재확인 대기(세션14 수정분)**:
  - **라이더 방향** — `m_FlipX=0`으로 정방향 맞는지(원본 그림 방향 추론이라 확실치 않음). 화질(Point/무압축)도 선명해졌는지.
  - **UFO 수송선** — 우측 1/3 안 침범하고 좌측~중앙에 등장하는지. 1초 간격 3회 투하 + 투하당 리젠트 1마리 섞이는지. 총 물량(12~15/캐리어) 과한지. 호버 고도(28%) 자연스러운지.
  - **거대 회오리 + 미니 회오리** — 거대 회오리 보유 상태에서 미니 회오리(path0)도 함께 나오는지.
  - **보스 사망 UFO** — 보스 죽은 자리에서 나와 상승 퇴장하는지(플레이어 머리 위 확정 피해 사라졌는지).
  - **스킬트리 안개** — 인접 노드에 이름·효과·비용 표시, 2링크 이상은 완전 은폐되는지.
- **실플레이 확인 대기(세션13 잔여)**:
  - **벽 스테이지** — 9(방패만)/13(UFO만)이 실제로 벽처럼 느껴지는지, 물량(9=28·13=15UFO)·간격이 과하거나 부족한지.
  - **11+ 밸런싱** — 여전히 쉬운지/과한지. 수치는 `Balance Dashboard` 또는 StageTable에서 조정.
- **UFO 격추 난이도 잔여 튜닝**: 13스테이지(×5.6)면 UFO 45HP→약 252HP를 ~1.3초 하강 중 녹여야 격추 보상 성립. 대공 빌드가 실제로 가능한지 확인, 안 되면 HP↓ 또는 `carrierDescendSpeed`↓.
- **시작 캐릭터 풀 축소 필요**: 루프가 성립하려면 `Char_Strawberry.allowedPool`을 1~2개로 줄여야 함(지금은 넓어서 트리 해금 없이도 스킬이 나옴).
- **`ESC 요약`의 "전체 피해량"은 스킬트리 보너스까지 포함한 합계** — 힘 패시브 단독 기여분이 아님.
- **씬 직접 조작 OK**(사용자 승인): MCP로 씬 열기/수정/저장 자유.
- **에디터 인스펙터 예외(무해)**: 플레이 진입 시 `ObjectPreview.DrawPreview ... Image destroyed`.
- **노션 토큰 노출**(기존): 폐기·재발급 권장.
- (기존) **사운드 볼륨 이중곱** — 우선순위 낮음.

---

## 🎨 스킬 이펙트 픽셀 크기 컨벤션
스킬 이펙트 스프라이트는 배경과 픽셀 밀도가 안 맞아, **그림은 작게 그리고 엔진에서 1.5배로 표시**(플레이어 딸기 프리팹 `localScale=1.5`에 맞춤).
- 새 이펙트 프리팹은 기존값에 곱하지 말고 `localScale`을 **직접 (1.5,1.5,1.5)로** 세팅.
- PPU **32**, Main Camera Orthographic Size 5(화면 세로 = 10유닛).
- 공식: `원본 그림 px = (목표 월드 유닛 × 32) ÷ 1.5` (예: 화면 세로 꽉 = 64 × 213px)

---

## 🧭 방향 확정 (세션14 논의) — "시원하게 쓸어버리기 + 파워 스파이크"

> **핵심 진단**: 두 문제는 하나다 — **성장할 헤드룸이 없다.** 시작이 이미 세고, 만렙 이펙트가 이미 화면을 덮고, 그 사이 곡선이 완만해 중간이 뭉갠다.
>
> **킬존 진단(중요)**: 적이 화면 왼쪽 등장 지점에서 다 죽어 화면 나머지가 빈 땅이다. → **밀도를 올려도 소용없다**(사용자 확인). **맵 확대도 역효과**(경로가 길어지면 최대 사거리 구간만 길어짐). 해법은 **전투를 플레이어 쪽으로 당기는 것** = 중간 소환 + 대각선 비행. **맵 크기는 목표가 아니라 "소환 지점이 부족할 때" 따라오는 결과.**
>
> **확정된 작업 순서** (재작업 최소):
> 1. ~~대각선 비행~~ ✅ 세션14 완료(종이비행기만, UFO는 그대로). 화살 발사점 0.65 땜빵은 **아직 남아 있음** — 대공 진화를 "발사각/히트박스 확장"으로 재설계할 때 같이 정리할 것.
>    - *부수 효과*: 크기/범위 성장이 **"비행체를 더 일찍 격추"**라는 눈에 보이는 의미를 얻는다(아래 3번과 맞물림).
> 2. ~~경험치 시각화~~ ✅ 세션14 완료.
> 3. **레벨업 커브 재작성** — 코드 무변경, `Prog_*` 데이터만. 현재 성장의 **2/3이 안 보이는 스탯**(피해×1.2, 쿨×0.95). 회오리 크기·주기 / 오브 크기·관통 / 낙뢰 확률 / 화살 개수처럼 **보이는 스탯** 중심으로. ⚠️ 초반은 "약하게"가 아니라 **적 압박으로** 어렵게 하고, 레벨업 간격을 촘촘히(안 그러면 어려운 게 아니라 지루해짐).
> 4. ~~**진화 재설계**~~ ✅ 세션15 완료 — 아래 "세션15" 절 참고.
> 5. **중간 소환 메커닉** — 화면 안 지점에 **2~3초 예고 마커** 후 부대 등장(예고 없으면 대응 불가 = 짜증). 인프라 대부분 있음: [Enemy.cs:392 `SpawnDeathBurst`](Assets/Scripts/Enemy.cs#L392)가 임의 위치 부대 배치 + 레인 착지 역산을 이미 품고 있고, `PopIn`·`EmergeAsBurstCarrier`도 재사용 가능.
> 6. **맵 확대** — 5번이 자리가 부족해지면 그때. ⚠️ 진짜 비용은 아트: 카메라 size를 키우면 "스킬 이펙트 픽셀 크기 컨벤션"(아래) 공식이 전부 바뀌고 기존 이펙트가 다 작아진다.

## 📊 밸런스 (세션16)
> 플레이 피드백: "초반도 쉽고, 후반은 정말 너무 쉽다"
> → **초반은 적이 아니라 스킬 쪽에서, 후반은 적 체력에서** 난이도를 만든다(세션14에 정한 방향 그대로).

**후반 적 체력 ↑ (StageTable `enemyHpMultiplier`) — 1~9스테이지는 손 안 댐**

| 스테이지 | 1~9 | 10 | 11 | 12 | 13 | 14 | 15 |
|---|---|---|---|---|---|---|---|
| 이전 | (동일) | 3.2 | 4.3 | 5.3 | 5.6 | 7.3 | 8.6 |
| **지금** | **그대로** | **3.6** | **5.5** | **7.2** | **8** | **11** | **14.5** |

3스테이지마다 붙는 `ScalingTable.hpStepBonus`가 또 곱해져 **15스테이지 실효 ×24.9** (기본 블루베리 12 → **299**, 이전 178).

**스킬 피해 ↓ (`Prog_*` 데이터의 `baseDamage`만)**
- `baseDamage` 약 **−33%**. **레벨업 피해 스텝은 ×1.3 그대로**(한 번 ×1.4로 올려봤다가 되돌림).
- 결과: 곱셈 커브라 **전 구간 −30~33%** — 1레벨만이 아니라 만렙 피해도 같은 비율로 내려갔다.

| | 기본공격 | 회오리 | 오브 | 낙뢰 | 독수리 | 스나이핑 | 호밍 | 산탄 |
|---|---|---|---|---|---|---|---|---|
| Lv.1 | 9→**6** | 4→**2.8** | 5→**3.5** | 6→**4** | 6→**4** | 15→**10** | 4→**2.8** | 15→**10** |
| Lv.10 | 25.7→**17.1** | 6.8→**4.7** | 14.3→**10** | 17.1→**11.4** | 17.1→**11.4** | 42.8→**28.6** | 11.4→**8** | 42.8→**28.6** |

> ⚠️ **후반은 3중으로 어려워졌다** — 적 체력 ×1.5~1.7 · 스킬 피해 −33% · 만렙 10 상한. 곱하면 15스테이지 기준 **체감 난이도 약 2.5배**다. 셋 다 한 판에 들어갔으니, 후반이 벽이면 **적 체력(11~15) → 피해 −33% → 만렙 순으로 되돌릴 것.**

> ⚠️ **낙뢰만 `Prog_Lightning.baseDamage`(=0)가 아니라 `LightningStorm.BaseProcDamage`(코드 상수)가 실제 시작 피해**다 — 6→4로 여기서 내렸다.
> ⚠️ **개수형 시작값(발사 수·대상 수·타수)은 안 건드렸다** — 이미 세션14에 하향했고 더 내리면 스킬이 고장난 것처럼 보인다.
> ⚠️ **보스도 스테이지 체력 배율을 먹는다** — 15스테이지 보스 실효 HP 17,800 → **약 30,000**. 싸움이 늘어지면 `EnemyDef_Boss.maxHealth`(1200)를 내려 상쇄할 것(스펀지는 재미가 아님).
> ⚠️ 만렙 10 신설도 후반 파워를 깎았으니 **후반이 과하면 11~15 배율부터 되돌릴 것.**

## 다음 할 일
1. 🔴 **세션16 변경 실플레이 검증** — 위 "현재 상태"의 6항목(만렙·게이트·루트 잠금·아이템 유용성·후반 난이도).
2. **중간 소환 메커닉** (로드맵 #5) — 예고 마커 후 화면 안에 부대 등장. 진화가 끝났으니 다음 차례.
3. **밸런스 2차 조율** (📊 절) — 1차 상향이 과했는지/모자랐는지 플레이 후 판단.
4. **세션13 적/스테이지 실플레이 확인** — UFO 수송선 궤적·투하, 벽 스테이지 물량 감각.
5. **실플레이로 루프 검증** — 승천 1 클리어 → 승천 2 해금 → 못 깸 → 트리 해금 → 재도전.
6. **시작 캐릭터 풀 축소** (위 이슈).
7. 트리 UI 레이아웃 손보기(죽은 `BuildBar`·`OutgameLevelBar`는 세션13에 제거 완료).

### 진화 재설계가 남긴 청소거리
- **`OrbCanHitFlying` 스킬트리 노드** — 히트박스 판정 도입 후 UFO에만 영향. 다른 효과로 용도 변경 필요(예: 오브 공전 반경 확대).
- **화살 발사점 `FlyingArrowSpawnRaise = 0.65` 땜빵** — 대공 진화를 "발사각/히트박스 확장"으로 재설계할 때 정리.
- **`EvolutionTierTextTableSO`(에디터 텍스트 override)** — 여전히 (path, tier) 좌표계라 새 루트/티어와 어긋난다. 지금은 설명 폴백이 코드라 문제없지만, 에디터에서 텍스트를 덮어쓸 거면 루트 좌표계로 바꿔야 함.
- **보스 박치기 피해** — 아직 70. `HeadbuttDamageScale`(0.35) 하나로 전 적을 곱하고 있어 보스만 따로 스케일이 필요.
- **적 풀링 없음** — `EnemySpawner`가 생 `Instantiate`. 후반 물량 늘리면 성능 이슈.

## 📌 다음 세션 예정: 승천별 스테이지 수 확장
> **목표**: 총 스테이지 수(=보스가 나오는 최종 스테이지)가 승천 레벨마다 늘어난다. **승천1=20, 승천2=25, 승천3=30**(+5/레벨). 스테이지당 물량이 아니라 **판 길이(스테이지 개수) 자체**가 늘어나는 것. (현재는 15 고정.)
>
> **손댈 접점**:
> 1. `AscensionTier`에 `finalStage`(또는 stageCount) 필드 추가 — 승천1=20/2=25/3=30. ([AscensionTable.cs](Assets/Scripts/AscensionTable.cs))
> 2. `GameManager.FinalStage` **const(=15) 제거** → `AscensionTable.Get(RunConfig.AscensionLevel).finalStage` 런타임 참조. GameClear 판정 2곳([GameManager.cs:73](Assets/Scripts/GameManager.cs#L73)·L86)이 이걸 봄.
> 3. **보스 스테이지 이동**: `EnemySpawner`의 `isBossStage = currentStage == map.bossStage` — 보스가 승천 최종 스테이지에 나오도록 `bossStage`(현재 `MapDefinition`=15 고정)를 승천 최종값으로. RunBootstrap이 판 시작 시 덮거나 EnemySpawner가 직접 승천값 참조.
> 4. **StageTable 데이터 확장**: 현재 15개 엔트리, `GetStage`가 16+를 마지막(15=보스 config)으로 클램프 → **스테이지 16~최종−1의 실제 데이터 필요.** 선택: (a) StageTable에 엔트리 추가(수작업), (b) 15 초과는 non-boss 마지막(14) 기반으로 `ScalingTable` 스텝을 계속 곱해 절차 생성. **클램프가 보스(15) config를 물면 안 됨.**
> 5. HUD "Stage X/N"의 N도 승천 최종값 참조.
> 6. 확장된 판에 벽 스테이지(방패/UFO 등) 재배치 고려.

## 보류 중인 결정 / 백로그
- UI 시스템: uGUI + TextMeshPro 확정 (한글 폰트 = `Galmuri11 SDF`)
- 배포(itch.io 등)는 아직 진행 안 함
- 아웃게임 컬렉션(캐릭터/스킬 해금) = 스킬트리 이후 Phase
- 딸기 전용 초상화 도트 미완(`Player.png` 임시) — 그리면 `Char_Strawberry.portrait`만 교체
