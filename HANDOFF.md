# HANDOFF.md — 세션 인계 문서

<!--
📋 유지 가이드 — 이 문서는 "다음 세션이 지금 뭘 해야 하는가"만 담는다. 길어지지 않게 유지할 것.
- 담을 것: 현재 빌드 상태, 미해결 이슈, 다음 할 일. 그것뿐.
- 지울 것: 완료·검증된 피쳐/버그/이슈는 즉시 삭제 (git 히스토리에 이미 남음).
- 세션 로그를 누적하지 말 것. 매 세션 새로 쓰는 게 아니라, 끝난 항목은 지우고 새 항목만 갱신.
- 게임 스펙(스킬 수치·시스템 규칙 등)은 여기 말고 GDD.md에. 여기엔 "무엇을 확인/수정해야 하는지"만.
- 목표: 항상 한 화면 안에 읽힐 것.
-->

## 🧭 North Star
> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 액티브 4 + 패시브 4를 진화 트리로 강화해 빌드를 만드는 벙커 디펜스. 상세 스펙은 `GDD.md` §4 참고.

---

## 현재 상태 (2026-07-14)

**빌드**: 정상 동작 확인됨(유저 확인). 컴파일 에러 없음. **이번 세션 변경은 실제 플레이 검증이 안 됐음** — 이 프로젝트 컨벤션상 동적 결과(파티클·애니·밸런스 체감)는 스크린샷으로 검증하지 않으므로, 아래 항목은 다음 세션에 실제 플레이로 확인 필요.
> ※ 직전 세션의 StageTable 빌드버그(전 스테이지 동일 로드)는 빌드 정상 동작으로 **해소된 것으로 간주**.

### 이번 세션 패스 요약 (2026-07-14, 검증 대기)
- **🛡️ 방패 블루베리 신규 적**: 관통 투사체·오브를 통과 못 시키고 소멸시키는 벽 몹. `Enemy.blocksProjectiles` 플래그 → `Projectile`(관통 잔량 무시 소멸)·`Orb`(마지막 1틱 후 소멸)에서 처리. 회오리/낙뢰/독수리는 영향 없음. 프리팹 `Enemy_ShieldBlueberry`(HP80·이속1.5·xp8, 5프레임 8fps 애니, FlipX=0으로 방패가 앞쪽), **9스테이지부터 등장**(shieldChance 9~10=0.10 / 11~14=0.12 / 15~20=0.15). 치트창 "방패 블루베리 스폰 (테스트)" 버튼 추가.
- **기본공격 후반 데미지 억제** (곱연산 폭주 진단 후):
  - 치명타 확률 **상한 70% 캡** 신설(`PlayerSkills.MaxCritChance`) — 100% 상시 크리 불가
  - 기본공격 전용 크리확률 진화(path1 T1) **+0.30→+0.15**
  - 힘 path1 치명타 배율 성장 **+0.3/0.7/1.0 → +0.15/0.35/0.5** (최대 크리배율 5.0→4.0)
  - 힘 path0(공통)·path2(기본공격 전용) 피해배율을 **곱→덧셈 통합**(`ComputeBaseDamage`) — 힘 트리 3중딥 곱연산 제거
- **암살 진화 path1(치명타 처치 경험치) 절반 너프**: 1.5/3/4배 → **1.25/2/2.5배**
- **진화 UI Juicy**: 패널 열릴 때 9개 노드 스태거 pop-in + 진화 가능 노드 펄스 강조 + 스킬 아이콘 punch (기존 잠금/interactable 로직은 불변, DOTween `SetUpdate(true)`로 모달 일시정지 중에도 재생)

---

## ⚠️ 미해결 이슈

### 1. 플레이 검증 필요 항목 (이번 세션, 코드만 확인)
- **방패 블루베리**: 9스테이지+ 실제 등장 빈도(스폰 분기상 elite 뒤라 희석됨 — 체감 후 shieldChance/분기순서 조정), 애니 속도·방향, 충돌박스(현재 0.75, 블루베리값 유지) 적정성, 관통·오브 차단 실제 동작
- **기본공격 세기**: 크리 70%캡 + 힘 곱→덧셈 통합 후 후반 데미지 체감. 여전히 세면 힘 path2 수치나 기본공격 진화 path0 T2(×2)까지 손볼지 판단
- 암살 경험치 너프 체감, 진화 UI juice 연출 실제 느낌
- (이전 패스 미검증분) 패시브 5종 3path 진화 훅(건강 반격·리프레쉬×낙뢰 쿨감 등), UFO 밸런스, 체인 라이트닝 연결선, 오브 설치기 위치/쿨타임

---

### 2. 사운드 시스템 (나중으로 미룸)
볼륨 소스가 여러 군데(개별 프리팹 `m_Volume`, `fireSfxVolume`/`castSfxVolume`, `SfxPlayer.MasterVolume`)로 분산돼 이중 곱해지며 예측 불가한 상태 — 볼륨 급증/씹힘 증상이 남아있음. 재설계 권장하나 **우선순위 낮음, 한참 뒤로 미룸.** 손댈 때 통합 대상: `SfxPlayer.cs`, `AudioThrottle.cs`, `ObjectPool.cs`, 각 Vefects VFX 프리팹 AudioSource.

---

## ★ 다음 세션 체크리스트
- [ ] 위 플레이 검증 항목(특히 방패 블루베리 등장 빈도 + 기본공격 후반 세기) 실제 플레이로 확인 → 발견된 밸런스/버그 조정
  - (검증 툴: `Window > Blueberry Defense > Cheat Window` — Play 모드에서 레벨업/획득/진화/스테이지 스킵/방패 스폰)

---

## 🎨 스킬 이펙트 픽셀 크기 컨벤션
스킬 이펙트 스프라이트는 배경과 픽셀 밀도가 안 맞아, **그림은 작게 그리고 엔진에서 1.5배로 표시**한다(플레이어 딸기 프리팹 `localScale=1.5`에 맞춤).
- 새 이펙트 프리팹은 기존값에 곱하지 말고 `localScale`을 **직접 (1.5,1.5,1.5)로** 세팅.
- PPU **32**, Main Camera Orthographic Size 5(화면 세로 = 10유닛).
- 공식: `원본 그림 px = (목표 월드 유닛 × 32) ÷ 1.5`
  - 예) 화면 세로를 꽉 채우는 세로형 이펙트 = 64 × 213px.
- 카메라 Size나 플레이어 localScale이 바뀌면 이 배율을 전부 다시 계산할 것.
- 1.5 적용된 프리팹: `Whirlwind_Skill`, `Orb_Skill`, `BigOrb_Skill`, `Whirlwind_BigTornado`, `OrbAltar_Skill`, `SmallOrb_Skill`, `Projectile_BasicAttack`, `EagleDrop_Visual`. (`HitParticle`는 소품이라 예외로 0.7)

---

## 보류 중인 결정 / 백로그
- UI 시스템: uGUI + TextMeshPro 확정
- 배포(itch.io 등 실제 공개)는 아직 진행 안 함
- 진화 시스템 도입 후 신규 수치(오버힐 캡, 미니 회오리 데미지 분배 등)는 감으로 잡은 값 — 플레이테스트로 조정 필요
