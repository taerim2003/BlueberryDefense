# HANDOFF.md — 세션 인계 문서

<!--
📋 유지 가이드 — "다음 세션이 지금 뭘 해야 하는가"만 담는다.
- 담을 것: 현재 상태 · 미해결 이슈 · 아직 안 정해진 것. 그것뿐.
- 🔴 **"함정"·"이래서 이랬다"는 적지 말 것.** 이미 고친 것의 원인 설명은 **다음 세션이 알 필요가 없다** —
  git 커밋 메시지와 코드 주석에 남는다. 다시 밟을 함정이면 그건 규칙이므로 **CLAUDE.md에** 적는다.
- 지울 것: 완료·검증된 것은 즉시 삭제(git에 남는다). 세션 로그를 누적하지 말 것.
- 🔴 **축적형 문서가 아니다.** 매 세션 "추가 + 삭제" 둘 다 한다. 1022줄까지 불어난 적 있음(2026-08-26에 잘라냄).
- 사본 금지: 할 일·일정은 **칸반이 원본**, UI 규격은 **CLAUDE.md §5-1**, 씬 배선은 **SCENE_MAP.md**, 구조는 **ARCHITECTURE.md**.
  여기엔 포인터만 남긴다.
- 목표: 한 화면에 읽힐 것. **200줄을 넘기면 자른다.**
- ⚠️ 여기 적힌 "~해야 함"·예시·해법은 **낡는다.** 사용자에게 말하기 전에 코드로 1건 확인할 것.
-->

## 🧭 North Star
> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 액티브 4 + 패시브 4를 진화로 강화해 빌드를 만드는 벙커 디펜스. 스펙은 `GDD.md`.

**세션 시작**: `brief` 스킬이 절차다. **세션 종료**: `/wrap`. `GDD.md`는 grep으로 필요한 절만.

🔧 **에디터 조작은 지금 쓰는 서드파티 Unity-MCP로 끝까지 간다 — 결정됐다(2026-09-02). 다시 검토하지 말 것.**
Unity CLI(1st-party)를 검토했고 **다음 프로토타입의 세션 0에서** 채택하기로 했다. 진행 중인 프로토타입에서
갈아타면 이득은 나중에 오고 비용만 지금 낸다. 근거·함정은 `prototyping-kit/templates/CLAUDE.md` §6.

### 📋 일정·할 일의 원본 = 노션 (여기에 사본을 만들지 말 것)
- **칸반 "POC 할일"** — DB `3b06394c-d983-803d-af4a-c26f924121b7`. 조회 절차·함정은 **`kanban` 스킬**.
  사용자가 세션과 무관하게 카드를 올린다 → **매번 조회해서 답할 것.** 어긋나면 칸반이 이긴다.
- **캘린더 "블루베리 디펜스 일정"** — `3a16394c-d983-80ac-98e8-caf8a4804dc0`. 날짜를 인용하기 전에 조회.
- 못 미루는 고정점 셋: **9/12 빌드 리뷰 제출**(리뷰 7영업일) · **9/25 출시**(3,000원) · 8/29~31 사용자 부재(여행 + 외부 테스트).
- 🧊 **콘텐츠 동결.** 폴리싱이 기본값이고, 콘텐츠 추가는 **칸반 카드로 올라온 것만**. 먼저 제안하지 말 것.

---

## 현재 상태 (2026-09-18, 세션 69)

**세션 69 — 버그 수정 · 레벨업 카드 전수 정리 · 폭발/영역 범위.** 상세는 칸반 「9/17~18 버그 수정」(완료) 카드. 다음 세션이 알아야 할 것만:
- 🔴 **진화 스킬의 레벨업은 이제 `Evo_*.levels`가 정한다(15개 채움).** 비어 있는 나머지는 여전히 `Prog_*` 커브를 탄다. 새로 채울 땐 "효과 없는 카드"를 다시 셀 것(검사 스크립트는 저장소에 없다 — `script-execute`로 11스킬 × 5상태 × Lv2~10을 `StepFor`·`ApplyStep`·`DescribeUpgradeEffect` 리플렉션으로 돌렸다). 원리: 스텝을 적용한 복제 스킬의 소비처 값(개수·반경·틱 등)이 안 바뀌면 효과 없음.
- 🔴 **레벨업 "크기"(Scale)가 폭발·영역 반경에 곱해진다** — 호밍 폭발 · 폭탄 독수리 · 독수리의 비 · 포도 중독사 폭발 · 피뢰침. 카드 문구는 스킬별 `step.Scale.*`(`PlayerSkills.DescribeStep`).
- **개수에 배수가 붙는 진화(추적 오브 ×1.5 · 호밍 R1 ×2/×5 · 관통 산탄 ×2/×3)는 카드가 실제 증가량을 적는다** — 발사 코드와 카드가 같은 함수(`HomingOrbCount`·`HomingMissileCount`·`ShotgunPelletCount`)를 쓴다.
- 확인 대기 88건은 9/18 태리미가 전부 확인했다(절에서 삭제). 피뢰침 범위 실게임 확인만 칸반 「실제 exe QA」에 남았다.
- 봇 플레이테스트 세션이 같은 시각에 돌았다 — 봇 인프라(`Assets/Scripts/Bot/`, `balance`·`balance-loop` 스킬)는 그 세션 작업.

**세션 67 — 빌드 QA: 세이브 초기화 · 포도 해금.** 다음 세션이 알아야 할 것만:
- 🔴 **세이브는 이제 `SaveStore` 파일(`LocalLow/taerimgames/BlueberryDefense/save.json`, 에디터는 `save_editor.json`)이다**(커밋 2408d92). 갈림길(음량·언어 동기화 · 에디터/빌드 파일 분리 · 레지스트리 이관)은 9/18 태리미 확인 완료.
- 🔴 **Steam 빌드 리뷰가 Steam Cloud로 반려됐다**(BuildID 25232625). 코드는 끝났고 **Steamworks Auto-Cloud 설정 · 재빌드 · 업로드 · 재제출은 태리미 몫** — 절차는 칸반 「Steam 빌드 리뷰 반려 — Steam Cloud 동기화」.
- **9/17 22:19 빌드는 세이브 초기화가 안 된다**(리셋 코드 수정 전에 뽑힘) → **재빌드 후 확인**이 칸반 「실제 exe QA」에 올라가 있다. 초기화하면 타이틀 씬을 다시 불러온다.
- **포도 해금 조건은 정상**(누적 정수 2000 + `Map_Wide20` 승천 1 이상 클리어 — 에디터에서 6조합 실측). 빌드에서 실제로 깨서 풀리는지만 남았다.

**세션 66 (9/15) — 스킬트리 구조·가격 재구성 · 밸런스 수치 · 호밍 직진.** 다음 세션이 알아야 할 것만:
- 🔴 **스킬트리는 이제 "선행이 아니라 가격으로" 순서를 유도한다.** `tier` = 대역(1~6) = 일반 노드 이름 숫자(I~VI).
  비용은 대역마다 ×4(`SkillTreeData.TierCost`), 해금·기타 해금은 같은 대역의 60%(`CostOf`), 부유 노드가 벽을 넘는 수단.
  배치 원칙(태리미): 일반 노드는 종류를 섞어 흩뿌림 · 한 노드에 자식 2~3개 · 선 길이 1로 촘촘히 · 금별은 은별 뒤(거리는 가격으로). 현황은 칸반 「스킬트리 재설계」.
- **설계 문서 `SKILLTREE_DESIGN.md`는 태리미 요청으로 삭제**했다. 강화·기타 노드 id 계약은 `Meta.cs`의 `SkillEffects`.
- **세이브(에디터 PlayerPrefs)는 9/14에 한 번 전부 초기화**했고 그 뒤 태리미가 테스트로 다시 쌓았다. 초기화 전 백업은 이번 세션 스크래치패드에만 있었다(보존 안 됨).
- **밸런스**: 산탄 알 6 · 독수리 투하 5회×3 · 오브 피해 10 · 화살 1타 8·관통 1·쿨 3초(`BalanceConstants`·`Prog_*`).
- **호밍 미사일은 어떤 경우에도 멈추지 않는다**(대상 없으면 발사·진행 방향으로 직진) — `HomingMissile.Update`.
- 스킬트리 노드 아이콘은 **84개 전부** 있다(비행 `Icon_Fly` · 보스 사냥 `Icon_Boss` 추가). 새 그림은 `SkillIconLibraryBake` 표 → 메뉴 「스킬 아이콘 라이브러리 굽기」.

**세션 65**: 노드 칸 아이콘 + 우측 하단 단계 숫자/은별·금별(`SkillTreeUI.IconOf`·`StageOf`·`UpgradeRankOf` — 숫자는 이름 끝 로마자, 별은 선행 경로에 같은 스킬 강화가 있으면 금별).

**세션 64**: 진화 아이콘 일괄 배선은 `b4f7b6f`로 커밋됨(2차 빈 칸은 호밍 R1 하나). 밸런스 복구(피해·정수·경험 레벨당 수치)는 세션 65 커밋에 같이 들어갔다 — 레벨당 피해 240% 등(9/18 태리미 확인 완료, 원문은 git 이력의 HANDOFF).

**세션 63 — 2차 진화 아이콘·피뢰침 이펙트 · 스킬트리 86노드 · UI 폴리싱 3건.** 다음 세션이 알아야 할 것만:
- **액티브 2차 전용 아이콘 배열이 생겼다** — `LevelUpUI.activeEvo2Icons`(빈 칸이면 1차 그림으로 떨어진다). 21칸 배선(9/14 추가 반영 — 포도 1차·2차 포함). 빈 칸은 호밍 R1 2차 「초강력 슈퍼 로켓」 하나.
  새 그림은 `Icon_{스킬}R{루트}_2.png`로 두고 메뉴 「진화 아이콘 배선」을 돌리면 된다. 폴더째 올라온 그림은
  `EvolutionIconWiring`의 `ActiveOverrides`·`ActiveStage2Overrides` 표에 경로를 적는다. 남은 칸은 칸반 「아트 마감」 카드(빗금 안 친 줄).
- **피뢰침 1차 = 테슬라 코일(`Effect_LightningRod`), 2차 = 제우스상(`Effect_ZeusStatue`)**. 제우스상은 첫 타격이 0.5초 늦다.
- **스킬트리 86노드**(원본 `MainSkillTree.asset`). 🔴 **진화 해금 노드가 생겨서 이제 진화가 트리에 잠긴다.**
  만렙을 줄인 노드의 옛 세이브 초과 레벨은 `SkillTreeSave.EffectiveLevel`이 만렙으로 읽고 정수를 돌려준다.
- **오브 둔화는 「끈적한 오브」 노드를 사야 걸린다**(20% 감속). 기본 오브엔 없다.
- **키보드로 모달 칸을 옮기면 마우스 호버 연출이 꺼진다**(`JuicyButton.CancelHover`). 체력 위험 점멸은 가장자리 108px 띠·알파 0.22로 약해졌다.
- 갈림길 7건은 9/18 태리미 확인 완료.

<details><summary>이전 세션 상태 (세션 61)</summary>

**세션 61 — 스킬 UI 문구 반영 + 진화 구조 개편.** 노션 「UI 문구 편집」 09/08 판을 게임에 넣고,
거기 딸린 구조 변경까지 끝냈다. 상세는 칸반 「스킬 UI 문구 반영 + 액티브 2차 진화 · 패시브 2차 폐지」 카드에 있다.
다음 세션이 알아야 할 것만:
- 🔴 **진화 문구 키 좌표가 바뀌었다.** `evo.*.desc.{스킬}.{루트0·1}.{차수1·2}` — 화면에 보이는 그대로다.
  **효과 쪽 `PathTier`(1차=2, 2차=3)는 그대로**라 두 좌표계가 공존한다. 헷갈리면 그 자리 주석을 볼 것.
  `evo.*.title.*`는 폐지됐다 — 제목은 `evo.name` 하나가 소유한다.
- 🔴 **패시브는 2차 진화가 없다.** `EvolutionRoutes.MaxPassiveStage = 1`. 진화 창·도감에서 2차 칸과 화살표가 안 뜬다.
- 🔴 **액티브 2차에 「열쇠」가 붙었다** — 지정된 진화체를 먼저 만들어야 2차가 열린다(`EvolutionRoutes.Stage2Prereq`).
  **9/18 규칙 재정렬(사용자 결정)**: 열쇠는 반드시 **그 루트의 1차 조건 스킬의 진화체**이고 루트만 고른다 · 1차 조건이 캐릭터 전용이면 열쇠도 전용 · **열쇠끼리 순환 금지** · 22칸 열쇠는 서로 안 겹친다.
  독수리 R0 2차는 **의도적으로 파인애플 전용**(← 메카 버스터). 오브 R1 조건은 노션대로 **독성 포도알**(포도 전용 루트).
  세션61의 "`KeyAudit` 슬롯 초과 0"은 틀렸었다(호밍 R0이 액티브 5개 요구). 표를 고치면 **실제 `EvolutionRoutes.cs`를 컴파일해 부르는 감사**를 다시 돌릴 것 — 규칙 위반·도달 불가·슬롯·순환·겹침을 세고, 옛 표(HEAD)로 대조군을 같이 돌린다.
- 🔴 **되감기 R1 2차만 진화 쿨감의 예외다**(사용자 승인). GCD 0 + 전 스킬 쿨 ×0.7.
- **산탄 버프의 노란 테는 사라졌다** — HUD 버프 줄 아이콘 + 남은시간이 전부다.
- ⚠️ **밸런스 확인이 필요한 둘**: 건강 R0은 복리라 만렙 최대체력 **약 ×20**, 방어 R1 반사는 **화면 전원에게 만렙 40배**.
  손잡이는 `HealthSturdyPerLevel`·`DefenseThornsPerLevel`.
</details>

- **포도(`Char_Slot3`)가 들어왔다** — 스킬 `GrapeToss`(독성 포도알) + 진화 2루트, 아트·아이콘·문구까지.
  수치와 남은 일은 **칸반 「3번째 캐릭터 포도 기획안」 카드 본문**이 원본이다(여기 옮겨 적지 않는다).
- **UI 문구를 노션에서 고친다** — 「UI 문구 편집」 페이지 6장(299행). 편집은 노션,
  **되읽기(반영) 도구는 `.claude/scripts/notion-loc-apply.js`** (2026-09-03).
  절차: 각 시트를 `API-retrieve-page-markdown`으로 읽어 파일로 저장 → `node .claude/scripts/notion-loc-apply.js <파일>`
  (미적용 미리보기) → 확인 후 `--apply` → Unity `Window > Blueberry Defense > 번역 - 모든 TSV를 표에 적재` → `Loc.T`로 몇 건 조회.
  없는 키는 절대 만들지 않고 매칭 실패는 목록으로 보고한다. 시트 6장 id는 부모 페이지 `3cf6394c-d983-81a5-91e8-d0d8be896cd9`에 있다.
  (칸반 「UI 문구 노션 왕복 — 되읽기(반영) 도구」 카드에 복원 규칙까지 적어 뒀다).
- **아이콘 사본이 두 계통이다.** `Resources/SkillIconLibrary.asset`은 컬렉션 화면용이고,
  **전투 HUD·레벨업·진화창은 씬에 배선된 `Sprite[] activeIcons`를 쓴다**(`HUDController`·`LevelUpUI`·`EvolutionTreeUI` 셋).
  새 스킬 아이콘은 **양쪽 다** 넣어야 화면에 뜬다 — 굽기만 하면 값은 맞는데 화면은 빈다.

- **갈림길 창은 이제 `Canvas/TreasureChoicePanel`**(Battle 씬)이다. 예전처럼 `LevelUpPanel`을 돌려쓰지 않는다.
- **진화 노드는 `EvolutionPanel/Window/Node_R{루트}T{티어}` 4칸 + `Arrow_R{루트}` 2개**가 전부다(3×3 잔재는 지웠다).
- 🔴 **패널을 씬에서 다시 만들면 인스펙터 참조가 통째로 끊긴다.** 특히 노드의 `Button` 컴포넌트까지 사라지면
  `EvolutionTreeUI.Awake`가 경고를 찍고 그 칸을 건너뛴다 — **콘솔 경고부터 볼 것.**
- **씬에 쓴 문구를 코드가 덮어쓴다.** 문구를 바꿨으면 씬이 아니라 `Assets/Localization/*_ko.tsv`를 고치고
  `Window > Blueberry Defense > 번역 - 모든 TSV를 표에 적재`를 돌린다.

---

## ❓ 확인 대기 (Claude가 물어야 했지만 안 묻고 진행한 것)

무인 실행 중에는 질문으로 턴을 끊지 않는다(CLAUDE.md §1 최상단). 갈림길을 만나면 한쪽을 골라 끝까지 만들고
여기에 적어 둔다. 태리미가 읽고 뒤집으면 그 줄을 지운다.

**형식:** `- [세션N] 갈린 것 → 고른 쪽(이유) / 반대쪽이면 달라지는 것 / 되돌릴 곳: 파일:라인`

**안 하고 남긴 것** (되돌릴 수 없거나 귀·눈으로 골라야 하는 것):

- 액션아이템 24 **보물 획득 사운드 교체** — 현재 `coins_gather_medium.wav`(동전 소리), 요청은 "밝고 기대감 있는 팡파레". 후보를 들어 봐야 골라진다. 자리: `Assets/Resources/SfxLibrary.asset`의 `treasureOpen`
- 액션아이템 23 **오브 이미지 해상도 통일** — "통일"의 기준이 불명(`Effect_BigOrb` 64×64 / `Effect_SmallOrb` 32×32 / 타 이펙트 48×48)
- **에디터 세이브 유실분**(도감·맵 클리어) — 태리미가 "그대로 둔다"로 결정(2026-09-03)
- **스테이지 난이도 재조정** — 9/2 산탄 개편의 영향을 **재기만 했고 바꾸지 않았다**. 미진화 산탄 빌드는 전 스킬 평균 피해 배율이 1.357배 → 1.0배로 떨어져 **후반일수록 손해가 커진다**(빌드 DPS 6 이상이면 산탄 자체 피해 2.14 DPS로 상쇄가 안 된다). 반대로 집중 산탄 루트는 지속 7초/쿨 14초 = 1.5배로 **예전보다 세졌다**. 어느 쪽으로 맞출지는 플레이테스트가 필요해 손대지 않았다. 손잡이: `Assets/Data/Skills/Prog_Shotgun.asset` · `StageTable.extendedHpGrowth`

---

## ⚠️ 미해결 이슈 (아직 안 끝난 것만)

- 🔴 **스킬트리 재설계 — 세션66에 구조(가지 많은 트리)·가격(대역 곱셈)을 재구성했고 *플레이로 손맛 확인이 남았다*.** 노드 조합은 탐색 알고리즘 결과이고
  태리미가 자리를 하나씩 옮기는 중이다. **강화·기타 노드의 id 계약은 `Meta.cs`의 `SkillEffects`**(`case "id"`)가 원본이다(설계 문서는 9/14 삭제).
  ⚠️ **강화·기타 노드는 id가 한 글자만 달라도 조용히 무효**다(정수만 나가고 효과가 없다).
  ⚠️ **포도 투척은 해금 노드도 강화 노드도 목록에 없다** — 유일하게 트리에 안 걸린 스킬이다.
  ⚠️ 구 노드 `orb_BasicFly`·`Shotgun_CloseBonus`는 **2026-09-14 트리 에셋에서 뺐다**(태리미 요청 — 선행은 `New_Reroll`·`exp_2`로 이음). `Meta.cs`의 두 `case`와 번역 표 `tree.*` 4줄은 남아 있다(이제 아무 노드도 안 부른다).

- ⚠️ **8/24 플레이스루 밸런스 수정 2건이 죽은 층에 들어가 있었다**(2026-09-03 발견). `SkillProgression.DefaultBaseDamage`의
  독수리 11→13 · 회오리 7→6 은 **`Prog_*` 에셋에 가려 게임이 한 번도 안 봤다**(`p != null ? p.baseDamage : Default...`).
  같은 의도를 에셋에 옮겨 적용했다(독수리 4→6 · 회오리 3→2). 코드 표에는 가려져 있다는 경고 주석을 달았다.
  → **훑었다(2026-09-03): 이 2건이 전부다.** 코드 기본값 표 11종 × 쿨/피해를 에셋과 전수 대조했고,
  변경 흔적이 있는 주석은 이 둘뿐이었다. `BalanceConstants` 31개도 참조 여부를 전수 확인해 30개가 읽힌다.
  (검사 스크립트는 스크래치패드의 `balance_shadow.js` · `const_usage.js` — 층이 늘면 다시 돌릴 것)
- ⚠️ **밸런스 재점검이 필요하다 — 산탄이 크게 바뀌었다**(2026-09-02). 8/27 QA 결정 5건이 전부 반영됐고
  그중 셋이 산탄이다: 미진화 산탄에서 **버프가 빠져 순수 공격기**가 됐고(버프는 집중 산탄 루트에서만),
  관통 산탄은 **3초 연사(전탄발사)**로 바뀌었으며 **관통이 무한**이다. 스테이지 난이도는 아직 안 맞춰 봤다.
- ⚠️ **버려진 루트를 걷어내며 기능 넷이 사라졌다**(2026-09-02, 세션56). 넷 다 **원래 도달 불가능**이라
  게임 동작은 그대로지만, 있는 줄 알았다면 지금은 없다 — 되살릴 게 있으면 살아있는 루트로 옮겨 붙여야 한다:
  건강 패시브의 **체력 재생** · 기본 공격의 **비행 적 타격** · 오브의 **공중 추가 피해** · `Effect_RewindYellow`.
  (2루트 개편 때 버려진 path에만 걸려 있어서 `PathTier[버려진]`이 영원히 0이었다.)
  ⚠️ 세션60에 그 흔적이던 `BalanceConstants.FlyingArrowSpawnRaise`(참조 0)를 지웠다 — 되살리려면 값을 다시 정해야 한다.
- ⚠️ **다음 exe 빌드에서 확인해야 할 것 둘** — ① 전체화면 화질(에디터엔 전체화면 전환이 없어 검증 불가),
  ② 컬렉션 아이콘이 호버 때 움직이는지(코드에 위치를 움직일 장치가 없고 실측 변화량 0이라 재현 실패.
  8/27 빌드 이후 선택 테 구조가 바뀌면서 사라진 것으로 보인다).
- 🔴 **광활한 밭 배경 미제작** — 576×324(PPU 18)이 나와야 `cameraYLift`를 포함한 나머지가 따라온다.
  배경 없이 실행하면 가장자리가 빈다(버그 아님).
  ⚠️ 맵 3종이 서로 다른 단계에 있으니 **밸런스를 고치기 전에 어느 맵인지 확인**할 것 — 값은 각 `MapDefinition` 에셋이 정답이다.
- 🔴 **2차 진화 *이펙트*가 모자란다**(2026-09-08 실측 정정 — 예전의 "그림이 전무"는 틀렸다).
  **1차 아이콘은 다 있고, 2차 전용 아이콘은 13칸이 배선됐다(세션63).** 이펙트는 피뢰침·제우스상이 새로 들어갔고(세션63), 그 밖에 있는 건 `Effect_ArrowR2`·`Effect_Rewind_R2` 둘뿐이고
  나머지는 1차 이펙트를 그대로 쓴다(빙결 오브·파열·재앙의 눈 등). 9/5~9/8 구간에 2일치로 잡혀 있다.
  🔴 **세션61에서 액티브 2차 22칸 중 8칸만 구현했다** — 칸반 「09/05~09/08」의 *그려야 할 그림* 목록에 이름이 있는
  **14칸은 문구만 넣고 로직을 안 건드렸다**(사용자 지시: 새 그래픽이 필요 없는 것부터). 구현한 8칸 =
  블루베리 사냥꾼 · 독수리의 비 · 저 하늘의 별처럼 · 내 지휘를 따라! · 과충전 · 블루베리 절멸의 시간 · 재앙의 역병 · 메두사의 간식.
  ⚠️ 미룬 14칸 중 **하늘의 울음(취약) · 저글러(증량) · 로열 팔라딘의 망치(장시간 기절)** 셋은 **로직이 이미 문구대로 돈다** — 그림만 기다리면 된다.
- 🔴 **번역 — TSV는 다 고쳤고 *표 적재만 남았다*(세션62).** 심사 빌드를 위해 영어를 Day1에서 앞당겨
  `harvest_en.tsv` **123건을 ko 기준으로 다시 썼다**(세션61에 en이 옛 문구를 새 좌표에 재배치해
  진화 이름·설명이 폐지된 루트를 말하고 있었다). ko 하드코딩 4자리도 키로 뺐다(`ui.result.gameOver/gameClear`·`ui.hud.*`).
  ✅ **표 적재까지 끝났고 en·ko 캡처 검증도 했다**(설정·일시정지·전투 HUD). 영어 길이 문제는 아래 오토사이즈로 해결됐다 —
  레벨업 3택 제목은 가장 긴 `Hour of Blueberry Extinction`이 28pt에서 480px(칸 360)이지만 오토사이즈가 **21pt로 줄여** 담는다(min 17).
  ⚠️ **진화 노드 제목 칸(`Node_R*T*/Title`)만은 원래부터 오토사이즈+줄바꿈**이라 손대지 않았다 — 칸이 177×40으로 좁아 두 줄이 의도다.
  긴 영어 이름이 세 줄로 접히는지는 **진화 창을 실제로 띄워** 봐야 한다(아직 안 봤다).

- **UI 텍스트에 오토사이즈를 일괄로 걸었다(세션62, 134건).** 규칙: `fontSizeMax = 그때의 fontSize`(사용자 지정 — 지금 크기가 상한),
  `fontSizeMin = max × 0.6`(하한 12), 그리고 **`NoWrap`을 같이 건다.**
  🔴 **오토사이즈만 켜면 줄바꿈이 안 풀린다** — TMP는 "접어서 높이에 들어가면 됐다"고 판정하므로 `NoWrap`이 있어야 폭에 맞춰 줄어든다.
  이름에 **`Desc`·`Hint`·`Condition`·`Body`·`Detail`**이 들어가면 설명문으로 보고 줄바꿈을 남긴다 — **새 TMP를 만들 때 이 이름 규칙을 따를 것.**
  이미 오토사이즈가 켜져 있던 것은 건드리지 않았다. 대상: `Title.unity`·`Battle.unity` + `OptionsPanel`·`CollectionPanel`·`PausePanel`·`SkillNode` 프리팹.
  제외: `DamageNumber.prefab`(월드 텍스트라 크기가 요동친다).

- **인게임 모달을 키보드로 고를 수 있다(세션62).** 새 공용 클래스 **`UIFocusGroup`**(MonoBehaviour 아님 —
  각 UI가 필드로 들고 `Tick()`을 부른다)이 WASD·화살표로 포커스를 옮기고 Space·Enter로 확정한다.
  🔴 **방향은 축을 적어 두지 않고 rect 좌표로 판정한다** — 화면마다 배치가 달라서다(레벨업은 세로 3장 +
  **오른쪽**에 리롤, 진화 창은 **행이 루트·열이 티어**). 배치를 옮겨도 저절로 맞는다.
  포커스 테는 맵 선택 카드와 **같은 방식**(판 그림 + `UI/SelectOutline`)을 **런타임에 만들어** 붙이므로 씬 배선이 없다 —
  머티리얼만 `UISkin.selectOutline`(`Assets/Resources/UISkin.asset`)에 한 번 꽂혀 있다. **비면 테가 통째로 안 나온다.**
  ⚠️ 모달은 겹치므로(갈림길 → 진화) **static 스택의 맨 위 그룹만** 입력을 먹는다. 새 모달을 만들면 열 때 `Open`,
  닫을 때 `Close`를 **짝으로** 부를 것 — `Close`를 빠뜨리면 그 그룹이 스택에 남아 다음 창이 입력을 못 받는다.
  ⚠️ 게임오버(`DamageMeterUI`)만 `ModalPause`를 안 쓴다(GameManager가 직접 timeScale을 0으로 만든다).
  검증한 화면: 일시정지 · 레벨업 3택+리롤 · 게임오버(각각 키 이동 실측 + 캡처). **진화 창과 갈림길 2택은 아직 눈으로 못 봤다.**

- **독성 포도알·호밍 미사일이 대상 없을 때 「전방에서 대기」한다(세션62).** 예전엔 **뒤로** 나갔다 —
  🔴 **이 프로젝트의 전방은 `Vector.left`(-x)** 인데(기본공격·산탄·충격파 전부 그렇다) 호밍만 `Vector2.right`에
  *"적 방향(오른쪽)"* 이라는 틀린 주석까지 달고 있었고, 포도알의 적 없을 때 폴백도 `Random.Range(2f, 7f)`로 +x였다. 둘 다 부호를 뒤집었다.
  이제 **적이 0마리면 포도알을 전방에 세워 두고 멈춘다**(`GrapeProjectile.waiting`).
  적이 나오면 `HoldGrapesUntilEnemy`(0.1초 폴링)가 평소 조준(`PickGrapeSpots`)으로 날린다.
  ⚠️ **적이 영영 안 나오면 대기도 끝나지 않는다**(씬 전환으로만 정리된다). 판이 도는 동안은 적이 반드시 나오므로 의도한 성질이다.
  🔴 **미사일은 2026-09-14부터 대기하지 않는다 — 어떤 경우에도 멈추지 않는다**(태리미 결정). 대상이 없으면 소환 방향(또는
  직전까지 향하던 방향)으로 직진하고 수명 4초가 다하면 사라진다. 직진 중 적이 나오면 다시 쫓는다. `HomingMissile.Update` 주석 참고.

- **우주 맵 유닛을 달 위로 내렸다 — `Map_Wide20.cameraYLift` 0 → 3.0**(세션62). 손잡이의 뜻은 "카메라와 배경을 같이 올려서
  **화면 안에서 레인을 내린다**"라 유닛의 월드 좌표는 그대로다. 근거: 배경 576×324@PPU18 = 18유닛이고 카메라 ortho도 9(=5×1.8)라
  화면과 1:1, 달 지평선이 아래에서 79px = 4.39유닛, 플레이어 발 y = −2.202 → lift 3.0이면 발이 표면에 **0.59유닛 묻힌다**.
  ⚠️ **배경 그림을 다시 그리면 지평선 높이가 바뀌므로 이 값을 다시 재야 한다.**
- ⚠️ **음원 파일은 git에 없다** — `Assets/Sound/`(209MB)가 `.gitignore`에 있고 **이건 결정된 사항이다. 다시 논의하지 말 것.**
  → 새로 클론하면 **맵·타이틀 BGM과 효과음 참조가 전부 빈다. 버그가 아니다.** 음원을 다시 받아 같은 경로에 놓아야 소리가 난다.
- ⚠️ **Battle 씬을 단독 Play하면 모달이 안 보인다**(레벨업·진화·게임오버 전부 alpha 0에서 얼어붙는다).
  `GameManager.Awake`의 `DOTween.defaultTimeScaleIndependent = true`가 DOTween 지연 초기화(`DOTweenSettings`)에 덮여
  런타임 값이 `False`가 된다. **타이틀부터 들어가면 정상이라 빌드엔 영향이 없다** — 씬 단독 테스트만 걸린다. 미수정.
- 🔴 **커서 크기가 실제로 커지는지 미확인** — `Title/Controllers`의 `CursorSetter.pixelScale`을 3→4로 올렸지만
  **화면에서 검증을 못 했다.** 확인된 것: 원본 PNG는 256×256인데 임포트가 **64×64로 줄여 들이고**(`maxTextureSize: 64`),
  코드가 그걸 정수배 확대한다. 그림은 캔버스의 64%×53%뿐이라 실측 x3=123×102, x4=164×136.
  **OS는 범인이 아니다** — `SM_CXCURSOR`가 32여도 그건 시스템 기본값일 뿐이고, `CreateIconIndirect`로 재 보니
  Windows는 256×256 커서를 그대로 보존했다.
  → **남은 질문은 하나: Unity가 `Cursor.SetCursor`에 넘기기 전에 줄이는가.**
  **재는 법**: 게임을 실행하고 마우스를 게임 화면 위에 둔 채 `GetCursorInfo` + `GetIconInfo`로 실제 커서 비트맵을 읽는다
  (`hotspot`이 44,48이면 우리 커서다). 감시 스크립트는 스크래치패드의 `cursorwatch.ps1`.
  Unity가 줄이는 것으로 판명되면 `pixelScale`은 소용없고 `CursorMode.ForceSoftware`로 바꿔야 한다 — **꺼내기 전에 문서를 열 것.**
  그림 둘레 투명 여백이 36%라, 여백을 잘라내면 같은 텍스처 예산으로 그림만 1.5배 키울 수 있다(태리미 아트 파일 수정이라 안 건드림).
- **노션 토큰** — 2026-08-27 전수 확인: **두 저장소(이 repo·키트) 작업트리와 git 이력 전체에 실제 토큰 값이 없다.**
  토큰은 `~/.claude.json`의 `mcpServers.notion.env.OPENAPI_MCP_HEADERS`(사용자 전역, repo 밖)에만 있고,
  키트의 `ONBOARDING.md:136`·`journal/2026-07-06.md:20`은 플레이스홀더(`<토큰>`)와 설명문이다.
  → **저장소를 통한 노출은 확인되지 않았다.** repo 밖 경로(대화·문서·캡처)로 샌 적이 있다면 그건 태리미만 안다 — 재발급 여부는 그 판단에 달렸다.

---

> 코드 구조·시스템 소유권·손잡이는 여기 적지 않는다 — **해당 코드의 주석**과 `ARCHITECTURE.md`에 있다.
