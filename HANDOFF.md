# HANDOFF.md — 세션 인계 문서

<!--
📋 유지 가이드 — 이 문서는 "다음 세션이 지금 뭘 해야 하는가"만 담는다.
- 담을 것: 현재 빌드 상태, 미해결 이슈, 다음 할 일. 그것뿐.
- 지울 것: 완료·검증된 피쳐/버그/이슈는 즉시 삭제 (git 히스토리에 이미 남음).
- 세션 로그를 누적하지 말 것. 매 세션 "추가 + 삭제" 둘 다 한다.
- 옮길 것: 구조적 함정 → ARCHITECTURE.md / 게임 스펙·설계 철학 → GDD.md / 씬 배선 → SCENE_MAP.md.
  여기엔 "무엇을 확인·수정해야 하는지"만 남긴다.
- 목표: 한 화면 안에 읽힐 것. (497줄까지 불어난 적 있음 — 그렇게 두지 말 것.)
- ⚠️ 이 문서는 **할 일의 목록이지 코드의 진실이 아니다.** 여기 적힌 "~해야 함"을 사용자에게
  말하기 전에 코드로 1건이라도 확인할 것. 이미 해결됐을 수 있다(실제로 여러 번 그랬다).
-->

## 🧭 North Star
> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 액티브 4 + 패시브 4를 진화로 강화해 빌드를 만드는 벙커 디펜스. 스펙은 `GDD.md`.

**세션 시작 시**: `brief` 스킬이 절차다 (칸반 → 이 문서 → **코드 대조** → 브리핑). `GDD.md`는 grep으로 필요한 절만.

### 📋 일정·할 일의 원본 = 노션 칸반보드 "POC 할일"
> https://app.notion.com/p/3b06394cd983803daf4ac26f924121b7 (DB id `3b06394c-d983-803d-af4a-c26f924121b7`)
> 상태: `시작 전` / `진행 중` / `완료`. **여기가 원본이고 아래 "다음 할 일"은 미러다.**
> - **일정은 이 보드로 확인한다.** 이 문서의 사본을 근거로 답하지 말고 매번 조회할 것.
> - **사용자가 세션과 무관하게 아무 때나 카드를 올린다.** 세션 시작에 반드시 다시 조회.
> - 어긋나면 **칸반이 이긴다.** 발견 즉시 이 문서를 맞출 것 — 미루지 말 것.
> - 보드는 **공동 관리**다. 끝낸 카드는 `완료`로, 착수한 카드는 `진행 중`으로 옮기고, 새 할 일은 카드로 만든다.
> - 전체 규칙·조회 절차(2단계 고정)·MCP 함정은 **`kanban` 스킬**에 있다 — `/kanban`으로 열 것.

### 📅 일정 (노션 원본)
> ⚠️ **원본은 노션 캘린더 DB "블루베리 디펜스 일정"**(`3a16394c-d983-80ac-98e8-caf8a4804dc0`). 아래는 미러다 — 날짜를 인용하기 전에 조회할 것.
> (2026-08-06에 이 표의 폴리싱 마감이 **8/7로 잘못 적혀 있던 것**을 캘린더 기준 8/9로 고쳤다.)

| 마일스톤 | 날짜 | 내용 |
|---|---|---|
| ~~프로토타입~~ | ~~7/6~7/8~~ | ✅ |
| ~~POC~~ | ~~7/18~7/31~~ | ✅ 완료 — 빌드 배포됨. **이후 게임 구성은 바꾸지 않는다** |
| ~~폴리싱(버티컬 슬라이스)~~ | ~~8/1~8/9~~ | ⚠️ **기간 종료, 내용은 안 끝났다.** 남은 것은 아래 스팀 빌드 기간이 흡수한다 |
| **스팀용 빌드 제작** | **8/10~8/31** | ← **지금 여기.** 폴리싱 잔여분 + 콘텐츠·밸런싱 마무리 |
| 상점 페이지 에셋 준비 | 8/17~8/23 | ⚠️ **다낭 여행(8/17~8/22)과 겹친다** |
| 상점 페이지 등록 | 8/24 | 상점용 번역 |
| 출시 빌드 마감 | 9/1~9/4 | QA·막판 밸런스 |
| 트레일러 제작 | 9/5~9/6 | |
| 출시 | 9/11 (제한선 9/25) | 가격 3,000원 |

---

## 현재 상태 (2026-08-13, 세션 39)

**빌드**: 컴파일 통과. **번역 시스템 골격 완성** — Unity Localization 도입 + 진화/스킬 텍스트 402건을 표로 옮김.

### 🌐 번역 시스템 (칸반 「번역 시스템」 = `진행 중`)

**조회는 `Assets/Scripts/Loc.cs` 한 곳만 지난다.** 게임 코드는 Unity Localization API를 직접 부르지 않는다.
`Loc.T(키)` · `Loc.F(키, 인자…)`(보간) · `Loc.TOr(키, 폴백)`.

- 표 = `Assets/Localization/Tables/Game` (ko 397 / en 12). **en에 없으면 ko로 폴백**하므로 번역을 나눠 채워도 화면이 안 빈다.
- 🔴 **키를 손으로 짓지 말 것.** enum 이름에서 파생한다 — `evo.active.desc.{스킬}.{루트}.{티어}` 꼴.
  **진화·스킬을 추가하면 키도 저절로 늘고, 표에 값만 넣으면 된다.**
- 🔴 **한국어 원문은 이제 코드에 없다.** `DescribePathEffect`·`GetPathTierTitle`·`EvolvedName`·`GetActiveSkillName` 등
  **표 7개가 전부 한 줄짜리 조회로 바뀌었다.** 문구를 고치려면 코드가 아니라 **표**를 고친다.
- 도구: `Window > Blueberry Defense > 번역 - 하드코딩 테이블 수확` / `- 수확본을 ko 테이블에 적재`
  (`Assets/Editor/LocHarvest.cs`·`LocTableImport.cs`). **TSV가 원본이고 표는 거기서 밀어넣는다** — 손으로 편집하면 되돌릴 수가 없다.
- ⚠️ **`LocalizationSettings` 초기화는 지연이다.** `Loc`이 첫 조회에서 `WaitForCompletion()`을 한 번 태운다.
  이걸 빼면 첫 프레임에 화면에 **키가 그대로** 뜬다(`skill.name.Whirlwind` 같은 글자).
- ⚠️ 설정 패널의 언어 행은 `Loc.SetLocale` → `Loc.LocaleChanged` → **패널 통째로 재생성**이다.
  런타임에 코드로 지은 UI라 TMP를 하나씩 고치는 것보다 다시 짓는 쪽이 싸다.

**남은 것**: INLINE 문자열 116건 · 씬 TMP 28건 · `MainSkillTree.asset` 노드 68건 · `Char_*`/`Map_*` 5건 · **영어 번역 385건**.
용어집 초벌은 검수 대기 — 자세한 건 칸반 카드 본문.

🔴 **영어는 한국어보다 길다.** 진화·레벨업 카드는 폭이 고정이라 넘칠 수 있다.
영어를 다 채운 뒤 **en으로 한 판 돌려 넘치는 카드를 볼 것.**

🔴 **`Assets/Sound/`(164MB)는 여전히 미커밋이다** — LFS가 없어 그냥 넣으면 히스토리에 영구히 박힌다. 사용자 결정 대기 중.

<details><summary>세션38에 한 것 — 타이틀 UI 폴리싱 + 폰트 아웃라인</summary>

**세션38에 한 것** — 전 버튼 dim 연출 · 흰 박스를 블루베리 색으로 통일 · 카드 테두리/꺾쇠/이름표 ·
캐릭터 카드 공격 모션 · 3번 캐릭터 슬롯(잠김) · 맵 카드 배경 애니 · 커서 2배 · 벽지 무압축 ·
**폰트 아웃라인**(아래) · 🐛 **캐릭터 헤더가 화면 위로 빠지던 버그**.

- 🎛 **전 버튼 공통 연출** — 비호버·비선택 = 0.92배 + 어둡게, 호버·선택 = 원본. 카드는 `JuicyButton.SetSelected(bool)`로 연동.
  ⚠️ 원본 색을 **`Start()`에서** 잡는다 — 카드를 런타임 복제하는 화면이 `Instantiate` 직후(Awake 뒤, Start 앞)에 색을 덮어쓰기 때문.
  ⚠️ `PointerInsideBaseRect`가 이제 `RestScale`(idle 크기)로 판정한다 — `_originalScale`로 되돌리면 **세션32의 호버 굳음이 재발**한다.
- 🐛 **캐릭터 헤더 버그** — `UIFloat`이 자기 `Awake`에서 읽은 "제자리"가 `PanelSplitTransition`이 화면 밖으로
  밀어둔 좌표였다(`_baseY=1160`, 정상 400). 활성화 순서 경합이라 **전환을 소유한 쪽이 `SetHomeY()`로 알려주게** 고쳤다. 되돌리면 재발한다.
- 🖼 **카드 테두리** — `Panel_outline_2`(캐릭터) / `Panel2_outline_0`(맵)을 썸네일 **위에** 얹어 그림이 테두리를 넘지 않게 했다.
  **맵 카드의 `BackLine`은 `Border`로 대체해 제거**했다(둘 다 있으면 테두리가 두 겹으로 보인다).
- 🔒 **3번 캐릭터 슬롯 `Char_Slot3`** — 잠김 + 자물쇠(전용 도트가 없어 코드로 구운 16×16, `lockIcon`에 꽂으면 교체됨).
  **이름(`???`)·시작 스킬·해금 조건(광활한 밭 승천1 + 정수 2000)이 전부 임시값이다.**

### 🔤 폰트 아웃라인 — padding이 천장이었다 (되돌리기 주의)

**`pixelroborobo SDF`·`Pretendard-Bold SDF`의 아틀라스 padding을 9 → 20으로 올리고 2048로 재생성했다.**
Dynamic 모드라 글리프는 화면에 나올 때 자동으로 다시 채워진다 — **게임 씬을 한 번 돌려야 인게임 글자가 채워진다.**

- **아웃라인 두께의 물리적 천장 = padding.** padding 9에서는 값을 아무리 올려도 34pt에서 1.31px가 한계였는데,
  20으로 올리자 같은 조건에서 **2.99px**가 나왔다. (세션36이 데미지 숫자용 `pixelroborobo Num SDF`를 padding 20으로 따로 만든 게 같은 이유였다.)
- 값은 두 수식이 전부다 (`r` = `_ScaleRatioA`):
  - **바깥 번짐** `0.5·(D·r² + O·r)` — 크면 한글 자모 사이가 메워져 **글자 뒤에 회색 박스**로 보인다
  - **안쪽 침범** `0.5·(O·r − D·r²)` — 크면 아웃라인이 흰 면을 파먹는다. **사용자 요구로 0에 고정**(`O = D·r`)
- 안쪽 침범 0에서는 아웃라인만으로 두께를 못 키운다 → **Underlay 병용.** Underlay는 셰이더에서 `(1 − c.a)`로 곱해져
  **면·아웃라인이 있는 자리엔 안 그려진다** = 구조적으로 안쪽을 못 파먹는다. 손잡이는 `_UnderlayDilate`.
- ⚠️ **`_ScaleRatioA`/`_ScaleRatioC`를 코드로 직접 계산해 넣었다.** TMP 인스펙터에서 이 머티리얼을 손으로 만지면
  TMP가 자기 공식으로 재계산해 두께가 달라진다. 조절은 코드 쪽에서 할 것.
- ⚠️ **크기별 머티리얼이 둘** — `* - WhitePurple`(23pt 이상 12개) / `* - WhitePurple Small`(22pt 이하 6개).
  padding 20에서는 15pt도 박스가 안 나서 지금은 값이 같지만, 작은 글씨만 따로 조절할 여지로 남겼다.
- 🔴 **원본 폰트 에셋 백업이 `Temp/fontbak/`에 있다**(git ignore). 인게임에서 이상하면 그걸로 복원할 것.

### 🍍 캐릭터 카드 공격 모션 — 아트가 바뀌면 도구를 돌릴 것

**`Window > Blueberry Defense > 캐릭터 카드 공격모션 재계산`** ([CharacterCardFraming.cs](Assets/Editor/CharacterCardFraming.cs))

- 카드는 `preserveAspect`로 캔버스를 맞추므로 공격 프레임 PNG를 다시 그려 **캔버스가 바뀌면 몸통이 쪼그라들거나 밀린다.**
  실제로 파인애플이 150×60 → 150×96으로 바뀌면서 구워둔 값이 통째로 어긋났다(이번 세션에 두 번 구웠다).
- 도구가 하는 일: 애니메이터 `*_Attack` 클립에서 프레임 재수집 + **재생 속도를 인게임의 2배 느리게** +
  초상화/공격 프레임의 **불투명 bbox 실측**으로 `attackFrameScale`·`attackFrameOffset` 계산.
- ⚠️ **Title 씬이 열려 있어야 한다** — 보정값이 카드 썸네일의 표시 크기에 의존한다.
- 검증 대조군: **딸기는 캔버스가 같아 `scale 1 / offset 0`이 나와야 한다.**

### 🎨 스킬 이펙트 배선 (세션38 병렬 — 스킬 이펙트 쪽)

**사용자가 그린 이펙트를 전부 배선했다.** 진화 이펙트 명세(칸반 「스킬 리소스 확충」)의 🟡·⬜ 항목이 대부분 해소됐다.

| 이펙트 | 붙은 곳 | 비고 |
|---|---|---|
| 산탄 알 `Effect_Scatter1~5` | `Scatter_Pellet.prefab`(신규) | 부채꼴 각도만큼 알도 회전 |
| 발사 화약 `Effect_ScatterFire1~5` | `FireShotgunPellets` | **알 하나당 하나씩** 뒤쪽에 스폰 |
| 미니 회오리 `Effect_MiniTornado1~4` | `Whirlwind_Mini.prefab`(신규) | 그림이 48px(본체 64px)이라 **scale 1.333× + 콜라이더 0.75×로 상쇄** — 화면 크기·판정·바닥선 전부 불변 |
| 피뢰침 `Effect_LightningRod1~4` | `SpawnLightningRod` | 회색 막대 프리미티브 제거 |
| 큰 낙뢰 `Effect_BigThunder1~5` | `LightningRodRoutine` | 틱(1초)마다 기둥 꼭대기에. 피해·기절과 같은 틱 |
| 호밍 폭발 `Effect_Explosion1~4` | `Homing_Missile.prefab` | Vefects 대체. `explodeVfxScale` 0.6→**2.0**(지름 3유닛 = 폭발 반경) |
| 충격파 `Effect_Shockwave1~2` | `Swing_Shockwave.prefab` | `Pinapple4` 오려낸 임시본 대체. 콜라이더 불변 |
| 경험치 보석 `Particle_Exp` | `XpGemFlight` | `UI_SolidFill_0`+노란색 → 원본 색(gemColor를 흰색으로 품) |
| 되감기 3종 | `FireRewind` | 기본 / 루트1 `RewindYellow` / 루트2 `Rewind_R`. **연출이 아예 없던 자리** |
| 뇌운 `Effect_StormCloud1~4` | `StormCloudAura`(신규) | 낙뢰 스택 수만큼 오프셋 겹쳐 띄움(최대 6) |
| 파인애플 망치 `Effect_Hammer1~5` | `Pinapple_Idle`/`_Attack` 클립 | `Player/Hammer` 자식 트랙. 캔버스가 몸과 같은 150×96이라 좌표 보정 없음 |

- 🆕 **`RiseFade.cs`** — 머리 위 표식(되감기·뇌운)이 아래에서 쓱 올라오고 사라질 때 위로 쓱 빠진다.
  ⚠️ 위치를 `transform`이 아니라 **`Anchor`** 로 잡는다 — 뇌운처럼 플레이어를 따라다니는 쪽이 `transform.position`을 덮어쓰면 **연출이 통째로 죽는다.**
- 🆕 **`RockDebris.cs`** — 휘두르기에 맞은 적한테서 돌조각이 튄다(**전 루트**, 사용자 요청). 같이 **노란 범위 사각형을 제거**했다(`SpawnSwingRange` + 상수 5개 삭제).
- 🔴 **`ObjectPool.Despawn`에 중복 방지가 없다.** 플립북 자동반환(`despawnOnFinish`)과 호출측 타이머가 겹치면 **같은 오브젝트가 큐에 두 번** 들어간다.
  → 호출측이 Despawn을 부르는 VFX(`Effect_Explosion`)는 `despawnOnFinish=false`, 안 부르는 쪽(`ScatterFire`·`BigThunder`·되감기)만 true. **새 VFX를 만들 때 이 짝을 맞출 것.**
- 🔴 **임포트 정규화 스크립트로 `spriteAlignment`를 건드리지 말 것.** 세션38에 `Effect_*` 전체 정규화를 돌리면서
  `Effect_BigTornado` 4장의 pivot을 **바닥중앙(alignment 9) → 정중앙(0)** 으로 바꿔 버렸다(세로 213px이라 5유닛 어긋난다).
  git으로 되돌렸지만 **같은 스크립트를 다시 돌리면 또 깨진다.** 정규화는 `spriteMode`·PPU·filter까지만.
- ⚠️ **사용자 아트가 Multiple로 들어온다.** 이번에도 14장이 기본값이었고 그중 **6장이 실제로 조각나 있었다**(`BigHammer5`는 4조각).
  배선 전에 `spriteMode`와 조각 수를 반드시 확인할 것.

</details>

**🔴 딱 하나 남았다 — 망치 진화 컨트롤러 2개를 인스펙터에 꽂기** (세션39에 코드로 재확인 — 여전히 미배선)

에셋·코드는 전부 있는데 **씬 배선만 못 했다**(세션 내내 플레이모드였다). `SampleScene`의 `Player` → `PlayerSkills`에서:
- `bigHammerController` ← `Assets/Animations/Pinapple_BigHammer.overrideController`
- `shockHammerController` ← `Assets/Animations/Pinapple_ShockHammer.overrideController`

안 꽂아도 **기본 망치는 정상 동작**한다(`ApplyHammerLook`이 null이면 그냥 return). 진화해도 망치 그림이 안 바뀔 뿐이다.
⚠️ `ApplyHammerLook`은 **base 컨트롤러가 같을 때만** 교체한다 — 딸기가 휘두르기를 진화시켜도 파인애플로 바뀌지 않게 한 가드다. 지우지 말 것.

**🔎 플레이 확인 대기 (세션38 스킬 이펙트 — 값·배선은 검증됨, 남은 건 화면)**
- 🍍 **파인애플이 위로 떠 보이는지** — 캔버스가 150×60 → **150×96**으로 커졌다. pivot이 중앙이라 몸이 아래쪽에 있으면
  이론상 **0.84유닛 위로** 이동한다(늘어난 36px ÷ 2 ÷ PPU32 × scale 1.5). 뜨면 각 PNG의 `spritePivot`만 내리면 된다.
- 🪨 **돌조각이 과한지** — 맞은 적 **한 마리당 4개**가 튄다(`RockDebrisPerHit`). 광역이라 5마리면 20개다.
  ⚠️ `Effect_BigHammer` 그림에 **파편이 이미 그려져 있어** 겹쳐 보일 수 있다.
- 🌀 **미니 회오리·산탄 알 크기** — 판정은 불변인데 **그림 크기는 사용자가 그린 캔버스를 따른다**(미니는 기존과 동일하게 맞췄고, 산탄 알은 48px로 커졌다).
- ⏱ **되감기 표식이 루트별로 다른 그림인지** — 기본/`RewindYellow`(루트1)/`Rewind_R`(루트2). **사용자가 "일걸"이라고 한 대응이라 확인이 필요하다.**
- ☁️ **뇌운 개수·간격** — 스택마다 1개씩 최대 6개, 가로 간격 0.5유닛으로 겹친다(`StormCloudAura`의 `spacingX`·`stagger`).

<details><summary>세션37에 한 것 — 일정 재논의 · 사운드 시스템 · 아트 선행 명세</summary>

- 📅 **일정 재논의** — 병목은 날짜가 아니라 **아트 손 하나**. 상점 페이지 아트를 외주로 빼서 다낭 여행(8/17~22) 구멍이 거의 해소.
  **출시일(9/11 vs 제한선 9/25)은 8/31 스팀 빌드 상태를 보고 정한다.**
- 🎨 **친구용 UI 프레임 9-slice 사양서** — https://claude.ai/code/artifact/44f65c89-cde0-42dc-9ad8-64001e0c20de
  **아티팩트가 비공개라 공유 설정 후 전달해야 한다.**

### 🔊 사운드 — 배선은 끝, 음원 선정이 남았다 (칸반 카드 「사운드」 = `진행 중`)

- **에셋을 먼저 만들어야 소리가 난다**: `Blueberry Defense > 사운드 라이브러리 만들기` → `Assets/Resources/SfxLibrary.asset`.
  **경로·파일명이 곧 배선**이라(`Resources.Load<SfxLibrary>("SfxLibrary")`) 손으로 만들지 말 것. 슬롯이 비면 조용히 무시되므로 하나씩 채워도 된다.
- **음원은 이미 있다** — `Assets/Sound/`에 **400 Sounds Pack + UI Soundpack(608개)**. Combat·Items·UI·Musical Effects에서 14종이 거의 다 나온다.
  **없는 건 BGM뿐**(둘 다 효과음 팩). `MapDefinition.bgm`에 꽂으면 `RunBootstrap`이 재생한다 — 현재 전 맵 null = 무음.
- 🔴 **음원 팩 164MB가 아직 커밋되지 않았다.** LFS가 없어서 그냥 넣으면 히스토리에 영구히 박힌다.
  608개 중 쓸 건 14개라 **고른 것만 남기는 쪽을 권고**했고, 사용자 결정 대기 중이다.
- ⚠️ **스킬 캐스트음 9종은 `SfxLibrary`가 아니라 `PlayerSkills` 인스펙터**(`whirlwindCastSfx` 등)에 이미 슬롯이 있다. 중복해 넣지 말 것.
- ⚠️ **`JuicyButton`은 별도 어셈블리**(`JuicyUI.Runtime.asmdef`)라 `SfxPlayer`를 참조할 수 없다.
  `JuicyButton.Clicked` static 이벤트를 `SfxPlayer`가 구독하는 **역방향 배선**이다 — 직접 호출로 되돌리면 컴파일이 깨진다.
- ⚠️ **승리는 `GameOver()`를 거치지 않는다** — `GameClear()`에 따로 배선돼 있다.

### ✅ 되감기 연출 — 세션38에 해결 (그림 3세트 + 배선 완료)

명세는 `Effect_Rewind_Base`/`P0T1`/`P1T1`이었지만 실제 파일명은 **`Effect_Rewind*`(기본) · `Effect_RewindYellow*`(루트1) · `Effect_Rewind_R*`(루트2)** 로 들어왔다.
셋 다 48×48이고 **머리 위에 떴다 사라지는** 방식으로 통일했다(사용자 결정 — 루트별로 성격을 나누지 않음).

### 🖼 Panel 그림 3장 — 쓰기 전에 정할 것 (커밋 `e06b06b`, 아직 미배선)

- `Panel.png`(1794×1216) = **16조각 시트**(4색 × {정사각, 바} × {깔끔, 손그림}) · `Panel2.png` = 가로로 넓은 4조각 · `Panel_outline.png` = 속 빈 4조각
- ⭐ **색은 안 잘라도 된다** — 테두리가 검정이고 안쪽이 흰색이라 `Image.color`를 곱하면 테두리는 그대로고 흰 부분만 물든다.
  ✅ 세션38에 이 성질을 그대로 썼다(흰 박스 9개를 블루베리 색으로).
- ✅ **`Panel_outline`의 속 빈 조각 2개는 세션38에 배선됐다** — `Panel_outline_2`(캐릭터 카드) · `Panel2_outline_0`(맵 카드).
  썸네일 위에 덮어 그림이 테두리를 넘지 않게 하는 용도. 크기가 `UI_Rect_Unclean`·`UI_LongRect_Unclean`과 거의 정확히 대응한다.
- 🔴 **손그림 스타일은 9-slice와 상성이 나쁘다** — 변 중간을 늘리는 방식이라 삐뚤빼뚤한 선이 같이 늘어난다.
  `Panel2`(가로형)를 따로 그린 게 그 증거다. **깔끔한 스타일=9-slice 1장으로 전 창 / 손그림=비율마다 따로**의 두 갈래.
- 🔴 **스타일 방향이 미정이다**(픽셀아트 vs 지금의 매끈한 벡터풍). **친구에게 준 사양서가 여기에 달려 있어서**, 정해지면 사양서도 같이 고쳐야 한다.
- 아래쪽 그림자가 두꺼워서 9-slice border는 **상하 비대칭**으로 잡아야 한다(아래를 그림자 두께까지 포함).

</details>

<details><summary>세션36에 한 것 — 칸반 요청 7건 + 아트 리소스 명세</summary>

### 🔤 폰트 세션 (세션36 병렬) — UI 폰트 전면 교체 + 데미지 숫자 재설계

**갈무리11을 걷어내고 역할별로 나눴다. 씬·프리팹 텍스트 97개 전부 교체 완료(갈무리 잔여 0).**

| 역할 | 폰트 | 라이선스 |
|---|---|---|
| 제목·버튼·HUD 수치·이름 (71개) | **픽셀로보로보체** | SIL OFL |
| 설명문·툴팁 본문 (26개) | **Pretendard** Regular/Bold | SIL OFL |
| 데미지 숫자 | **픽셀로보로보체** + 포스터라이즈 셰이더 | SIL OFL |

- **갈무리11은 상업 이용이 안 될 거라는 짐작이 틀렸다** — OFL이라 원래 안전했다. 교체 이유는 라이선스가 아니라 **B급 도트 톤**이다.
- **데미지 숫자** = 이중 테두리(흰 `_OutlineWidth` 0.15 + 검정 `_UnderlayDilate` 0.55, offset 0·softness 0) + 세로 그라데이션.
  일반 크림→주황, 치명타 주홍→진빨강([DamageNumber.cs](Assets/Scripts/DamageNumber.cs)의 `CritGradient` 상수). fontSize 8.
- 🆕 **`Assets/Shaders/TMP_SDF_Posterize.shader`** — TMP `Mobile/Distance Field` 복제본에 **면 색상 계단화 함수 하나만** 추가한 것.
  머티리얼의 **`Posterize Steps`(현재 4)** 로 조절한다. 3=가장 도트답고, 6=거의 원래대로.
  ⚠️ TMP 커스텀 GUI를 일부러 뺐다 — 안 그러면 이 속성이 인스펙터에 안 보인다.



- **플테후 제안 3건**(카드 완료) — ① 호밍 미사일 타겟 분산(`HomingMissile.TargetRank` = 발사 순번 → *비행 우선 → 가까운 순* 정렬의 i번째)
  + 발사각 ±15도 난수 ② `swing_StartLev` 노드 신설(파인애플 휘두르기 3레벨 시작) ③ 보물 화면 Space·Enter 스킵.
- **UI 폴리싱 4건** — 🐛 **레벨업 화면에서 쿨 스킬이 눌리던 버그**(`PlayerSkills.Update`가 timeScale 0에도 돌았다 → `ModalPause.IsPaused` 가드) ·
  스킬트리 노드 테두리 4색 · 정수/하트 곡선 비행 · 적 발밑 그림자 + HUD 좌상단 "이번 판 정수".
- **하트 크기** `HeartPickup.prefab` 0.6 → **1.0유닛**(사용자 "잘 안 보인다"). 정수는 0.85유닛.
- **아트 명세 2건** — 「스킬 리소스 확충」 카드에 **진화 20루트 이펙트 표**(그릴 것·권장 px·현재 상태, 프리팹 실측),
  새 카드 **「아트 리소스 제작」**에 그려야 할 것 전부를 우선순위로 모음.

> 검증: 전부 컴파일 + 값 대조. 픽업 곡선은 **24케이스 도착 시뮬레이션**(최악 1.98초, 대조군 8/8 실패) ·
> 그림자 타원은 **면적 78%(=π/4)** · 그림자 지면 고정은 **lift 0·0.8·2.2에서 드리프트 0**(보정 없으면 lift만큼 그대로 떠오름).

</details>

<details><summary>세션35에 한 것 — 진화 아이콘 32종 배선 + 힘 R1 명세 반영 + 화살 진화 이펙트</summary>

- **진화 아이콘 32종 전부 배선** — 사용자가 그린 `Icon_*R1/R2` 32장(액티브 10×2 + 패시브 6×2).
  **파일명 R1=루트0 / R2=루트1**이고 1·2차 티어는 같은 그림을 쓴다(그림으로 교차검증: 화살·회오리·산탄·건강).
  배열은 **`LevelUpUI`가 단독 소유**하고 HUD·진화 트리가 `LevelUpUI.Instance`로 빌려 간다 — 32칸 배열이 세 벌 되는 걸 막으려는 것.
  적용 화면: HUD 액티브/패시브 슬롯 · 레벨업/보물/진화 대상 카드 · **조합 미리보기(루트별 진화 후 그림)** · 진화 트리 노드 2×2 · ESC 요약.
  **HUD 버프 아이콘(산탄·낙뢰)은 일부러 안 건드렸다** — 버프 종류 표시라 진화와 무관.
- **힘 R1("완력/괴력")을 명세대로 고쳤다** — `BasicAttackDamageMultiplierBonus`(화살 쏘기 고정) → **`FirstSlotDamageMultiplierBonus`(Q 슬롯 스킬)**.
  ⚠️ **Q는 슬롯 이름이 아니라 가장 먼저 얻은 스킬**이다(`AcquireSkill`이 `SlotKeys`를 순서대로 붙임) — 파인애플처럼 휘두르기로 시작하면 대상이 달라진다.
  값(10/17.5/25%)은 그대로. ESC 요약은 이제 지금 Q에 있는 스킬 이름을 읽어 표시한다.
- **화살 진화 투사체 그림** — `Effect_ArrowR1`=1차(암살 사격) / `Effect_ArrowR2`=2차(처형 사격). `SpawnBasicAttackProjectile`에서 교체하고
  **뒤따르는 추적 화살은 원본 그대로**(명세상 "기본 화살"). 화면상 3×1.5 유닛으로 기존 화살(1.5×1.5)의 가로 2배다.
  ⚠️ 두 PNG는 **Multiple 자동 슬라이스로 조각나 있던 것**을 Single·PPU32·Point·무압축으로 고쳤다.
- **에디터 도구 `Assets/Editor/EvolutionIconWiring.cs`** — `Window > Blueberry Defense > 진화 아이콘 배선`.
  아이콘 임포트 정규화 + `LevelUpUI` 배열 자동 배정. **아이콘을 새로 그리거나 고치면 이걸 다시 돌리면 된다.**

> 검증: 배선 **34칸 전부 파일명과 대조(불일치 0)** · 리프레쉬 자리 2칸 null · 런타임 경로를 리플렉션으로 직접 태움(`Shotgun R1→Icon_ScatterR2` 등).
> 대조군 2종: ① 임포트 정규화가 "0장"이라 나와서 **일부러 1장을 Bilinear·PPU100으로 망가뜨려** 재실행 → 정확히 1장을 잡아 되돌림(검사가 살아 있음을 증명).
> ② 힘 R1은 `bonus=0`일 때 전부 100 · `0.25`일 때 Q만 125이고 **화살 쏘기는 100**(옛 규칙이었다면 125가 나왔을 자리) · Q를 바꾸면 대상도 따라감.

</details>

<details><summary>세션34에 한 것 — 노션 명세 16종을 진화 효과로 전부 구현</summary>

규칙 두 개를 사용자가 정하고 시작했다: **불릿 = 1차 / 2차 = 같은 기믹의 강화판**, 그리고 **명세 없는 16루트는 손대지 않는다.**

- **패시브 6종** — 방어R0(피격 시 휘두르기 자동) · 방어R1(최대체력 +200%·피해 3배 반사) · 가속R0(쿨 초기화) ·
  가속R1(피격 시 전체 쿨 −0.5초) · 건강R1(하트 드랍 5배) · 암살R1(쿨 5초↑ 스킬 확정 치명타)
- **액티브 10종** — 화살R0(관통∞ 한 발+치명타 추적화살) · 화살R1(화살비) · 회오리R0(2개+소멸 시 미니) ·
  오브R1(추적 오브 관통3) · 낙뢰R0(버프 중첩 부활) · 낙뢰R1(피뢰침) · 독수리R0(독수리 비 3초) ·
  스나이핑R0(주변 독수리) · 호밍R1(소형 다수) · 산탄R1(전방 집중)
- **`HasEvolutionDesign` 가드가 사라졌다** — 방어·가속 진화가 생겨 더는 필요 없다. 새 패시브를 만들면 진화까지 같이 만들 것.

> 검증(전부 대조군 포함): 진화 이름·설명 텍스트 **64/64** · 효과 값(화살R0 dmg×3·연사→0, 독수리R0 쿨×1.4, 호밍R1 쿨×0.7·0.8) ·
> **path 좌표 16개가 전부 `RoutePath`와 일치**(손으로 세지 않고 값으로 확인). 대조군은 전부 빈칸/원본값이 찍혀 검사가 무력하지 않음을 같이 증명했다.

</details>

<details><summary>세션33에 한 것 — 진화 조건표 전면 개편 + 패시브 "가속" 신설 + 아이콘 2종</summary>
- **연계 조건을 루트 좌표에서 떼어냈다** — 예전엔 `RoutePath`가 곧 조건이었지만(path1=패시브·path2=액티브·path0=무조건)
  노션 NEW 조건표가 그 틀을 셋 다 깬다: **두 루트가 둘 다 액티브**(회오리·독수리·산탄) · **둘 다 패시브**(되감기) · **R0=액티브/R1=패시브인 반대 배치**(낙뢰·스나이핑·호밍·가속·방어).
  → [EvolutionRoutes.cs](Assets/Scripts/EvolutionRoutes.cs)의 `RoutePrereq`가 `(스킬, 루트) → 조건`을 **단독 소유**한다.
  **`RoutePath`는 진화 효과 매핑용으로 그대로 살아 있고 `PathTier[3]` 저장 구조도 안 건드렸다.**
- **조건 없는 루트가 10개 → 0개.** 신규 4종(스나이핑·호밍·산탄·되감기)의 연계 조건 부재가 해소됐다(사용자 확인된 의도).
- **패시브 "가속" 신설** — 전 스킬 쿨타임 감소. 획득 5% / 레벨당 +2.5%p → 만렙 **27.5%**. 아래 카드 ⑥.
- **리프레쉬 폐지** — 레벨업 후보 배열에서만 뺐다. **enum과 `Passive_Refresh.asset`은 남긴다**(정수 직렬화라 지우면 방어=5·가속=6이 밀린다).
- **아이콘 2종 배선** — 사용자 아트 `Icon_Defence`·`Icon_Accel`(각 32×32)을 3곳 `passiveIcons`에. 배열 5 → 7칸.
- **ESC 요약에 방어 줄 추가** — `DescribeCurrentEffect`에 빠져 있었다(세션32 누락분).

> 검증: 조건 **32/32** 노션 표와 일치 · 조건 없는 루트 0개. 대조군으로 "옛 규칙이었다면 나왔을 값"을 같이 찍어 **22루트가 실제로 달라졌음**을 확인했다.
> 가속 쿨감은 대조군(미보유) 0% → 획득 5% → 만렙 27.5%, 잠금은 지식 R0가 미보유 False → 가속 획득 후 True → 그래도 암살 R0는 False.

</details>

<details><summary>세션32에 한 것 — 버튼 버그 2건 + 노션 문서화 + 패시브 "방어" 신설</summary>
- **호버 떨림 수정** — `visualRoot`에 버튼 자신을 넣어 쓴 탓에 **회전이 히트박스를 통째로 돌려** 커서가 가장자리에 있으면
  `Exit ↔ Enter`가 무한 반복됐다(900×170 레벨업 카드에서 신고). `JuicyButton`에 "회전 중 Exit는 미뤘다가 회전이 0으로 돌아온 뒤 한 번만 재판정"을 넣었다.
- **호버 굳음 수정** — 위 재판정이 **확대된 rect(1.15배)** 로 판정해서, 커서가 나갔는데도 "안"으로 읽혀 커진 채 굳었다(버튼 사이 빠르게 이동 시).
  `PointerInsideBaseRect`로 **원래 크기·각도로 되돌려 판정**하게 고쳤다. 대조군까지 실측 검증.
- **버튼 피벗 19개 전수 조사 → 8개 교정** — 끝 피벗이라 한쪽으로만 커지던 것을 중앙으로. `anchoredPosition` 보정으로 보이는 위치는 유지(이동 0px 검증).
  런타임 생성 버튼 5개는 배치 뒤 `JuicyTuning.CenterPivot()` 호출.
- **패시브 "방어" 신설**(새 칸반 카드) — 받는 피해 비율 감소. **기본만 구현, 진화는 다음 세션.** 아래 카드 ⑤ 참고.
- **노션 문서 2개** — 진화 피드백 수집본 + **스킬 데이터 시트**(전 스킬 수치·커브·진화 조건 다이어그램). 아래 카드 ① 참고.
- **일정 정정** — 폴리싱 마감이 이 문서에 8/7로 잘못 적혀 있었다(캘린더 원본은 **8/9**).

> 밸런싱 수치는 **이번에도 하나도 안 건드렸다.** 사용자가 스킬 데이터 시트에 수치와 함께 방향을 정리해 둘 예정 → 다음 세션에 반영.

</details>

> (세션27·29는 게임 코드를 안 고친 **파이프라인 정비** 세션이었다 — `CLAUDE.md` 244→159줄, 절차를 스킬 **4종**으로 분리(`kanban`·`unity-mcp`·`wrap`·**`brief`**), 서브에이전트는 안 쓰기로 확정.
> 키트 스킬은 `prototyping-kit/skills/`가 원본이고 `~/.claude/skills/`에 정션으로 연결돼 있다. `kanban`만 프로젝트 고유라 `.claude/skills/`에 있다.)
>
> 🆕 **세션29: 세션 시작 루틴이 `brief` 스킬이 됐다.** `CLAUDE.md` §7은 포인터 + 규칙 3개만 남았다.
> 핵심은 **HANDOFF의 "~해야 함"을 코드로 대조해 ✅해결/🟡어긋남/⬜유효로 분류하고, ✅는 그 자리에서 지우는 것** — 이 문서가 매 세션 줄어들게 하는 장치다.
> 읽기 전용이라 사용자가 안 쳐도 **세션 첫 턴에 자동 실행**된다.

### 🔎 플레이 확인 대기 (세션38 — 타이틀 씬은 화면까지 봤다, 남은 건 **인게임과 손끝 감**)
- 🔤 **인게임 글자** — 폰트 아틀라스가 비워졌다(Dynamic). **게임 씬을 한 판 돌려** 글자가 정상으로 채워지는지,
  HUD·레벨업·툴팁이 padding 20에서도 멀쩡한지 볼 것. 이상하면 `Temp/fontbak/`로 복원.
- 🎛 **버튼 dim 강도** — 비호버 시 0.92배 + `idleTint`(0.55). **타이틀 메뉴 버튼이 꽤 칙칙해졌다.**
  세면 `JuicyButton`의 `idleTint`/`idleScale`만 올리면 19개가 전부 따라온다.
- 🖱 **커서 2배가 적당한지** — `CursorSetter.pixelScale`(2). 정수 배수만 도트가 안 뭉갠다.
- 🐛 **버튼 호버가 굳지 않는지** — `PointerInsideBaseRect` 판정 기준을 `_originalScale` → `RestScale`로 바꿨다.
  세션32에 실제로 났던 버그의 자리라 **버튼 사이를 빠르게 왕복**해 볼 것.
- 🍍 **파인애플 공격 모션의 잘림** — 몸통을 초상화 크기에 맞추면 캔버스가 카드보다 커서 충격파가 마스크에 잘린다.
  더 보이게 하려면 `Char_Pineapple.attackFrameScale`을 낮추면 되는데 그만큼 몸통이 작아진다.
- 🗺 **맵 카드 이름표 간격은 안 줄였다** — 캐릭터 카드만 -24 → -4. 맵도 같은 값이라 원하면 같이 줄이면 된다.

### 🔎 플레이 확인 대기 (폰트 세션 — 배선·글리프는 값으로 검증됨, 남은 건 **화면**)
- 🔢 **데미지 숫자 계단 그라데이션** — `pixelroborobo Num Posterize.mat`의 **`Posterize Steps`(4)** 가 적당한지. 3이면 더 도트답다.
  **fontSize 8**도 같이 볼 것 — 맵마다 `RunBootstrap`의 `fieldScale`로 카메라 ortho가 달라져 **체감 크기가 맵별로 다르다.**
- 🔡 **작은 설명문 가독성** — 15pt `UnlockPoster/Condition`, 18pt `Tooltip/Desc`가 Pretendard로 잘 읽히는지.
- 🅱 **강약이 사라진 자리** — **픽셀로보로보체엔 Bold가 없다.** 전엔 갈무리 Bold/Regular로 강약을 주던 자리가 이제 같은 굵기다.
  강조가 필요하면 색이나 크기로 줘야 한다.
- ➡️ **기호 8개만 Pretendard로 남았다** — 승천 `AscPrev`/`AscNext`(◀▶)와 진화 트리 `Arrow_P*_*` 6개(▶).
  도트 사이에서 이것만 매끈해 보이면 **기호를 이미지로 갈아끼우는 쪽**이 답이다(폰트로는 해결 안 됨).

### 🔎 플레이 확인 대기 (세션36 — 로직은 값·시뮬레이션으로 검증됨, 남은 건 **화면과 감**)
- 🎯 **호밍 미사일이 실제로 흩어지는지** — 적이 많을 때 미사일이 각각 다른 적으로 가는가. 각도 난수 ±15도가 과하면 `FireHoming`의 `spreadJitter`.
- 🌲 **`swing_StartLev` 노드 위치** — 지금 (650, -790)에 있고 선행이 `atk_1`이다. **자리부터 사용자가 옮길 것**(트리 에디터).
  파인애플로 시작하면 휘두르기가 3레벨로 시작하는지 확인. 딸기 판에선 대상이 없어 **조용히 무시**되는 게 정상.
- 💠 **스킬트리 노드 테두리 4색** — 만렙=파랑 / 지금 찍을 수 있음=초록(펄스) / 정수 부족=빨강 / 선행 미충족=회색.
  ⚠️ **전엔 링이 대부분 꺼져 있었는데 이제 보이는 노드 전부에 켜진다** — 시끄러우면 회색만 다시 끄면 된다([SkillTreeUI.cs](Assets/Scripts/SkillTreeUI.cs)의 `v.ring.enabled = true`).
- 🍯 **정수·하트가 곡선으로 날아오는지** — 무작위 방향으로 튄 뒤 휘어 붙는다. 곡선 크기는 `curveTurnDegPerSec`(260, 낮을수록 크게 휨).
  도착 보장은 24케이스 시뮬레이션으로 확인했다(최악 1.98초, 대조군 8/8 실패).
- ⌨️ **레벨업·보물·진화·ESC 창이 떠 있는 동안 스킬이 안 나가는지** — `PlayerSkills.Update` 첫 줄에서 `ModalPause.IsPaused`면 통째로 return한다.
  ⚠️ **부작용 확인 필요**: 모달이 닫힌 뒤 키를 계속 누르고 있으면 정상 발동해야 한다.
- 💰 **HUD 좌상단 "N 정수"** — **이번 판에 모은 정수**(`MetaRun.RunCurrency`, 총 보유가 아니다). 값이 오를 때 펀치.
  ⚠️ **씬이 아니라 `HUDController.UpdateEssenceText`가 런타임 생성**한다(다른 세션이 동시에 Unity를 쓰고 있어 씬 저장을 피했다).
  좌상단은 HUD에서 유일하게 비어 있던 모서리다. 위치·색을 바꾸려면 그 함수 안 `anchoredPosition`(28,-28)·색 상수.
  폰트는 `levelText`에서 빌려오므로 **그게 미배선이면 정수 표시도 안 만들어진다**.
- 💗 **하트가 커진 것** — `HeartPickup.prefab`의 localScale **0.6 → 1.0**(그림은 `핫뚜1` 32×32 그대로). "잘 안 보인다"는 사용자 지적.
  정수(`태양정수_0`, scale 0.85 = 0.85유닛)보다 살짝 크다. **정수·체력 아이콘 그림은 이걸로 확정**이라 다시 그리지 않는다.
- 👤 **적 발밑 그림자** — 16×8 타원을 **코드로 구워 모든 적이 한 장을 공유**한다(`Enemy.BuildShadowSprite`). 지상 프리팹 8종만, 비행 2종은 제외.
  크기는 몸통 폭의 70%, 알파 0.35, 본체 바로 뒤(sortingOrder −1). 전용 도트가 나오면 `BuildShadowSprite`를 로드로 바꾸면 된다.
  **몸이 떠도 그림자는 땅에 남는다** — `Enemy.LateUpdate`가 뜬 높이만큼 자식 좌표를 내려 지면에 고정한다(부모 스케일로 나눠 보정).
  지면 기준은 **콩콩이=`hopBaseY` / 팝콘 분출=`popGroundY` / 나머지=자기 y**. 드리프트 0을 lift 0·0.8·2.2에서 값으로 확인했다.
  ⚠️ **높이 떠도 그림자 크기는 그대로다**(작아지지 않는다). 그건 안 넣었다.

### 🔎 플레이 확인 대기 (세션35 — 배선·경로는 값으로 검증됨, 남은 건 **화면**)
- 🎨 **진화 트리 노드 2×2의 새 아이콘** — 루트 색상 아이콘이던 자리가 진화 후 그림으로 바뀌었다. **노드가 작아 32×32 도트가 뭉개져 보이는지**가 관건.
  뭉개지면 노드 아이콘만 `pathIconSprites`로 되돌릴 수 있다([EvolutionTreeUI.cs](Assets/Scripts/EvolutionTreeUI.cs)의 `RouteIcon(route) ?? GetIcon(pathIconSprites, route)`에서 앞을 빼면 된다).
- 🖼 **조합 미리보기** — 한 줄에 루트 2개가 **서로 다른 그림**으로 떠야 맞다(전엔 같은 원본이 두 번 떴다).
  세션28에 "같은 아이콘이 두 번 뜨는 건 상관없다"고 정리했던 항목이 이걸로 자연 해소된다.
- 🏹 **화살 진화 투사체** — 1차/2차에서 그림이 각각 바뀌는지, **가로 2배(3×1.5 유닛)가 과하지 않은지.**
  화살촉 방향이 진행 방향(왼쪽)과 맞는지도 볼 것 — Projectile은 로컬 left로 날아간다.
- 💪 **힘 R1 대상이 Q 스킬로 바뀐 것** — ESC 요약에 `Q(스킬이름) 피해량 +N%`로 뜬다. **파인애플로 한 판** 돌려 휘두르기가 강해지는지 볼 것(딸기는 화살이라 예전과 구분이 안 된다).
- ✨ **HUD 슬롯은 진화 순간 아이콘만 바뀌고 `PunchIcon`이 안 튄다**(진화는 슬롯 채움·쿨 종료가 아니라서). 밋밋하면 연출 추가.

### 🔎 플레이 확인 대기 (세션34 — 값·로직은 대조군까지 검증됨, 남은 건 **세기**)
진화 16종이 통째로 바뀌었다. 아래는 전부 "동작은 하는데 과한가/약한가"를 눈으로 봐야 하는 것들이다.
- 💥 **독수리 비**(독수리R0) — 3초간 0.3초마다 화면 전체. 틱당 피해를 **35%로 눌러** 총량을 기존 2~4회 투하의 3~6배로 맞췄다.
  세면 `EagleDropRoutine`의 `rainDamageRatio`, 늘어지면 `rainDuration`/`rainInterval`.
- 🏹 **화살비**(화살R1) — 발당 50% × 10발 × 2회(2차 3회). 손잡이는 `ArrowRain*` 상수 4개.
- ⚡ **피뢰침**(낙뢰R1) — 본체 3배 × 1초마다 × 6초, 반경 7. 기둥은 **임시 프리미티브**(회색 막대+노란 촉).
- 🛡 **방어R1 최대체력 +200%** — 진화 시점의 최대체력 기준이라 **건강 패시브를 많이 올린 뒤 진화하면 폭발**한다. 순서 의존이 의도인지 볼 것.
- 🎯 **암살R1 확정 치명타** — 쿨 5초 이상 스킬은 치명타 확률 **상한 70%를 무시하고 100%**다. 대상: 오브·낙뢰·독수리·스나이핑·산탄·되감기.
- 🌀 **회오리R0** — 본체 2개가 좌우 0.7씩 벌어져 나가고, **각각 사라질 때** 미니 2개(2차 3개)를 남긴다. 화면이 회오리로 덮이는지.
- ⚪ **오브R1** — 산탄 알 프리팹을 재사용한 추적 오브다. **전용 그림이 없어 산탄 알처럼 보인다**(`SpawnHomingSmallOrbs`에서 교체).

### 🔎 플레이 확인 대기 (세션33 — 값은 전부 대조군까지 검증됨, 남은 건 화면)
- 🔗 **진화 목록이 초반에 텅 비지 않는지** — 조건 없는 루트가 0개가 됐다. 보물상자를 일찍 먹었을 때 고를 게 아예 없는 판이 생길 수 있다.
  답답하면 **되돌릴 자리는 [EvolutionRoutes.cs](Assets/Scripts/EvolutionRoutes.cs)의 `RoutePrereq` 한 곳뿐**이다(일부 루트를 `(null, null)`로).
- ⏱ **가속 만렙 27.5%가 과하지 않은지** — 스킬트리 전역 쿨감과 **곱해진다.** 세면 `Passive_Accel.asset`의 두 값만 고치면 된다.

### 🔎 플레이 확인 대기 (세션32 — 로직은 대조군까지 검증됨, 남은 건 손끝 감)
- 📐 **피벗 교정 8개가 제자리에 있는가** — 화면 좌표 이동 0px을 값으로 검증했지만 눈으로는 미확인.
  대상: 타이틀 `StartButton`·`AscPrev`/`AscNext`·`CloseButton`·`BackButton`×2·`ChangeCharButton` / 인게임 `ReturnToTitleButton`.
- 🛡 **방어 패시브** — 레벨업 카드에 뜨는지(아이콘은 빈칸이 정상), 실제로 덜 아픈지. **초반엔 체감이 없는 게 정상**(아래 카드 ⑤).

### 🔎 플레이 확인 대기 (세션31 — 전부 로직·배선은 검증됨, 남은 건 "눌러보고 감")
- 🎛️ **버튼 호버 손맛** — 가로 1.3배로 늘어나며 **7도 기울었다가** 돌아오고 1.15배로 커진다. 과하거나 떨리면
  `JuicyTuning.ShakeStrength`(7) / `HoverScale`(1.15) 하나만 고치면 **19개가 전부 따라온다.**
  ⚠️ 회전은 `visualRoot`에 **버튼 자기 자신**을 넣어 켰다(자식 래퍼를 안 만들려고) — 즉 **회전이 히트박스를 통째로 돌린다.**
  세션32에 이것 때문에 **커서를 가장자리에 두면 무한히 덜덜 떨리는 버그**가 실제로 났고(사용자 신고, 900×170 레벨업 카드),
  `JuicyButton`에 "회전 중 Exit는 미뤘다가 회전이 0으로 돌아온 뒤 한 번만 재판정"을 넣어 고쳤다. **되돌리면 떨림이 돌아온다.**
  ⚠️ 그 재판정은 반드시 **`PointerInsideBaseRect`(원래 크기·각도로 되돌려 판정)** 를 써야 한다. 확대된 rect로 판정하면
  **원본 반폭 450 ~ 확대 반폭 517.5 사이 띠**에서 커서가 나갔는데도 "안"으로 읽혀 **호버가 커진 채 굳는다**(세션32에 실제로 났고 대조군까지 검증).
- ⚔️ **휘두르기 판정 높이** — `SwingCenterYOffset`(**0.6**)은 눈대중 값이다. 세로 판정이 아래 −1.4 ~ 위 +2.6이 됐다
  (전에는 ±2.0). **비행 적을 실제로 때려보고** 더 올릴지 판단할 것. 판정과 범위 표시 사각형이 같은 중심을 쓰는 건 값으로 대조했다.
- 🪜 **맵 선택 화면의 승천 화살표** — 해안가에서 승천2 이상으로 못 넘어가는지. 로직은 대조군까지 검증했고 **화면은 미확인.**
- ⚙️ **인게임 ESC → "설정"** — 타이틀 진입은 플레이모드로 열어 확인했으나(스크린샷), 인게임 경로는 미확인.

### 🔎 플레이 확인 대기 (세션30 후반 — 맵 해금)
- 🔒 **맵 잠금이 화면에서 제대로 보이는지** — 잠긴 카드는 **실루엣 + "🔒 해안가 승천2 클리어"** 문구로 뜨고 버튼이 비활성이다.
  판정은 대조군까지 검증했다(기록없음·승천1=잠김 / 승천2=해금, 세이브 원복 확인). **남은 건 카드가 실제로 그렇게 보이는지뿐.**
  ⚠️ 카드 이름 자리에 조건 문구를 넣었더니 **길어서 잘릴 수 있다** — 잘리면 카드에 별도 텍스트 자식을 두는 쪽으로 바꿀 것.
- 🗺️ 현재 세이브 기준: 농장·해안가 **열림**, 광활한 밭 **잠김**(해안가 승천2 필요). 광활한 밭은 배경 미제작이라 잠긴 게 오히려 맞다.

### 🔎 아직 감을 더 봐야 하는 값 (세션28~30, 동작 자체는 확인됨)
- 🩸 **휘두르기 흡혈** — 맞은 적 1마리당 초과체력 2(`SwingLifestealPerHit`). 광역이라 5마리면 10 — **과할 수 있다.**
- ⚠️ **넉백의 "밀리는 동안 전진·박치기·돌진 정지" 분기를 지우면 덜덜 떨림이 돌아온다** — 박치기 돌진(`AdvanceLunge`)이 매 프레임 x를 `holdBaseX + sin곡선`으로 **덮어쓰기** 때문. 구조적 함정이라 남겨 둔다.
- 🧩 **갈림길 UI 세로 2장은 보물상자 "레벨업 vs 진화"에만** 적용된다(340×540, x=±185). **일반 레벨업(3택)은 씬 원본 그대로** — 헷갈리기 쉬운 지점.

### 🔎 플레이 확인 대기 (세션26 = 파인애플, 남은 것)
- 🎨 **프레임 간 몸통 흔들림** — 대기는 캔버스 48px, 공격은 72px이라 **중앙 pivot 기준으로 몸통이 좌우로 움직인다.** 의도한 무게감이면 두고, 아니면 각 PNG `.meta`의 `spritePivot`만으로 맞출 수 있다. **사용자가 "다시 확인해봐야겠다"고 한 항목.**
- ⚔️ **휘두르기 위아래 판정** `SwingHalfHeight` 2.0 — 비행 적이 실제로 닿는지. (앞쪽 사거리 4.8은 "괜찮다" 확인 완료)
- 💥 **타격 이펙트는 별도 VFX가 아니라 4번째 프레임 그림에 그려져 있다**(사용자 아트). Vefects 파티클을 겹쳐 봤으나 도트를 가려서 뺐다 — 연출을 더하려면 코드가 아니라 **그림 쪽**을 먼저 볼 것.
  ⚠️ 그림상 충격파는 pivot에서 왼쪽 **3.5유닛**까지인데 판정은 **4.8**이다. 판정이 관대한 쪽이며 **의도된 것**. 그림과 맞추려면 pivot에서 왼쪽 **102px**까지 그릴 것.
- 🔒 **해금 포스터** — **스킬트리 창**(Btn_업그레이드) 우측 하단. 지금은 배경이 **단색 패널**이라 원티드 포스터 그림이 들어가야 한다.

### 🔎 플레이 확인 대기 (세션25 변경분)
- **농장 난이도** — 게릴라 체감은 사용자 확인 완료("재밌네"). 물량·비행 비율은 더 볼 것.
- **배경 4컷 애니 400ms** — 속도가 빠르면 `Map_BlueberryField.backgroundFrameSeconds`만 고치면 된다.

### ✅ 세션28에 사용자가 확인해 준 것 (다시 묻지 말 것)
- **농장 레인 높이** `cameraYLift=1.15` — "딱 맞다"
- **보물상자 진화 빈도** — "괜찮다"
- **휘두르기 앞쪽 사거리** `SwingReach=4.8` — "괜찮다"
- **미리보기에 같은 아이콘이 홀로 두 번 뜨는 것**(스나이핑·호밍·산탄·되감기는 연계 조건 자체가 없음) — "상관없다", 그대로 둔다

### 🔒 되돌리지 말 것 (의도적 결정)

- **해안가 `StageTable_Coast`의 UFO 판(13·22)만 2배** — 나머지 판은 물량 ×3인데 여기만 2배다. UFO 1대가 12~15마리를 쏟는데 쿼터엔 1로 잡혀서, 3배면 실제 700마리가 된다.
- **해안가 `ambushCount`는 안 올린다** — 중간 소환 1회당 예고 2.5초 동안 정문 스폰이 멈춘다. 16판은 이미 예고 대기(20초)가 실제 스폰 시간(11.7초)보다 길다. 올리면 난이도가 아니라 **늘어짐**이 는다. 물량은 `spawnCount`/`burstSize`로.
- **`extendedHpGrowth = 1.28`** — 사용자가 의도적으로 고른 가파른 값. 못 깨면 1.08 방향으로 내리는 것이지 버그가 아니다.
- **최종 판에만 상자·진화 아이템이 없는 것** — 의도된 동작(깨면 게임이 끝나 보상을 쓸 데가 없다). **중간 보스 판(15·20)에는 둘 다 나온다.**
- **진화 설명에 수치가 없는 것** — BTD6 파라곤식으로 일부러 뺐다. 감이 아예 안 잡히면 *제목만* 기능어로 되돌리는 절충안이 있다.
- **게릴라를 늘릴 땐 `ambushCount`가 아니라 `ambushSquads`** — `ambushCount`는 **예고 횟수**라, 올리면 1회당 2.5초씩 정문 스폰이 멈춰 난이도가 아니라 **늘어짐**이 는다(세션25 검산에서 구 농장 표 25판 중 **14판이 이 위반**이었다). `ambushSquads`는 예고 한 번에 여러 부대를 동시에 쏟아 예고 시간을 안 늘리고 물량만 올린다.
  → 불변식: **`ambushCount × 2.5초` < 정문 스폰 시간**. 검산 스크립트를 다시 쓰려면 세션25 일기 참고.
- **충격파는 방패 블루베리에 막힌다**(세션30, 사용자 요청). 코드 주석과 이 문서에 예전엔 "관통 무제한 — 방패도 못 막는다"가 **의도된 설계로** 적혀 있었으니 되돌리지 말 것. 충격파만 방패를 무시하면 방패 스테이지가 통째로 무의미해진다.
- **보스는 둔화·기절·넉백을 절반만 받는다**(`Enemy.BossCrowdControlScale` 0.5, 세션30). 무한 스톨링 때문이다 — 세기만 깎고 지속시간은 그대로 둔 것도 의도(기절이 "느려짐"으로 바뀌어야 보스가 전진한다).
- **`evolutionItemDrops`는 이제 "확정 엘리트 수"** — 진화가 보물상자로 통합돼 아이템은 안 떨군다. 필드명만 낡았다. 0으로 만들면 **벽 스테이지 엘리트까지 사라진다.**
- **승천 해금은 맵별이다**(세션31, 사용자 지적). 상한 = **그 맵에서 깬 승천 + 1**(`MapClearSave` 기준). 예전엔 전역 `AscensionSave.Unlocked`를 모든 맵에 그대로 써서
  **한 번도 안 깬 해안가가 승천3까지 열려 있었다.** 앞 맵에서 올린 승천이 새 맵에 넘어오면 맵을 넘어갈 때 난이도가 안 떨어진다.
  → 전역 `AscensionSave`는 **표시·치트용으로만** 남았다(치트창 "해금 최대로"는 선택 화면에 영향 없음 — 승천을 열려면 맵 섹션의 "승천+1 클리어 기록").
  ⚠️ `MapSelectUI.Select()`의 `RefreshAscension()` 호출을 지우지 말 것 — 맵을 바꿔도 화살표가 옛 범위로 남는다.
- **연계 조건은 `RoutePath`와 무관하다**(세션33). 조건은 `EvolutionRoutes.RoutePrereq` 표가 단독 소유하고, `RoutePath`는 **진화 효과 매핑 전용**으로 남았다.
  둘을 다시 묶으면 NEW 조건표의 **액티브 2개·패시브 2개·반대 배치**를 표현할 수 없다.
- **`PassiveSkillId.Refresh`를 enum에서 지우지 말 것**(세션33 폐지). `Passive_*` 에셋이 enum을 **정수로 직렬화**해서 지우면 방어(5)·가속(6)이 밀리고,
  `passiveIcons` 배열도 `(int)id`로 직접 인덱싱해서 아이콘까지 어긋난다. 폐지는 **레벨업 후보 배열에서 빼는 것**으로 끝난다.
- **진화 효과 명세는 "불릿=1차 / 2차=강화판"이다**(세션34, 사용자 결정). 명세가 없는 **16루트는 기존 유지** — 이번에 안 건드렸다.
- 🗑 **명세를 따르다 호출부가 사라진 기능 4개** — 되살리기 쉽게 코드·프리팹·VFX 배선을 **전부 남겨 뒀다.** 지우지 말 것:
  **오브 제단**(`SpawnOrbAltar`+`OrbAltar.cs`) · **초과 피해 연쇄**(`OverkillChain`+`overkillSplashVfxPrefab`) · **체인 라이트닝**(`LightningStorm.ChainEnabled`) ·
  **회오리 최고체력 타겟팅**(`Whirlwind.TargetHighestHealth` — 이건 켜는 static 필드까지 지웠다. 프로퍼티만 남음).
  각 함수 주석에 "호출하는 곳이 없다 + 되살리는 법"이 적혀 있다. ⚠️ 오브 제단을 되살리면 `TryUseSkill`의 `OrbAltarCooldown` 분기도 같이 되돌릴 것.
- **진화 아이콘 배열은 `LevelUpUI`가 단독 소유한다**(세션35). HUD·`EvolutionTreeUI`는 `LevelUpUI.Instance`로 빌려 간다 —
  원본 아이콘처럼 3곳에 각자 배선하면 **32칸짜리 배열이 세 벌**이 되어 서로 어긋난다(`PauseMenu`가 이미 쓰던 패턴을 따랐다).
  그림이 비면 전부 원본 아이콘으로 떨어지므로 **배선이 안 된 상태에서도 게임은 정상 동작**한다.
- **아이콘 파일명의 R1/R2는 티어가 아니라 루트다**(세션35). `R1`=루트0, `R2`=루트1이고 **1차·2차 진화는 같은 그림**을 쓴다.
  코드·문서 표기(R0/R1)와 파일명(R1/R2)이 **한 칸 어긋나 있다** — 헷갈리면 `EvolutionIconWiring`의 `PathFor()`가 정답이다.
  ⚠️ 단 **`Effect_Arrow*`(투사체)는 티어다** — R1=1차(암살 사격) / R2=2차(처형 사격). 사용자가 그렇게 정했다(세션35).
- **힘 R1은 "Q키에 할당된 스킬" 강화다**(세션35, 노션 명세). 예전의 "화살 쏘기 고정"으로 되돌리지 말 것 —
  **Q = 가장 먼저 얻은 스킬**이라 캐릭터·획득 순서마다 대상이 달라지는 게 의도다.
- **새로 쓰는 진화 분기는 `>= 2`(1차) / `>= 3`(2차)로 읽는다.** 1차 진화가 `PathTier`를 곧바로 **2**로 만들어서 `>= 1`은 1차만 해도 참이 된다.
- **휘두르기 판정 중심을 위로 올린 것**(`SwingCenterYOffset` 0.6, 세션31). `skill.Scale`·`reachMult`를 **일부러 안 곱한다** — 곱하면 크기를 키울수록 판정이 통째로 떠올라 지상 적을 놓친다.
- 🔤 **픽셀로보로보체는 "한글·영문만" 지원한다**(폰트 세션). cmap 실측: ASCII 95/95 · 한글 11,172/11,172 · **Latin-1 2.1% · 화살표/도형 없음**.
  **`◀ ▶ → ← ↑ × —`가 전부 없다** — 이 기호를 쓰는 텍스트 8개를 일부러 Pretendard로 남겼다. **픽셀로보로보체로 되돌리면 두부(□)가 된다.**
  ⚠️ **런타임에 코드가 조립하는 문자열은 검사 못 했다.** 새로 `"×2"`·`"→"` 같은 걸 넣으면 그 자리만 깨진다.
- ⚠️ **TMP의 글리프 보유 조회 API를 믿지 말 것**(폰트 세션에서 셋 다 틀렸다).
  `HasCharacters()`는 **Dynamic 폰트에선 아직 안 구운 글자를 "없음"으로** 답하고, `TryAddCharacters()`는 에디트모드에서 조용히 실패하며(Pretendard에 'T'가 없다고 나왔다),
  `Font.HasCharacter()`는 **없는 글자에도 True**를 준다(▶조차 있다고 답했다).
  → **정답은 TTF/OTF의 cmap 테이블을 직접 파싱하는 것**뿐이다. 그리고 이런 검사기를 새로 쓰면 **정답을 아는 입력으로 대조군부터 돌릴 것** — 이 셋 중 둘이 그렇게 잡혔다.
- **데미지 숫자의 페이드는 `text.color`가 아니라 그라데이션 네 꼭짓점의 알파**다(폰트 세션). 정점 그라데이션을 켜면 `text.color`로는 알파가 안 먹는다.
  `VertexGradient`가 **구조체라 대입 시 복사**되는 덕에 풀링 재사용 시 원본이 안 망가진다 — 클래스로 바꾸면 두 번째 스폰부터 투명해진다.

---

## ⚠️ 미해결 이슈 / 주의

- 🔴 **맵 3종이 서로 다른 단계에 있다.** 밸런스를 고치기 전에 **어느 맵인지 먼저 확인할 것.**

  | 맵 | fieldScale | 스테이지 테이블 | 배경 | cameraYLift | 콩콩이·서핑 |
  |---|---|---|---|---|---|
  | 블루베리 밭 | **1.25** | **`StageTable_Farm`** | ✅ 400×225 **4컷 애니(400ms)** | **1.15** | ✖ |
  | 해안가 | 1.5 | `StageTable_Coast` | ✅ 480×270 | 2.2 | ✅ |
  | 광활한 밭 | **1.8** | `StageTable`(구 농장 표를 그대로 물려받음) | 🔴 **576×324 미제작** | 🔴 0(미설정) | ✖ |

  → 배율은 **1.25 → 1.5 → 1.8 등비(공비 1.2)**. 셋째 맵이 원래 계획 2.25에서 1.8로 내려왔다(세션25 확정).
  → **광활한 밭은 배경 그림(576×324, PPU 18)이 나와야 나머지가 따라온다.** 배경 없이 실행하면 가장자리가 빈다(버그 아님).
  → 새 배경 PNG는 **임포트 기본값(PPU100·Bilinear·Multiple·압축)으로 들어온다.** 반드시 PPU 18·Point·Single·중앙 pivot·압축 해제로 고칠 것(세션25에 실제로 걸렸다).
- 🔴 **맵별 클리어 기록(`MapClearSave`)은 세션26에 새로 생겼다 — 과거 클리어는 소급되지 않는다.** 기존 세이브의 `ascension.unlocked`는 "어느 맵에서 깼는지"가 없어서 옮겨올 수가 없다. **이미 게임을 깬 사람도 농장을 한 판 더 깨야 파인애플이 열린다.** 테스트로 열어보려면 `MapClearSave.RecordClear("Map_BlueberryField", 1)`.
  → 이건 **카드 ②(맵 해금 시점 옮기기)의 선행 구조와 같은 것**이다. 카드 ②는 이제 `AscensionSave`를 맵별로 쪼개는 대신 `MapClearSave`를 쓰면 된다.
- ⚠️ **파인애플 해금의 정수 조건(300)은 사실상 이미 충족돼 있다** — 개발 세이브 기준 누적 2828. 즉 실질 조건은 "농장 승천1 클리어" 하나뿐이다. 두 조건을 다 의미 있게 하려면 정수 임계값을 올릴 것.
- ⚠️ **13·22스테이지(UFO 벽)는 투하물이 물량 쿼터에 안 잡힌다** — UFO 1대당 12~15마리를 쏟는데 `spawnCount`엔 UFO 수만 잡힌다. 게임에서 제일 무거운 판. 프레임이 떨어지면 **여기부터 의심**(해안가 일반 판에도 `ufoChance` 0.08~0.11이 남아 있다).
- ⚠️ **판 전환/게임오버 시 비행 중인 경험치 보석의 XP는 유실**(timeScale 0이면 멈춘 채 씬이 끝남). 무해하다고 판단했으나 신경 쓰이면 도착 강제 처리.
- ⚠️ **ESC의 "타이틀로 돌아가기"는 확인창 없이 한 번 클릭**에 게임오버 흐름을 탄다. 오발이 신경 쓰이면 2단계 확인.
- ⚠️ **Windows 빌드 세이브는 파일이 아니라 레지스트리** — `HKCU\Software\DefaultCompany\BlueberryDefense`. 삭제: `reg delete "HKCU\Software\DefaultCompany\BlueberryDefense" /f`(게임 종료 상태에서).
- **에디터 인스펙터 예외(무해)**: 플레이 진입 시 `ObjectPreview.DrawPreview ... Image destroyed`.
- **노션 토큰 노출**(기존): 폐기·재발급 권장.
- ⚠️ **VFX 프리팹 AudioSource의 볼륨은 스폰 시점에만 반영된다**(세션37). "효과음" 슬라이더에 안 걸리던 문제는 해결됐지만
  (`ObjectPool.Spawn`이 `SourceBaseVolumes[i] × VolumeSettings.Sfx`로 곱한다), **이미 재생 중인 루프 사운드는 슬라이더를 움직여도 안 바뀐다** — 다음 스폰부터 반영.
  대부분 1~2초 원샷이라 실사용에선 곧 따라온다. 거슬리면 `VolumeSettings`가 활성 소스를 등록받는 구조로 바꿔야 한다.

> ✅ **되살리지 말 것 (틀린 것으로 확인된 옛 메모)**
> - "시작 캐릭터 풀을 줄여야 루프가 성립" — **틀림.** 스킬 해금 게이팅은 스킬트리 해금 노드가 이미 담당한다(`MetaBonuses.SkillUnlockedForRun`). `CharacterDefinition.allowedActivePool`은 캐릭터 개성용이고 둘은 독립.
> - 노션 "다음으로 할일"의 미완료 3건(**고승천 정수 배율 / 고승천 스테이지 길게 / 거대 회오리 시 미니 회오리 안 나옴**) — **전부 이미 구현됨.** 순서대로 `AscensionTable.essenceMult`(1.25/1.5) · `AscensionTable.finalStage`(15/20/25) · `PlayerSkills.FireWhirlwind`(두 path 독립 소환, 세션14 수정). 노션 체크박스가 낡은 것.
> - 세션13~19의 "실플레이 확인 대기" 항목 — 전부 확인 완료.

---

## 다음 할 일 — 칸반보드 (2026-08-13 세션39 종료 시점: `시작 전` **0** / `진행 중` **7** / `완료` 11)

> 🔄 **세션39에 「번역 시스템」이 `시작 전` → `진행 중`** — 골격이 섰고 문자열 외부화가 남았다(위 🌐 절 + 카드 본문).
> **`시작 전` 칸이 비었다** — 다음 세션은 `진행 중` 7장 중에서 고르거나 새 카드를 만들어야 한다.
>
> 진행 중 7장: 번역 시스템 · 사운드 · 스킬 리소스 확충 · 아트 리소스 제작 · 뉴캐릭터 공격 이펙트 ·
> 진화 파워 밸런싱 · 폴리싱 할일 리스트업.
>
> 「폴리싱 할일 리스트업」에 남은 2항목(**UI 에셋 · 저장/불러오기**)은 일부러 안 쪼갰다 — 앞은 아트 카드와 겹치고 뒤는 판단만 하면 되는 작은 건이다.
>
> **이번 주(8/10~8/16) 범위는 소리 · 번역 · 스킬 이펙트**로 사용자가 정했다. 소리는 코드 쪽이 끝났고(음원 선정 대기),
> 번역은 골격이 끝났고, 스킬 이펙트는 배선이 끝났다(망치 컨트롤러 2개만 미배선).

> ✅ **세션36에 카드 2장이 완료로 갔다** — **"플테후 제안사항"**(3건 전부 구현, 아래 ②-C) ·
> **"진화 효과 재설계 구현"**(세션34에 이미 끝났는데 카드만 열려 있었다 — `brief`의 칸반↔코드 대조로 발견).
> 🆕 **새 카드 「아트 리소스 제작」**(`시작 전`) — 그려야 할 아트를 **전부 한 장에 모았다**. 아래 ②-D.

> ✅ **세션30에 카드 4장이 완료로 갔다** — 보물상자 진화 · 조합 미리보기 · 새 캐릭터 · 맵 해금.
> **콘텐츠 3건(새 캐릭터·농장맵·맵 해금)이 이로써 전부 끝났다** → 이제부터 **콘텐츠 추가·변경은 안 하는 것이 기본값**이고, 남은 건 폴리싱이다.
> 완료 카드에서 살아남은 후속 작업은 아래 "남은 청소거리"와 "사용자 아트 대기"로 옮겨 뒀다 — 카드가 닫혔다고 증발하지 않게.

> 🧊 **콘텐츠 변경 3건(새 캐릭터·농장맵 확장·맵 해금)은 POC 플레이 피드백 반영 — 마지막 예외다.**
> 결정된 사항이니 다시 묻지 말 것. **다만 이 3건이 끝나면 콘텐츠 추가·변경은 다시 "안 하는 것"이 기본값**이고,
> 이후 콘텐츠는 **칸반 카드로 올라온 것만** 한다. 먼저 제안하지 말 것. (`kanban` 스킬의 콘텐츠 동결 원칙)

**① 진화 파워 밸런싱 (나머지 스킬)** — 🔍 **세션30에 전수조사 완료 (칸반 `진행 중`). 다음 세션에 방향을 논의하기로 함.**
- 📄 **조사 결과 문서(30개 진화 전부)**: https://claude.ai/code/artifact/a48915cc-1b4d-403f-bdfb-95d89754d645
  등급 근거는 **곱연산 축 개수 + 적 수 비례 여부**. 🔴8 / 🟢10 / 🔵12(패시브 10 포함).
  ⚠️ 그 집계는 원문 내부에서 개별 판정과 안 맞는다 — **실제로 세면 🔴9 / 🟢10 / 🔵11**(세션32 확인).
- 📝 **노션 피드백 수집본**(스킬 단위로 재배열 + 피드백 칸 14개): https://app.notion.com/p/30-3b46394cd98381d690bae7f60d454afc
- 📊 **노션 스킬 데이터 시트**(전 스킬 기본값·레벨업 커브·만렙 누적·진화·연계 다이어그램): https://app.notion.com/p/3b46394cd98381dc9eafedb69268a47e
  세션32에 코드·에셋에서 기계적으로 생성. **밸런스를 만지기 전에 여기부터 볼 것.**
- **구조적 원인 3가지**: ① 진화가 레벨업 성장값(`ExtraProjectiles`·`ExtraPierce`) 위에 **곱해진다**
  ② 화면 전체 광역(독수리)에 "적마다 소환"을 붙여 **적 수가 배수**가 된다 ③ 대가(쿨 증가)가 붙은 루트와 공짜 루트가 섞여 있다.
- **손봐야 할 5개 중 3개는 세션34 재설계로 통째로 사라졌다** — 매사냥→화살비 · 제압 산탄→전방 집중 · 몰살 저격→주변 독수리.
  ⚠️ **남은 2개는 명세가 없어 그대로다**: **회오리 폭격**(독수리R1 — 적 수 × 회오리, 조사에서 가장 과한 진화) · **시간 붕괴**(되감기R1 — 전역 쿨 절반).
- ⚠️ **`MiniWhirlwindDamageBonus`는 여전히 static 전역** — 회오리R0가 올리는 값이 독수리R1의 회오리 폭격까지 강화한다(스킬 간 증폭). 재설계 후에도 유효한 경고다.
- ✅ **연계 조건 부재는 세션33, 진화 효과 재설계는 세션34에 끝났다.** 이 카드에 남은 건 **세기 조정**(위 "플레이 확인 대기 세션34")과 위 2개다.
- ⚠️ **이제 밸런싱 기준선이 바뀌었다** — 조사 문서(30종 등급표)와 노션 스킬 데이터 시트의 **진화 항목은 16개가 낡았다.** 수치를 만지기 전에 코드부터 볼 것.

🔴 **조사 중 발견한 실제 버그 — `화살 쏘기`는 UFO를 영원히 못 맞힌다** *(세션34에도 안 건드렸다 — 사용자 결정 대기)*
- ⚠️ **세션34로 더 이상해졌다**: 화살R0가 **관통 무한**이 됐는데 UFO만 못 뚫는다. 화살R1(화살비)은 위에서 떨어지는 그림이라 `CanHitFlying=true`로 줬으니 **R1이 사실상 유일한 우회로**다.
- 대공 타격(`Projectile.CanHitFlying`)은 `PathTier[0]`에서만 켜지는데, **화살의 path0은 진화로 도달할 수 없다**(루트가 path1·path2만 씀).
- 오브는 스킬트리 노드(`OrbCanHitFlying`)로 우회되지만 **화살엔 우회로가 없다.** UFO 판(13·22)이 가장 무거운 판인데 딸기 기본공격이 안 통한다.
- → 대공을 스킬트리 노드로 빼거나, 화살 루트 중 하나에 넣거나, `RoutePath`를 바꾸는 세 갈래. **사용자 결정 필요.**
- 참고: 스킬마다 **path 하나가 통째로 도달 불가**다(의도된 설계, `EvolutionRoutes` 주석에 이유 있음). 그 안에 잠든 효과 목록은 위 문서의 표 참고
  — 호밍 "미사일 ×5" · 독수리 "적 수 반비례 450%" 등이 **이름만 남고 안 열린다**(융단 폭격이 대표).

**②-A 진화 아이콘 배선** — ✅ **완료**(세션35). 남은 건 위 "플레이 확인 대기 세션35"의 화면 확인뿐.
- 아이콘을 **새로 그리거나 고치면** `Window > Blueberry Defense > 진화 아이콘 배선`을 다시 돌리면 된다
  ([EvolutionIconWiring.cs](Assets/Editor/EvolutionIconWiring.cs) — 임포트 정규화 + 배열 재배정을 한 번에).
- ⚠️ **파일명이 곧 배선이다** — `Icon_{이름}R{1,2}`. 이름 조각은 enum과 다른 게 있다(BasicAttack→`Arrow`, Shotgun→`Scatter`, Strength→`Power`, Assassinate→`Assasinate`, Defense→`Defence`).
  도구 안의 `ActiveFileNames`/`PassiveFileNames` 표가 그 대응이고, **새 스킬·패시브를 만들면 여기에도 줄을 추가**해야 한다.
- ⚠️ **새 PNG는 임포트 기본값(PPU100·Bilinear·Multiple·압축)으로 들어온다.** 도구가 앞 셋은 고치지만 **Multiple 자동 슬라이스는 안 건드린다** —
  아이콘은 조각이 하나뿐이라 문제가 없었지만 **`Effect_Arrow*`는 실제로 2조각으로 쪼개져 있었다.** 이펙트 그림은 Single인지 먼저 볼 것.

**②-B 스킬 리소스 확충** — 🔍 **칸반 `진행 중`**(세션35에 확인한 새 카드). 체크리스트 5개 중 1개 완료.
- ✅ 스킬 원본/진화형 아이콘 모두 채우기 — 그림·배선 둘 다 끝났다(위 ②-A).
- ⬜ **스킬 진화형 이펙트 계획 / 제작(그림) / 넣기** — 화살 R1·R2가 **첫 사례**다. 나머지 진화 30종은 이펙트가 없다.
  넣는 자리는 스킬마다 다르다(투사체 스프라이트 교체 · VFX 프리팹 · 애니메이션). 화살은 `PlayerSkills.ApplyEvolvedArrowSprite`가 본보기.
- ⬜ 피드백 후 수정

**②-C 플테후 제안사항** — ✅ **3건 전부 구현**(세션36). 남은 건 화면 확인뿐(아래 "플레이 확인 대기 세션36").
- 🎯 **호밍 미사일 타겟 분산** — `HomingMissile.TargetRank`(발사 순서 0,1,2…)를 `FireHoming`이 부여하고,
  `AcquireTarget()`이 **비행 우선 → 가까운 순으로 정렬해 rank번째**를 고른다. 발사각에도 ±15도 난수(`spreadJitter`).
  적이 미사일보다 적으면 순번이 한 바퀴 돌아 겹치는데, **그건 그대로 뒀다**(적이 적을 땐 몰리는 게 맞다).
- 🌲 **`swing_StartLev` 노드 신설** — 파인애플 기본무기(휘두르기) 3레벨 시작. `arrow_StartLev`와 **완전 대칭**으로 넣었다
  (type=SkillEnhance · tier 2 · prereq `atk_1` · 비용 20정수). ⚠️ **위치·선행 조건은 임의로 정한 것** — 트리 그래프 설계는 사용자 몫이니
  `Blueberry Defense > Skill Tree Editor`에서 옮길 것. 효과는 `Meta.Compute`의 `case "swing_StartLev"` → `SetSkillStartLevel(Swing, 3)`.
- ⌨️ **보물 화면 키보드 스킵** — Space·Enter로 넘어간다(`LevelUpUI.TreasureSkipKeyPressed`). **ESC는 일부러 뺐다**(일시정지 메뉴가 먹는다).
  ⚠️ 씬의 안내 문구는 아직 **"클릭하여 계속"** 이다(`treasureContinueText`) — 문구 수정은 씬 작업이라 안 했다.

**②-D 아트 리소스 제작** — 🆕 **칸반 `시작 전`**(세션36 신규). https://app.notion.com/p/3b56394cd9838141b5bef3ce130ec51c
- **그려야 할 아트를 전부 한 장에 모은 카드.** 진화 이펙트·망치·UI 프레임처럼 다른 카드에도 있는 것까지 **일부러 중복해 실었다**(사용자 요청 — 한눈에 보려고).
- 우선순위: 🔴 임시본 교체 3(경험치 보석·적 그림자·충격파) → 🟠 진화 이펙트 → 🟡 캐릭터·화면 4 → 🔵 UI 에셋 4.
- ⏸ **보류 2건**: **광활한 밭 배경**(버티컬 슬라이스 단계라 세 번째 맵은 비워둔다) · **2차 진화 전용 그림 전부**
  (2차는 당분간 1차 그림을 그대로 쓴다. 2차에서만 새로 필요한 건 빙결 오브·파열 이펙트·재앙의 눈 3개).
- ✅ **정수·체력 아이콘은 지금 것으로 확정** — 다시 그리지 않는다.
- ⚠️ **되감기 R0·R1은 그림이 없는 게 아니라 연출 자체가 없다.** 그리기 전에 "뭐가 보여야 하나"부터 정할 것.

**② 뉴캐릭터 공격 이펙트** — 🎨 **아트 대기** (칸반 `시작 전`, 세션28에 사용자가 올린 카드)

**③ 폴리싱 할일 리스트업** — 🔍 **칸반 `진행 중`**(세션31에 2항목 완료, 카드 본문에 체크리스트를 적어 뒀다)

> 🆕 **세션36: 이 카드 본문에 UI 폴리싱 상세 리스트가 통째로 새로 붙어 있다**(사용자가 세션 사이에 작성).
> 아래 6항목과 **별개의 목록**이고 분량이 훨씬 크다 — 메인화면 · 컬렉션 · 설정 · 업그레이드 · 맵 선택 · 전투 UI(레벨업/진화/체력바/경험치바/대미지 표기/적/아이템/승리·패배/스킬),
> 스크린샷·아이콘 에셋 링크·레퍼런스 게임까지 들어 있다. **여기 옮겨 적지 않는다** — 원본이 노션이고 분량이 한 화면을 넘는다.
> 착수할 땐 카드 본문을 직접 열 것: `API-retrieve-page-markdown`(page_id `3b06394c-d983-804e-8f97-ffe9b948d94e`).
> 세션36에 그중 코드만으로 되는 4건을 처리했다(노드 4색 테두리 · 레벨업 중 스킬 눌림 · 픽업 포물선 · 아래 ②-C 3건).
1. ✅ ~~**사운드**~~ — 세션37에 **별도 카드 「사운드」로 쪼개냈다**(`진행 중`). 코드 배선은 끝났고 음원 선정만 남았다(위 사운드 항목).
2. 🎨 **UI 에셋** — 사거나 그리거나. 트리 UI 레이아웃 손보기 포함. **설정 패널도 단색 프리미티브**라 스프라이트가 나오면 `OptionsMenu`의 색 상수만 갈면 된다.
3. ✅ ~~안 넣은 이펙트 / 쥬씨한 애니메이션~~ — 세션31 완료(손맛 확인만 대기).
4. ✅ ~~설정 패널~~ — 세션31 완료(인게임 진입 확인만 대기).
5. 💾 **저장·불러오기** — 현재 `PlayerPrefs`(Windows=레지스트리). 정식 세이브 필요 여부 판단.
6. ✅ ~~**번역 시스템**~~ — 별도 카드 「번역 시스템」(`진행 중`). **세션39에 골격 완성** — 위 🌐 절 참고.

→ **남은 2항목(UI 에셋 · 저장/불러오기)은 일부러 안 쪼갰다** — 앞은 「아트 리소스 제작」 카드와 겹치고, 뒤는 판단만 하면 되는 작은 건이다.

**④ 승천 해금을 맵별로 분리** — ✅ **완료**(세션31 신규 카드). 화면 확인만 위 "플레이 확인 대기"에 남아 있다.

**⑤ 패시브 "방어" 추가** — ✅ **완료**(세션34에 진화 2종까지 구현). 남은 건 위 "플레이 확인 대기"뿐.
- 효과: **받는 피해 비율 감소**. 획득 6% / 레벨당 +4%p → 만렙 **42%**. `Passive_Defense.asset`(생성·배선 완료, `progressions[6]`).
  적용 지점은 [PlayerHealth.cs](Assets/Scripts/PlayerHealth.cs)의 `TakeDamage` 맨 앞 — **보호막·체력보다 먼저** 깎고 **최소 1은 남긴다**.
- ⚠️ **초반 6%가 잔몹에게 효과 0인 것은 의도된 수용이다**(세션32, 사용자 "상관없음"). 피해가 정수라 `3 × 0.94 = 2.82 → 반올림 3`으로 뭉개진다 —
  잔몹 3이 2가 되려면 **17% 이상**이라 실질적으로 **레벨 3~4쯤부터 잔몹에 듣고, 그전엔 보스·큰 피해에만** 유효하다. **버그로 오해해 고치지 말 것.**
  (기본값 상향·소수점 이월 두 대안을 검토했고 둘 다 안 쓰기로 했다.)
- ✅ **아이콘·조건·ESC 요약은 세션33, 진화 2종은 세션34에 끝났다.** 진화 조건은 **R0=휘두르기 / R1=건강**(NEW 조건표).
- ⚠️ **`HasEvolutionDesign` 가드는 사라졌다**(세션34) — 방어·가속 진화가 생겨 뺄 이유가 없어졌다.
  **새 패시브를 또 만들면 진화까지 같이 만들 것.** 안 그러면 이름도 효과도 그대로인 **빈 진화**가 뜬다
  (`IsRouteUnlocked`는 연계 조건이 null이면 "조건 없음=열림"으로 본다).
- ⚠️ 패시브 후보는 [LevelUpUI.cs:526](Assets/Scripts/LevelUpUI.cs#L526)에 **하드코딩된 배열**이다 — 새 패시브를 만들면 여기 안 넣으면 게임에 안 뜬다.

**⑥ 패시브 "가속" + 진화 조건표 개편** — ✅ **완료**(세션33 신규 카드). 남은 건 위 "플레이 확인 대기"뿐.
- 효과: **전 스킬 쿨타임 감소.** 획득 5% / 레벨당 +2.5%p → 만렙 **27.5%**. `Passive_Accel.asset`(생성·배선 완료, `progressions[7]`).
  적용 지점은 [PlayerSkills.cs](Assets/Scripts/PlayerSkills.cs)의 쿨 계산 — 스킬트리 `MetaBonuses.CooldownMult`와 **곱해지고** 하한은 GCD 0.4초.
- 🔴 **`refresh_bonus` 스킬트리 노드는 현행 `MainSkillTree.asset`(노드 34개)에 애초에 없다** — `MainSkillTree_Backup.asset`에만 있다.
  코드는 가속 쿨감으로 재배선해 뒀으니 **트리에 노드를 다시 넣으면 바로 동작**한다. 값은 확률 5%p/레벨을 쿨감에 그대로 쓰면 `maxLevel 5 × 5 = 25%p`라 과해서 **2%p/레벨로 낮췄다.**
  노드 텍스트("리프레쉬 I")는 에셋 쪽이라 **트리 에디터에서** 고쳐야 한다.
- ⚠️ **리프레쉬 진화 4종(재생/생명/가속/무한 순환)은 도달 불가가 됐다.** 코드는 살아 있으니 가속 진화에 옮겨 쓸 수 있다.

### 칸반에 없지만 결정이 필요한 것
- 📊 **주간 "일기 검수" 리포트를 `/schedule`로 돌릴지** (세션29 논의, 미결). 클라우드 루틴이라 PC를 꺼도 돌지만 **알림이 없어 웹에 가서 봐야 한다.**
  뽑을 값어치가 있는 건 요약이 아니라 **규칙 효과 검증** — "이 규칙을 넣은 뒤 같은 사고가 또 났나 / 안 났으니 `CLAUDE.md`에서 덜어낼까".
  ⚠️ 일기가 주 4편 속도라 주 1회면 "새 패턴 없음"이 자주 뜬다. **격주·월 1회가 맞을 수 있어 몇 번 돌려보고 정할 것.**
- **공중 유닛 2종 추가**(위아래 지그재그 / 직선 가로지르기) — 넣을지 말지 미결. 넣기로 하면 칸반 카드부터 만들 것. 절차는 세션20 콩콩이가 최신 예시(ARCHITECTURE "새 적 타입" 행 + `isHopper` 한 벌).
- **`Map_Wide20`(광활한 밭)** — 배율은 **1.8로 확정**(세션25). 이제 배경만 나오면 된다: **576×324, PPU 18**. 순서: 배경 임포트(PPU18·Point·Single·중앙pivot·압축해제) → `background` 배정 → `cameraYLift`(농장처럼 길 픽셀을 재서 산출) → 전용 스테이지 테이블(지금은 구 농장 표를 물려받아 게릴라 개편이 안 들어가 있다).

### 남은 청소거리 (급하지 않음)
- 🧹 **`SaveResetButton.cs`가 미사용이 됐다**(세션31) — 세이브 초기화를 설정 패널 안으로 옮기며 `Controllers`에서 컴포넌트를 뗐다.
  되돌리기 쉽게 **파일은 남겨 뒀다.** 지울지 사용자에게 물어볼 것(스스로 지우지 말 것).
- **`OrbCanHitFlying` 스킬트리 노드** — 히트박스 판정 도입 후 **UFO에만** 의미가 있다. 다른 효과로 용도 변경 필요(예: 오브 공전 반경 확대).
- **화살 발사점 `FlyingArrowSpawnRaise = 0.65` 땜빵** — 대공 진화를 "발사각/히트박스 확장"으로 재설계할 때 정리.
- **`EvolutionTierTextTableSO`** — 여전히 (path, tier) 좌표계라 새 루트/티어와 어긋난다. 지금은 설명 폴백이 코드라 무해하지만, 에디터에서 텍스트를 덮어쓸 거면 루트 좌표계로 바꿔야 함.
- **보스 박치기 피해** — `HeadbuttDamageScale`(0.35) 하나로 전 적을 곱해서 보스는 1회 70. 보스만 별도 배율이 필요할 수 있음.
- **`EvolutionPanel` 씬 노드가 아직 3×3** — 진화가 2×2로 바뀌어 런타임에 5칸을 끈다. 정리하려면 SCENE_MAP 참고.

> 🎨 **아트 대기 목록은 세션36에 「아트 리소스 제작」 카드로 통째로 옮겼다**(위 ②-D). 여기서 세지 말 것 — 두 곳에 두면 어긋난다.
> **기능·배선은 전부 끝났고 그림만 임시본**이라, 전용 그림이 나오면 **같은 파일명으로 덮으면 재배선 없이 끝난다**:
> 경험치 보석(`XpGemFlight.gemSprite`) · 적 그림자(`Enemy.BuildShadowSprite`를 로드로 교체) · 충격파(`Effect_SwingShockwave.png`) ·
> 초상화 2종(`Char_*.portrait`) · 원티드 포스터(`Canvas/SkillTreeRoot/UnlockPoster/Panel`의 Image에 sprite가 비어 단색 패널로 뜬다).

---

## 🔧 손잡이 (감이 안 맞을 때 만질 곳)

| 바꾸고 싶은 것 | 어디 |
|---|---|
| 해안가 난이도 | `StageTable_Coast.asset` (체력 → 물량 순. ⚠️ `ambushCount`는 올리지 말 것) |
| 화면 밀도(위쪽 공간) | `paperPlaneChance`·`hopperChance`·`surferChance`. ⚠️ **else-if 사슬이라 앞 확률이 뒤를 갉아먹는다** — `eliteChance`를 올리면 나머지 전부가 같이 줄어든다 |
| 보물상자 후함 | `LevelUpUI.TreasureEscalateChance`(0.45) / `TreasureMaxRolls`(5) / `TreasureRevealInterval`(0.55초) / `treasureIconSize`(130) |
| 웨이브 리듬 | `StageData.burstSize`/`burstRest`. ⚠️ 무리 스테이지에선 `spawnInterval`이 **무리 안의 간격**. 0.03이 실질 하한(스포너가 프레임당 1마리) |
| 콩콩이 도약 | 프리팹 인스펙터 `hopHeight` 3.3 / `hopDuration` 0.62 / `hopGroundPause` 0.12 |
| 서핑 높이·무리감 | `diveSpawnHeightRatio` 0.03~0.14(물결 위) / `diveAimYSpread` 0.75(무리 두께) / `diveBobAmplitude` 0.25(너울) / `speedVariance` 0.15 |
| 맵의 레인 높이 | `MapDefinition.cameraYLift` (해안가 2.2). ⚠️ **서핑 강하 띠와 한 세트** — 리프트를 바꾸면 서핑 높이도 같이 움직인다 |
| 적 접촉 거리 | `BalanceConstants.ContactStopDistance`(2.55). ⚠️ **`HeadbuttLungeDistance`·`AmbushBandMaxX`와 3종 세트** — 하나만 바꾸면 박치기가 허공을 치거나 중간 소환이 대응 불가가 된다 |
| 후반 난이도 | `StageTable.extendedHpGrowth` → `ambushCount` → 적 체력 순 |
| 휘두르기 손맛 | `PlayerSkills.SwingReach`(**4.8** = 판정 **끝점**) / `SwingNearOffset`(**0.7** = 판정 **시작점**, 몸통 앞) / `SwingHalfHeight`(2.0 위아래) / `SwingKnockback`(**2.4**). 피해 **12**·쿨 1.5초는 `Prog_Swing.asset` |
| 휘두르기 범위 시작점 | `SwingNearOffset`(0.7). ⚠️ **일부러 `skill.Scale`을 안 곱한다** — 곱하면 크기를 키울수록 코앞이 비어 근접 캐릭터가 눈앞의 적을 놓친다. 덕분에 크기 진화는 앞쪽으로만 자란다 |
| 보스가 너무 잘/안 밀림 | `Enemy.BossCrowdControlScale`(**0.5**). 둔화·기절은 **세기만** 깎고(지속시간 그대로), 넉백은 거리를 깎는다. 1.0이면 옛 동작(무한 스톨링 가능) |
| 캐릭터 선택 기억 | [CharacterSave.cs](Assets/Scripts/CharacterSave.cs) — `PlayerPrefs["select.character"]`에 **에셋 이름**을 저장. 잠긴 캐릭터가 저장돼 있으면 조용히 첫 캐릭터로 폴백 |
| 맵 해금 조건 | 각 `MapDefinition` 에셋의 `unlockedFromStart`/`requiredClearMap`/`requiredClearAscension`. **테스트로 열려면 치트 창 "맵 해금"** (과거 클리어는 소급 안 됨) |
| 조합 미리보기 배치 | `LevelUpUI.ComboMaxPerLine`(2, 한 줄 최대) / `ComboMaxLines`(4, 넘으면 전체 축소) / `ComboIconSize`(46). **스킬 하나 = 한 줄**이 규칙 |
| 넉백 느낌(전 적 공통) | `Enemy.KnockbackDuration`(**0.25초**, EaseOutCubic이라 초반이 제일 빠르다). ⚠️ 거리는 스킬별 — 휘두르기 2.4 / 충격파 0.35·0.55 |
| 휘두르기 범위 표시 | `PlayerSkills.SwingRangeAlpha`(0.28 진하기) / `SwingRangeFadeDuration`(0.25초) / `SwingRangeSortingOrder`(0 = 배경 -100과 적 100+ 사이) |
| 휘두르기 흡혈 | `PlayerSkills.SwingLifestealPerHit`(**2**/마리, PATH1 1차부터). 광역이라 5마리면 10 — 세다 싶으면 여기부터 |
| 진화 파워 | `PlayerSkills.ApplyPathTierEffect`(영구 스탯) + 각 `Fire*`(실시간 기믹). ⚠️ **크기가 오르는 진화는 대형 오브뿐**이고 나머지는 실시간 계산에 숨어 있다 |
| 파인애플 맷집 | `Char_Pineapple.baseHealth`(**150**, 딸기의 1.5배). 사거리가 짧아 적을 가까이 붙여야 하는 근접 캐릭터라 체력으로 보상한 것 |
| 휘두르기 진화 세기 | `PlayerSkills`의 `Shockwave*` 상수(피해비 0.6/1.0 · 넉백 0.35/0.55 · 출발점 x 1.6 · **높이 y -0.85**) / `SwingStunDuration`(0.5초). 충격파 속도·수명은 프리팹 `Swing_Shockwave`의 `moveSpeed`(16)·`lifetime`(2.5초) |
| 진화 루트 쿨 페널티 | `PlayerSkills.ApplyPathTierEffect` — 쓸어치기 `(Swing,1,2)` **×3** / 충격파 `(Swing,2,2)` **×1.8**. 진화 공통 보너스(피해 1.5·쿨 0.9)가 이미 얹힌 위에 곱해진다 |
| 휘두르기 타격 타이밍 | `PlayerSkills.SwingImpactDelay`(**0.225초** = 충격파가 그려진 4번째 프레임). ⚠️ **`Pinapple_Attack.anim`과 한 세트** — 클립은 5프레임(1~4는 75ms, 5는 **225ms**, 총 0.525초)이라 프레임 타이밍을 바꾸면 이 값도 같이 고칠 것 |
| 휘두르기 모션 속도 | `Pinapple_Attack.anim` 키 시각. ⚠️ Unity는 PPtr 클립 길이를 **마지막 키 + 1/frameRate**로 잡는다 — 마지막 프레임을 길게 유지하려면 같은 스프라이트로 유지 키를 하나 더 박고 frameRate로 끝을 맞출 것(지금은 frameRate 40, 유지 키 0.5s → length 0.525s) |
| 파인애플 해금 난이도 | `Char_Pineapple.requiredEssenceEarned`(300) / `requiredClearMap`+`requiredClearAscension`(블루베리 밭 승천1). **둘 다** 충족해야 열린다 |
| 버튼이 한쪽 끝으로 자람 | `RectTransform.pivot`. 스케일은 pivot 기준이라 끝 피벗이면 한쪽으로만 큰다. 씬 8개는 세션32에 중앙으로 고쳤고, **런타임 생성 버튼은 배치 뒤 `JuicyTuning.CenterPivot(go)`를 불러야** 한다(배치 함수가 pivot을 덮으므로 순서가 중요) |
| UI 버튼 손맛 (19개 한꺼번에) | [JuicyTuning.cs](Assets/Scripts/JuicyTuning.cs) — `HoverScale`(1.15) / `SquashX`(1.3) / `ShakeStrength`(**7도 비틀기**) / `*Duration`(0.04~0.08). ⚠️ 상수를 고친 뒤엔 **씬에 다시 구워야** 한다(인스펙터에 직렬화돼 있음) — 런타임 생성 버튼만 `JuicyTuning.Attach`로 자동 반영 |
| 패널 열고닫기 속도 | `JuicyTuning.PanelDuration`(0.13 = 타이틀 전체화면 패널, 페이드만) / `ModalDuration`(0.2 = 안쪽 창이 팝하는 인게임 모달). 실제 값은 각 `UITransition` 인스펙터에 구워져 있다 |
| 휘두르기 판정 높이 | `PlayerSkills.SwingCenterYOffset`(**0.6** = 중심을 위로) + `SwingHalfHeight`(2.0). 지금 세로 범위는 아래 −1.4 ~ 위 +2.6 |
| 옵션 패널 배치·색 | [OptionsMenu.cs](Assets/Scripts/OptionsMenu.cs) 상단 색 상수 + `RowStep`(76) / `LabelWidth`(300) / `SliderWidth`(440). 아트가 나오면 색 상수만 교체 |
| 음량 | `VolumeSettings`(PlayerPrefs `option.volume.*`). 효과음 기본 게인은 `SfxBaseGain`(1.3 = 옛 `SfxPlayer.MasterVolume`) |
| 가속 패시브 세기 | `Passive_Accel.asset`(획득 0.05 / 레벨당 0.025 = 만렙 27.5%). ⚠️ 스킬트리 전역 쿨감과 **곱해진다** — 하한은 GCD 0.4초 |
| 독수리 비 세기 | `PlayerSkills.EagleDropRoutine`의 `rainDamageRatio`(0.35/0.28 = 틱당 피해) · `rainDuration`(3/4.5초) · `rainInterval`(0.3/0.2초) |
| 화살비 세기 | `PlayerSkills`의 `ArrowRainDamageRatio`(0.5) · `ArrowRainArrowsPerWave`(10) · `ArrowRainWaveInterval`(0.35) · `ArrowRainAngle`(50도 = 왼쪽 아래) |
| 피뢰침 | `PlayerSkills`의 `LightningRod*` 상수 — 지속 6초 · 간격 1초 · 반경 7 · 피해비 3배(2차 5배) · 기절 0.5초. **기둥 그림은 `SpawnLightningRod`**(임시 프리미티브) |
| 추적 오브(오브R1) | `PlayerSkills`의 `HomingOrbPierce`(3) · `HomingOrbDamageRatio`(0.45) · `HomingOrbScale`(0.8). 추적 로직은 [SmallOrb.cs](Assets/Scripts/SmallOrb.cs) |
| 개쎈 화살(화살R0) | 피해 배수는 `ApplyPathTierEffect`의 `(BasicAttack,1,2)` ×3 · `(1,3)` ×1.5. 추적 화살은 `ChasingArrowDamageRatio`(0.5) |
| 방어·가속 진화 | [PlayerPassives.cs](Assets/Scripts/PlayerPassives.cs) `ApplyPassivePathTierEffect`의 `Defense`/`Accel` case. 피격 반응 3종은 전부 `HandleDamageTaken` 한 곳에 모여 있다 |
| 진화 연계 조건 | [EvolutionRoutes.cs](Assets/Scripts/EvolutionRoutes.cs)의 `RoutePrereq` **한 곳뿐**. `(스킬, 루트) → 조건` 표라 자유롭게 바꿀 수 있다(액티브 2개·패시브 2개·반대 배치 전부 가능). 원본은 노션 "NEW 진화조건 표" |

---

## 🧩 콘텐츠 확장 = 순수 데이터
- **밸런스 편집**: `Window > Blueberry Defense > Balance Dashboard` 또는 인스펙터 — `Prog_*`·`Passive_*`·`EnemyDefinition`·`MapDefinition`·`StageTable`·`ScalingTable`·`CharacterDefinition`·`AscensionTable`. 전역 상수는 `BalanceConstants.cs`.
- **스킬트리 편집**: `Blueberry Defense > Skill Tree Editor`. 백업본 `MainSkillTree_Backup.asset`.
  **새 강화 노드**: 에디터에서 노드를 만들고 그 **id**를 `SkillEffects.Compute`의 case에 추가(효과는 id 기준 레지스트리 — 에셋 `effect` 필드는 신뢰 안 함).
- **새 맵/캐릭터**: `MapDefinition`/`CharacterDefinition`을 만들어 Title 씬의 각 Select UI 배열에 드래그.
- **치트**: `Window > Blueberry Defense > Cheat Window` — 정수 지급, 만렙, 스킬트리 초기화, 승천 등급 설정.

## 🎨 스킬 이펙트 픽셀 크기 컨벤션
이펙트 스프라이트는 배경과 픽셀 밀도가 안 맞아, **그림은 작게 그리고 엔진에서 1.5배로 표시**(플레이어 프리팹 `localScale=1.5`에 맞춤).
- 새 이펙트 프리팹은 기존값에 곱하지 말고 `localScale`을 **직접 (1.5,1.5,1.5)로** 세팅.
- PPU **32**, 기본 맵 카메라 ortho 5(화면 세로 10유닛).
- 공식: `원본 그림 px = (목표 월드 유닛 × 32) ÷ 1.5`
- **배경만 PPU 18** — 스프라이트가 자기 px/PPU만큼 커져 화면을 정확히 덮는다(1배=320×180px, 1.5배=480×270, 2배=640×360).
- ⚠️ **사용자가 새 PNG를 넣으면 임포트 기본값(PPU100·Bilinear·압축·Multiple·좌하단 pivot)으로 들어온다.** 같은 폴더 동종 스프라이트의 `.meta`와 대조해 맞출 것. spriteMode Multiple→Single 전환은 서브에셋 ID를 바꿔 **프리팹 참조가 깨지므로** 재임포트 → 재배선 순서 고정.
