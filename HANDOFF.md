# HANDOFF.md — 세션 인계 문서

<!--
📋 유지 가이드 — "다음 세션이 지금 뭘 해야 하는가"만 담는다. 목표: 한 화면. **200줄을 넘기면 자른다.**
- 담을 것: 현재 상태 · 미해결 이슈 · 아직 안 정해진 것. 세션별 서술은 쌓지 말고 **주제별 사실 목록**에 합친다.
- 🔴 "함정"·"이래서 이랬다"는 적지 말 것 — 커밋 메시지·코드 주석에 남는다. 다시 밟을 함정이면 규칙이므로 CLAUDE.md·ARCHITECTURE.md로.
- 🔴 축적형 문서가 아니다. 매 세션 "추가 + 삭제" 둘 다 한다. 완료·검증된 것은 즉시 삭제(git에 남는다).
- 사본 금지: 할 일·일정은 **칸반**, UI 규격은 **CLAUDE.md §5-1**, 씬 배선은 **SCENE_MAP.md**, 구조·코드 규칙은 **ARCHITECTURE.md**. 여기엔 포인터만.
- ⚠️ 여기 적힌 "~해야 함"·예시·해법은 낡는다. 사용자에게 말하기 전에 코드로 1건 확인할 것.
-->

## 🧭 North Star
> **"스킬을 강화해 적들을 시원하게 쓸어버리자."**
> QWER 액티브 4 + 패시브 4를 진화로 강화해 빌드를 만드는 벙커 디펜스. 스펙은 `GDD.md`(grep으로 필요한 절만).

**세션 시작**: `brief` 스킬. **세션 종료**: `/wrap`. **밸런스**: `balance` 스킬(봇 플레이테스트 루프는 `balance-loop`).

🔧 **에디터 조작은 서드파티 Unity-MCP로 끝까지 간다 — 결정됨(2026-09-02). 다시 검토하지 말 것.**
Unity CLI(1st-party)는 **다음 프로토타입의 세션 0에서** 채택. 근거는 `prototyping-kit/templates/CLAUDE.md` §6.

### 📋 일정·할 일의 원본 = 노션 (여기에 사본을 만들지 말 것)
- **칸반 "POC 할일"** — DB `3b06394c-d983-803d-af4a-c26f924121b7`. 조회 절차는 **`kanban` 스킬**. 어긋나면 칸반이 이긴다.
- **캘린더 "블루베리 디펜스 일정"** — `3a16394c-d983-80ac-98e8-caf8a4804dc0`. 날짜를 인용하기 전에 조회.
- 고정점: **9/25 출시**(3,000원). 빌드 리뷰는 **9/19 재제출 완료 → 결과 대기**(BuildID 25409086). 9/12 반려 사유였던 Steam Cloud는 해소됐다.
- 🧊 **콘텐츠 동결.** 폴리싱이 기본값이고, 콘텐츠 추가는 **칸반 카드로 올라온 것만**. 먼저 제안하지 말 것.

### 다음 할 일 — 칸반 카드 제목 (2026-09-22 조회. 내용은 카드 본문이 원본)
- 진행 중: 「09/09~09/11 — 실제 exe QA」 · 「진화 밸런스 — 진화 SO 값 채우기 · 시간 붕괴 2차」 ·
  「봇 플레이테스트 · 밸런스 루프 + 우주맵 비행선 정체성」 · 「9/18 후반 렉 2차 최적화 — 호밍 재타겟 병목」 ·
  「간체 중국어 추가 + 한/영/중 번역 검수 (9/22)」 · 「크레딧 창 · 엔딩(블루베리 군집체) (9/22)」
- 시작 전: 「Steamworks에 도전과제 등록하기」 · 「코드 정리 중 발견한 버그 6건」 · 「📖 스팀 실무」 · 「출시 후 (09/25~) — Day1 패치」 · 「노션 문서의 낡은 곳 정리 — 9/18 md 대조에서 발견」

---

## 현재 상태 (2026-09-21 기준) — 다음 세션이 알아야 할 사실

**세이브 · 빌드**
- 🔴 **세이브는 `SaveStore` 파일**이다 — `LocalLow/taerimgames/BlueberryDefense/save.json`(에디터는 `save_editor.json`). 음량·언어도 같은 파일. 옛 레지스트리 값은 첫 실행에 1회 이관.
- ✅ **Steam Cloud 반려 대응 끝(9/19 23:09).** Auto-Cloud 경로(`WinAppDataLocalLow` / `taerimgames/BlueberryDefense` / `save.json` / 재귀 끔) · 할당량(1048576 · 10) 게시 → **BuildID 25409086** 업로드 → `default` 라이브 → 동기화 실증 통과(종료 시 업로드 · `save.json` 삭제 후 복원) → **9/19 빌드 리뷰 재제출 완료. 결과 대기만 남았다.** 칸반 카드는 `완료`로 닫힘.
- 🔴 **심사 빌드는 9/18 19:12 빌드다 — 커서 수정(`e29ab44`)과 손플레이 피드백 13건(`542f29e`)이 안 들어가 있다.**
  재빌드 없이 가기로 한 사용자 결정(9/19, 근거: 빌드 심사는 게임 내 버그를 보지 않는다). 둘은 Day1 패치 빌드로 나간다 — 칸반 「출시 후 (09/25~) — Day1 패치」.
  ⚠️ 그래서 **심사 빌드로 손플레이하면 게임 씬에서 커서가 풀리는 게 정상**이다. 새 버그로 보고하지 말 것.
- **세이브 초기화 수정(`2408d92`)은 심사 빌드에 들어가 있다**(9/17 22:19 빌드는 수정 전이었다). 다만 **게임 안 초기화 버튼 실동작은 아직 exe로 확인 안 됨**(칸반 「실제 exe QA」). 초기화하면 타이틀 씬을 다시 불러온다.
- ⚠️ **업적 `ACH_FIRST_LAUNCH`는 배선만 맞고 표시가 임시값이다** — Display Name이 API 이름 그대로(`ACH_FIRST_LAUNCH`)라 달성 팝업에 그게 뜨고, 아이콘도 Steam 기본 "BAM!"(달성/미달성 동일). 심사를 막진 않지만 유저 눈에 보인다. → 나머지 25개와 함께 칸반 「Steamworks에 도전과제 등록하기」에서 처리.
- **포도 해금**(누적 정수 2000 + `Map_Wide20` 승천 1 이상 클리어)은 에디터에서 확인됨. 빌드에서 실제로 풀리는지만 남음(칸반 「실제 exe QA」).

**스킬트리**
- 🔴 **순서는 선행이 아니라 가격으로 유도한다.** 예외로 **리롤만 선행**(리롤 해금이 루트 옆, 리롤 I~IV는 그 가지 안). 현황·배치 원칙은 칸반 「스킬트리 재설계」.
- 🔴 **가격은 노드가 직접 갖는다(`SkillNode.cost`).** 대역(tier) 등비 공식은 **9/19에 폐기**됐다 — `TierCost`·`TierCostRatio`·`UnlockCostMult`는 코드에 없다.
  `tier`는 표시·그룹용으로만 남았고 **비용과 무관하다.** 규칙: 모든 간선에서 자식 가격 ≥ 부모 가격, 자식이 해금류면 예외.
  **가격을 고쳤으면 `node Tools/SkillTree/verify-costs.js`를 돌린다**(Unity 없이 단조성·중복·도달성·총비용·기준 트리를 검사하고 위반 시 exit 1). 가격표는 `Tools/SkillTree/inject-costs.js`.
- 🔴 **강화·기타 노드의 id 계약은 `Meta.cs`의 `SkillEffects`**(`case "id"`) — id가 한 글자만 달라도 정수만 나가고 **조용히 무효**. 설계 문서는 9/14 삭제됨.
- 🔴 **진화는 트리에 잠긴다** — 「진화 해금」·「2차 진화 해금」 노드를 사야 열린다(노드가 트리에 없으면 게이트 자체가 꺼짐).
- 만렙을 줄인 노드의 옛 세이브 초과 레벨은 `SkillTreeSave.EffectiveLevel`이 만렙으로 읽고 정수를 돌려준다. 이미 산 노드 가격이 오르면 남은 정수가 줄어든다(음수 가능).
- 노드 아이콘 84개 전부 있음. 새 그림은 `SkillIconLibraryBake` 표 → 메뉴 「스킬 아이콘 라이브러리 굽기」.
- 🔴 **죽은 노드 잔재는 9/19에 전부 걷어냈다** — `orb_BasicFly`·`Shotgun_CloseBonus`·`root_hp`·`refresh_bonus`의 `Meta.cs` `case`·`Totals` 필드·`MetaBonuses` 필드·소비 코드·번역 표 `tree.*`가 모두 제거됐다.
  같은 유형이 또 생겼는지 보려면 **에셋에 노드가 없는 `case`를 세면 된다**(현재 0건). `MainSkillTree_Backup.asset`도 삭제(GUID 참조 0건이었다).

**진화**
- 🔴 **패시브는 2차 진화가 없다**(`EvolutionRoutes.MaxPassiveStage = 1`).
- 🔴 **액티브 2차에는 「열쇠」가 있다** — 지정된 진화체를 먼저 만들어야 열린다(`EvolutionRoutes.Stage2Prereq`).
  규칙(9/18 사용자 결정): 열쇠 = **그 루트의 1차 조건 스킬의 진화체**(루트만 고름) · 1차 조건이 캐릭터 전용이면 열쇠도 전용 · 열쇠끼리 **순환 금지** · 22칸 열쇠는 서로 안 겹침.
  독수리 R0 2차는 의도적으로 파인애플 전용. 표를 고치면 **실제 `EvolutionRoutes.cs`를 컴파일해 부르는 감사**(위반·도달 불가·슬롯·순환·겹침 + HEAD 대조군)를 다시 돌릴 것.
- 🔴 **"진화는 쿨을 줄이지 않는다"의 유일한 예외 = 되감기 R1 2차**(사용자 승인 — GCD 0 + 전 스킬 쿨 ×0.7).
- 🔴 **진화 스킬의 레벨업은 `Evo_*.levels`가 정한다**(15개 채움, 빈 에셋은 `Prog_*` 커브). 새로 채우면 "효과 없는 카드"를 다시 셀 것 —
  검사 스크립트는 저장소에 없다(`script-execute`로 11스킬 × 5상태 × Lv2~10을 `StepFor`·`ApplyStep`·`DescribeUpgradeEffect` 리플렉션으로 돌림).
- **레벨업 "크기"(Scale)가 폭발·영역 반경에 곱해진다** — 호밍 폭발 · 폭탄 독수리 · 독수리의 비 · 포도 중독사 폭발 · 피뢰침.
- **진화 문구 키는 화면 좌표**(`evo.*.desc.{스킬}.{루트0·1}.{차수1·2}`)인데 **효과 쪽 `PathTier`는 1차=2, 2차=3** — 두 좌표계가 공존한다. 제목은 `evo.name` 하나.
- 2차 전용 아이콘은 `LevelUpUI.activeEvo2Icons`(빈 칸이면 1차 그림). 새 그림은 `Icon_{스킬}R{루트}_2.png` → 메뉴 「진화 아이콘 배선」
  (폴더째 온 그림은 `EvolutionIconWiring`의 `ActiveOverrides`·`ActiveStage2Overrides`에 경로).

**문구 · 번역**
- **UI 문구는 노션 「UI 문구 편집」(6장)에서 고친다.** 되읽기 도구 `.claude/scripts/notion-loc-apply.js` — 시트를 `API-retrieve-page-markdown`으로 파일 저장 →
  `node .claude/scripts/notion-loc-apply.js <파일>`(미리보기) → `--apply` → Unity `Window > Blueberry Defense > 번역 - 모든 TSV를 표에 적재`.
  없는 키는 만들지 않고 매칭 실패는 보고. 시트 id는 부모 페이지 `3cf6394c-d983-81a5-91e8-d0d8be896cd9`, 복원 규칙은 칸반 「UI 문구 노션 왕복」 카드.
- **영어는 심사 빌드에 들어갔다**(9/8 결정 — 노션 쪽 "Day1에 손본다" 문장은 낡음, 아래 확인 대기).
- **[9/22] 간체 중국어(zh-Hans) 추가** — 로케일 `简体中文`, `*_zh-Hans.tsv` 4개(464키, 초벌 = Claude). 설정의 언어 순서 ko → en → 简体中文.
  - 폰트: 도트는 **Z Labs RoundPix 16px**(태리미 결정, `pixelroborobo SDF`·`Num SDF`의 폴백), 본문은 **Noto Sans SC Bold**(`Pretendard-Bold SDF`의 폴백). OFL 전문은 `Licenses/`.
  - Z Labs 에셋은 Dynamic · **Scale 16/15**(한글 높이에 맞춤) · 굵기 `normalStyle 2`(0/2/3/4 캡처 비교로 고름). 세로 위치를 내리는 보정은 시도했다가 뺐다 — 이유는 `Loc.cs` 머리말.
  - 빈 칸 폴백은 **zh → en → ko**(`Loc.MidLocale`). Steam 라이브러리 언어(`schinese` 등)는 저장된 언어가 없을 때만 적용.
  - 스킬트리 **일반 노드 설명**은 이제 `tree.effect.{효과}` + `perLevel`로 짓는다 — 노드별 문구 47개 중 **11개가 밸런스 조정 뒤 틀린 숫자**를 보여주고 있었다. 노드별 `tree.desc.*`(일반 노드)는 TSV에서 지웠다.
  - 캡처 확인한 화면(zh): 타이틀 · 설정 · 스킬트리 툴팁 · 캐릭터/맵 선택 · 전투 HUD · 레벨업 3택 · 진화 창. **아직 안 본 것**: 일시정지 · 결과 · 컬렉션 · 보물 · 엔딩/크레딧.
- 포도 캐릭터의 수치·남은 일은 칸반 「3번째 캐릭터 포도 기획안」 카드 본문이 원본.

**봇 플레이테스트 · 밸런스 루프** — `Assets/Scripts/Bot/`, `Tools/BotPlaytest/`, `balance`·`balance-loop` 스킬.
- 무인 반복은 **새 세션에서 `/loop /balance-loop [최대이터레이션] [마감HH:MM]`** (자동 수락 모드 · Unity 켜 둔 채 · 다른 세션이 Unity를 안 쓸 때). 상태는 `BotRuns/loop_state.json`, 보고서 URL은 `BotRuns/report_url.txt`.
- **0~28회차 측정·판정 완료**(9/22 14:57 마감으로 루프 종료). 이터레이션별 가설·판정·수치는 `BotRuns/iterations/NN.json`과 보고서가 원본 — 여기 옮겨 적지 않는다.
  현재 진행 상황·다음에 볼 것은 **`BotRuns/loop_state.json`이 원본**이다(`insights`·`decisions`·`userPending`·`tickChecklist`). 남은 일 목록은 칸반 「봇 플레이테스트」 카드 본문 맨 아래.
  - 🔴 **루프는 정주행(campaign)만 돈다 — 서열(G2) probe는 안 돈다**(사용자 결정 9/22). 보고서의 G2는 미측정(—)이 정상이다.
  - 🔴 **다음 루프 첫 측정은 새 기준선이다** — 28회차 뒤에 사용자 결정 2건이 측정 없이 들어갔다: 쉬움 보스 피해 200→100(`EnemyDef_BossEasy`, 세 맵 공유) · 우주 전 층 체력 배율 ×1.3 · 속도 배율 ×1.1. 28회차와 나란히 비교하지 말 것.
  - 🔴 **무리 스폰 층(`burstSize` > 1)은 `spawnInterval`이 무리 안에서만 먹는다** — 적 간격은 `burstRest`다. **물량을 늘리면 간격을 그 이상 줄여야 한다**(물량 × 간격이 같으면 XP만 는다 — 사용자 지적 9/22).
  - 🔴 **"스테이지를 너프/버프해라"는 체력 배율이 아니라 구성(간격·게릴라·적 비율)을 바꾸라는 뜻이다** — `balance` 스킬 §2에 규칙으로 있다. 이번에 두 번 틀렸다.
  - 🔴 **9회차부터는 새 기준선이다** — 봇 레벨업 정책에 대공 가중치가 들어갔고(정책이 바뀌면 앞뒤 측정을 비교하지 않는다) 대공 게이트 폐지·방패 피해 하향까지 겹쳤다.
    8회차 이전과 스킬 지표·G4를 나란히 놓고 판정하지 말 것.
  - 🔴 **캠페인 3회의 판수 노이즈는 손대지 않은 목표도 ±1.7판**(22~28회차 실측) — 판수 ±1짜리 변화는 판정 못 한다. 판정은 바꾼 층의 판당 피해·사망 수로 한다.
  - **실측 속도**: 9목표 정주행 캠페인 3회 = 약 1시간 45분(22~28회차). Unity 정지가 나면 감시자 복구로 +10~30분.
  - 🔴 **무인 실행을 시작할 땐 감시자부터 띄운다**: `Tools\BotPlaytest\watchdog_launch.cmd <마감HH:MM> <최대재시작>`.
    밤새 정지가 6번 났고 감시자가 5번을 1분 안에 자동 복구했다 — 못 잡은 1번은 **감시자가 꺼져 있던** 때다(최소화된 `BotWatchdog` 창을 닫으면 안 된다).
- 🚦 **봇이 도는 중에 Unity가 필요하면 `BotRuns/yield` 파일을 만들고(내용 `now` = 즉시), 다 쓰면 지운다.** 규칙 전문은 `CLAUDE.md` §6-2 — 봇 쪽이 비켜 주는 게 원칙이다.
- 봇 정책(레벨업 열쇠 우선 등)을 바꾸면 **그 앞뒤 측정은 서로 비교하지 않는다.**

---

## ⚠️ 미해결 이슈 (아직 안 끝난 것만)

- 🔴 **스킬트리 — 플레이로 손맛 확인이 남았다.** 노드 조합은 탐색 결과이고 태리미가 자리를 옮기는 중. **포도 투척은 해금·강화 노드가 없는 유일한 스킬**이다.
- 🔴 **2차 진화 그림** — 2차 전용 아이콘은 22칸 중 호밍 R1 「초강력 슈퍼 로켓」 하나만 비었다. **이펙트**는 대부분 1차 것을 그대로 쓴다.
  남은 그림 목록은 칸반 「아트 마감」 카드(체크 안 된 줄). 하늘의 울음(취약) · 저글러(증량) · 로열 팔라딘의 망치(장시간 기절)는 로직이 이미 문구대로 돈다 — 그림만 기다림.
- ⚠️ **밸런스 확인이 필요한 둘**: 건강 R0은 복리라 만렙 최대체력 약 ×20, 방어 R1 반사는 화면 전원에게 만렙 40배(`HealthSturdyPerLevel`·`DefenseThornsPerLevel`).
- ⚠️ **눈으로 아직 못 본 것**: 진화 창에서 긴 영어 이름이 세 줄로 접히는지 · 진화 창과 갈림길 2택의 키보드 이동.
- ⚠️ **9/19 구현분 중 플레이모드로 화면을 못 본 것 4건**(값·컴파일은 전부 통과했다):
  ① 만렙 산탄이 볼리 4회 나가는지 ② 검은 화살이 방패를 뚫으면서 **일반 화살·화살비는 여전히 막히는지**(`Projectile` 풀 재사용 누수 — `OnEnable`에서 `PiercesShields = false`로 되돌린다)
  ③ 진화 트리의 **열린 칸**에 `['X' 연계]`가 붙고 날것 키가 안 보이는지 ④ 레벨업 카드가 "투사체 +N" 대신 발사 횟수 문구로 나오는지.
- ⚠️ **다음 exe 빌드에서 확인할 것**: ① 전체화면 화질 ② 컬렉션 아이콘이 호버 때 움직이는지(에디터 재현 실패) ③ 피뢰침 범위(칸반 「실제 exe QA」).
- ⚠️ **커서 크기 — `pixelScale`은 화면에 영향이 없다**(9/19 실측으로 확정). OS가 하드웨어 커서를 **32×32로 줄인다**:
  `SM_CXCURSOR`=32 · 256×256을 넘겼는데 `GetIconInfo` 비트맵이 32×32 · hotspot도 44,48 → 6,6으로 축소.
  **더 크게 하려면 `CursorMode.ForceSoftware`로 가야 하고, 그럴지는 미결정**(꺼내기 전에 문서를 열 것).
  → `CursorSetter`의 `Upscale`·`pixelScale`은 현재 아무 효과가 없다. 지울지는 위 결정에 딸린 문제.
- 🔴 **판 종료 직후 Unity 메인 스레드가 굳는다 — 200~300판에 1번.** `Editor.log` 마지막 줄이 `Game Clear`이고 예외 스택이 없다. `Update()`가 안 돌아 봇 안쪽 워치독은 원리상 못 잡는다.
  **유력한 자리: `GameManager.GameOver`(`GameManager.cs:102`)가 `Enemy.Update`에서 불려 동기 씬 로드를 한다.** 아직 재현으로 확정하진 못했다.
  현재 대응은 **바깥 감시자**(`Tools/BotPlaytest/watchdog.ps1`)다 — heartbeat 5분 정지를 보면 증거를 남기고 Unity를 죽였다 되살려 이어 돌린다(9/19 밤 6회 중 5회 자동 복구).
- 🔴 **우주 맵이 해변용 `EnemyDef_PaperPlane`을 스폰한다.** 우주 어려움 사망 집계에서 해변 종이비행기가 1,294 피해인데 우주 전용 `EnemyDef_PaperPlaneSpace`는 4뿐이다.
  비행선이 격추될 때 쏟아지는 선원이 **맵 프리팹이 아니라 기본 프리팹**을 쓰는 것으로 보인다 — **맵별 적 저작이 조용히 무효가 되는 경로**라 다른 맵도 같이 볼 것.
- ⚠️ **Battle 씬을 단독 Play하면 모달이 안 보인다**(DOTween 지연 초기화가 `defaultTimeScaleIndependent`를 덮음). 타이틀부터 들어가면 정상 — 빌드 영향 없음. 미수정.
- ⚠️ **음원 파일은 git에 없다** — `Assets/Sound/`(209MB)는 `.gitignore`. **결정된 사항, 다시 논의하지 말 것.** 새로 클론하면 소리 참조가 비는 게 정상.
- ⚠️ **우주 맵 `cameraYLift` 3.0은 지금 배경 그림의 지평선 높이에 맞춘 값** — 배경을 다시 그리면 다시 재야 한다.
- 2루트 개편 때 원래 도달 불가였던 기능 둘(건강 체력 재생 · `Effect_RewindYellow`)이 코드에서 빠졌다. 되살리려면 살아 있는 루트에 새로 붙여야 한다.
  (같이 적혀 있던 "기본 공격 비행 타격"·"오브 공중 추가 피해"는 **9/19에 개념 자체가 폐지**됐다 — 비행 적은 닿으면 맞으므로 되살릴 것이 없다.)
- 🔴 **후반 렉 2차 최적화의 판 전체 효과가 미측정** — 호밍 재타겟 병목은 고쳤고 벤치·동치 검증까지 됐지만(칸반 카드가 원본),
  **실제 판에서 얼마나 빨라졌는지는 안 쟀다.** 봇 재측정이 그 확인이다(목표: `Homing` 사용 런 3.64배속 → 5.5배속).
- ⚠️ **`Whirlwind`만은 결과 동치가 아니다** — 더 가까운 적으로 갈아타는 게 최대 0.1초 늦는다(`Whirlwind.RetargetInterval`).
  나머지 최적화는 표적 선택 결과가 완전히 같음을 A/B로 확인했다.
- ⚠️ **Mono 힙 1.09GB가 게임 누수인지 미확정** — 에디터 프로세스 전체값이라(MonoScript 9401개·콘솔·MCP·진단 어셈블리 포함) 단정할 수 없다.
  **빌드로 한 판 돌려 같은 값을 재야 갈린다.** 확실한 건 `GC.Collect()` 1회가 763.9ms 블로킹이라는 것(에디터 실측).

---

## ❓ 확인 대기 (Claude가 물어야 했지만 안 묻고 진행한 것)

무인 실행 중에는 질문으로 턴을 끊지 않는다(CLAUDE.md §1 최상단). 갈림길을 만나면 한쪽을 골라 끝까지 만들고 여기에 적는다. 태리미가 확인하면 그 줄을 지운다.

**형식:** `- [날짜] 갈린 것 → 고른 쪽(이유) / 반대쪽이면 달라지는 것 / 되돌릴 곳`

- [9/22] **"우주 전체 HP mult랑 Speed 좀 올려봐"의 크기** → 1~25층 전부 체력 ×1.3 · 속도 ×1.1("좀"을 이 폭으로 읽음) / 크게 잡으면 우주 어려움(28회차에 한 캠페인 30판 실패)이 먼저 벽이 된다 / 되돌릴 곳: `StageTable.asset`(우주) 전 층 `enemyHpMultiplier` ÷1.3 · `enemySpeedMultiplier` ÷1.1
- [9/22] **"UFO 벽 쉬움에 하나 보통에 하나"의 층** → 우주 11층(쉬움 구간) · 16층(보통 구간, 18층이면 17·18·19 세 층 연속 벽) / 28회차 측정에서 둘 다 피해 0 — UFO 스탯을 안 올리면 벽이 안 된다 / 되돌릴 곳: `StageTable.asset`(우주) 11·16층 — 이전 구성은 `BotRuns/iterations/27.json`의 `changes`

- [9/22] **zh 번역은 원어민 검수 없이 Claude 초벌** → 기계 검사(키·자리표시자·꺾쇠·폰트 커버리지) 0건 + 화면 캡처까지만 / 원어민 검수를 끼우면 어색한 표현이 걸러진다 / 되돌릴 곳: `*_zh-Hans.tsv`만 고치고 적재
- [9/22] **en 용어 통일 6줄** → 진화 루트 = Path, 연계 = Synergy(컬렉션의 "[아이콘] Synergy"에 맞춤 — 계획서의 "linked"에서 바꿈), 회복 아이템 = Health pickup, 치명 문장 = Critical / 되돌릴 곳: `ui.collection.route`·`ui.evotree.prereq`·`ui.evotree.keyPrereq`·`ui.evotree.continueRoute`·`passive.cur.Assassinate.critCap`·`tree.desc.health_HealItem`
- [9/22] **zh 난이도 칸(`ui.ascension.stats`)만 항목마다 줄바꿈** → 한자는 아무 데서나 줄이 바뀌어 「移动速度 / +20%」로 갈라졌다. ko·en은 그대로 / 되돌릴 곳: `inline_zh-Hans.tsv`
- [9/22] **빈 Addressables 그룹 `Localization-String-Tables-Korean`을 안 지웠다**(파일 삭제는 승인 사항) → 로케일을 추가하자 Localization 패키지가 한국어 표를 표시 이름 그룹 `…-한국어`로 옮겼다. 주소·라벨이 같아 동작은 같다 / 지우려면 그 그룹 에셋 1개 + Schemas 2개
- [9/22] **CJK 줄바꿈 금칙을 켰다**(TMP Settings 두 파일에 TMP 기본 금칙 문자) → 한글도 이 설정에선 글자 단위 줄바꿈이라 ko·en 줄에서도 `%`·`.`·`,`가 줄 맨 앞에 안 온다(개선 방향이지만 ko 화면 줄바꿈이 한두 곳 바뀔 수 있다) / 되돌릴 곳: `m_leadingCharacters`·`m_followingCharacters`를 None으로
- [9/22] **Steam 상점 페이지·업적 zh 문구는 아직 안 만들었다** → 상점 ko/en 원문이 저장소에 없다. 원문을 받으면 번역만 만들면 되고, Steamworks "지원 언어"에 간체 체크는 태리미 몫

- [9/22] **Steam 업적 26개 코드 배선 완료, Steamworks 등록은 안 함**(외부 게시라 승인 사항) → API 목록 원본은 `Achievements.cs`, 등록 절차·이름 초안은 칸반 「Steamworks에 도전과제 등록하기」. 코드 변경이라 **Day1 패치 빌드**에 실린다. **커밋 안 됨**(9/22 wrap 때 병렬 세션이 같은 파일을 고치는 중이라 보류 — 제 변경: `Achievements.cs` 신규 + `SteamBootstrap`·`GameManager`·`EndingSequence`·`Enemy`·`PlayerSkills`·`PlayerPassives`·`SkillTreeData`·`MetaRunApplier`·`CharacterSelectUI`·`CollectionSave`에 한두 줄씩). 에디터·봇에선 Steam이 꺼져 `[ACH] …` 로그만 찍힌다 — **봇 정주행·실기 Steam으로는 아직 확인 안 함**
- [9/22] **업적 갈림길** — ① **2차 진화(`ACH_EVOLVE_2`)는 남김**: 봇 완주 50회 중 21회가 엔딩까지 한 번도 2차 진화를 안 했다(= 저절로 받는 업적은 아님). 다만 엔딩 전에 트리가 항상 완성되고 엔딩 뒤에도 계속 플레이할 수 있어서 놓칠 수 없다. 빼려면 `Achievements.OnEvolved`·`SyncSave`의 `Evolve2` 두 줄 ② **누적 처치 1,000 / 10,000 · 진화 발견 10종** = 봇 완주 최솟값(처치 20,633 · 진화 14종)보다 낮게 잡음. 손잡이는 `Achievements.cs` 상단 상수 ③ **캐릭터별 클리어·첫 패배·게임 안 업적 화면·Steam 통계(진행 바)는 넣지 않음** — 정상 클리어가 요구하지 않거나 놓칠 수 있어서 ④ **엔딩 업적 소급**: 패치 전 세이브는 `Map_Wide20` 어려움 클리어 기록으로 엔딩을 본 걸로 친다 ⑤ **누적 처치는 패치 뒤부터 센다**(예전 판은 기록이 없다) ⑥ **엘리트 첫 처치는 태리미 요청으로 뺌**, 엔딩 업적 문구는 "블루베리 군집체"
- [9/22] **크레딧 명단 = `Assets/Prefabs/UI/CreditsContent.prefab`**(Title 크레딧 창에 인스턴스로 놓였고 엔딩이 같은 프리팹을 읽는다 — 고칠 땐 프리팹을 연다. 씬 인스턴스에서 고치면 엔딩에 안 들어간다).
- [9/22] **엔딩(`EndingSequence`) 갈림길** — ① 조건 = `Map_Wide20` + 승천 **3 이상** + 봇 실행 아님, **깰 때마다** 재생(첫 클리어만 아님) ② 정수 적립·클리어 기록은 **엔딩 시작 전에** 남긴다(중간에 꺼도 클리어 인정) → 일반 클리어 화면(피해 미터)은 이 맵·난이도에선 안 뜬다 ③ 군집체 체력 = **어려움 보스 기본값의 2배(1000, 태리미 결정)** × 25층 배율 → 방금 잡은 보스의 2배. 손잡이 `EnemyDef_BlueberryCluster.maxHealth` ④ 군집체는 몸박·이동 없음, 엔딩 중 플레이어 무적 ⑤ 군집체 그림 `Blueberry_Cluster.png`(240px 캔버스, 적 10종을 반지름 100px 원 안에 쌓은 **코드 생성 임시 그림** — 테두리·자르기 없이 가장자리는 울퉁불퉁. 몸통 반지름은 판정 상자 6.25가 기준) — 다시 그리면 같은 파일명으로 덮으면 된다 ⑥ 영어 문구는 내가 씀(`ending.*`, `inline_en.tsv`) / 시간·확대 배율은 `Resources/EndingSequence` 프리팹 인스펙터
- [9/22] **크레딧에 남긴 건 개발·아트·도움 주신 분 + 감사 인사**(효과음·VFX·폰트·도구·음악은 태리미 결정으로 뺌 — 전부 표기 의무 없음 확인). **섹션 제목은 번역 키에 묶여 있다**(`CreditsContent`의 `Heading_*` → `LocalizedTmp` `credits.*`) — 제목 글자를 바꾸려면 프리팹 TMP가 아니라 **`inline_*.tsv`(ko·en·zh-Hans)의 `credits.*`**를 고치고 표에 적재한다(프리팹 글자는 실행 시 덮인다). 이름(`Names_*`)은 번역 없이 프리팹 글자 그대로.
- [9/22] **크레딧·엔딩 작업도 커밋 안 됨**(wrap 때 병렬 세션 3개가 작업 중이었고, 같은 파일에 업적 호출이 섞여 있어 제 몫만 떼면 컴파일이 안 된다) — 제 변경: 신규 `CreditsUI.cs`·`EndingSequence.cs`·`BlueberryCluster.cs`·`Assets/Editor/CopyLicensesPostBuild.cs`·`CreditsContent.prefab`·`Resources/EndingSequence.prefab`·`Enemy_BlueberryCluster.prefab`·`EnemyDef_BlueberryCluster.asset`·`Blueberry_Cluster.png`·`Licenses/`(Pretendard·Galmuri·Pixelroborobo) + 수정 `TitleController`·`GameManager`(IsEnding)·`PlayerSkills`(Sealed)·`PlayerHealth`·`RunBootstrap`(SetBgmPlaying)·`EnemySpawner`·`Title.unity`(CreditsRoot)·`inline_ko/en.tsv`(`credits.*`·`ending.*`)·번역 표·`SCENE_MAP.md` / **마지막으로 wrap하는 세션이 전부 한 번에 커밋**하면 된다
- [9/22] **폰트 OFL 고지는 크레딧 대신 빌드 폴더의 `Licenses/`로** — 프로젝트 루트 `Licenses/`(Pretendard·갈무리·픽셀로보로보체 3개)를 `CopyLicensesPostBuild`가 빌드 후 exe 옆에 복사. **실제 빌드로는 아직 확인 안 함** — 다음 빌드에서 `Build/Licenses/`에 3개가 생기는지 볼 것. 픽셀로보로보체 저작권 문구는 폰트 메타데이터(`babaisyou531@gmail.com`) 그대로.
- [9/21] **독수리 레벨업 커브에서 "투하 간격" 스텝 3개를 전부 쿨감으로 바꿈** → 9칸 = 쿨감 ×0.88·0.88·0.85 / 피해 ×1.3 ×3 / 투하 +1 ×3(→10렙 6회). 칸이 9개라 투하 +1을 하나 더 넣으려면 뭔가를 빼야 했고, "쿨감을 더"에 맞춰 간격을 뺐다(10렙 쿨 15→약 9.9초). 투하 간격은 1초 고정이라 6회 투하가 5초에 걸쳐 떨어진다 / 간격을 남기면 쿨감이나 피해 스텝이 하나 줄어든다 / 되돌릴 곳: `Prog_EagleDrop.asset`의 `levels`(stat 9 = 간격, 1 = 쿨)
- [9/21] **맵 선택 기본 난이도 = 그 맵에서 해금된 최고 난이도로, 맵 카드를 바꿔 누를 때도 적용** → 지난 판 난이도를 이어받던 동작은 없어졌다. 쉬움을 일부러 다시 하고 싶으면 매번 ◀를 눌러야 한다 / 되돌릴 곳: `MapSelectUI.Select`의 `ascensionLevel = MaxSelectableAscension;` 한 줄

- [9/18] **노션이 게임·코드보다 낡은 곳 5건**(기획서 진화 목록 · Day1/문구 안내문의 영어 · 아트 마감·진화 밸런스 카드 첫 줄 · 데이터 시트 서술부) → 노션 수정은 승인 사항이라 안 고치고 칸반 「노션 문서의 낡은 곳 정리」 카드에 체크 목록으로 올림(목록 원본은 그 카드)
- [9/18] **GDD.md §7-5 루트 표를 지우고 노션 「스킬 데이터 시트」 포인터로 바꿈** → 같은 표가 GDD·기획서·데이터 시트 세 곳에 있었고 GDD·기획서 둘 다 낡아 있었다(데이터 시트만 9/18 최신) / GDD 안에서 표를 바로 보고 싶다면 `EvolutionRoutes`·`harvest_ko.tsv`에서 다시 옮겨 적으면 된다(대신 갱신할 곳이 하나 늘어남) / 되돌릴 곳: `git show HEAD:GDD.md`의 §7-5

- [9/18] **대형 오브 "좀만 올려"를 크기 비례 보정으로 함** → 크기(Scale)가 커질수록 아래로도 커져 묻히던 게 원인이라, 아래 가장자리를 크기 1.3(미진화 만렙) 오브에 고정(진화 직후 +0.4 · 2차 Lv10 약 +1.7). 고정값으로 조금만 올리면 레벨업할수록 다시 묻힌다 / 캡처 확인은 봇 세션이 Unity를 쓰는 중이라 못 함 / 되돌릴 곳: `PlayerSkills.FireOrb`의 `BigOrbLiftBaseScale`·`BigOrbLiftPerScale`

- [9/18] **파인애플 휘두르기 흔들림에 파란 화면이 보임 → 맵별 단색 채움 + 흔들림 0.12→0.07** → 채움색은 배경 가장자리에서 가장 많이 쓰인 색 하나(밭 초록 `#6ABE30` · 해안가 하늘 `#B3DFFF` · 우주 `#060513`)라서, 밭 **위쪽**(하늘)과 해안가 **아래쪽**(모래)에는 다른 색 띠가 잠깐 보인다 / 변마다 색을 따로 칠하면 네 변 모두 맞출 수 있지만 오브젝트가 늘어난다 · 흔들림을 더 줄이면 띠는 얇아지고 타격감도 약해진다 / 되돌릴 곳: 각 `Map_*.asset`의 `edgeColor` · `ScreenShake.SwingStrength`
- [9/18] **피뢰침 기둥·번개가 떠 있음 → 바닥을 카메라가 아니라 적 레인(`EnemySpawner` y) − 0.52(기본 블루베리 발밑)로** → 원인은 맵마다 다른 `cameraYLift`(우주 맵에서 2.6유닛 떠 있었음). 피해 판정의 중심은 그대로 카메라 / 발밑보다 더 파묻히게 하려면 이 값을 키운다 / 되돌릴 곳: `PlayerSkills.LightningRodLaneFeetDepth`
- [9/18] **휘두르기 범위 보이기 — 갈림길 셋** ① 망치는 `skill.Scale`만큼 커진다(힘 루트 1차의 ×1.45는 이미 큰 망치 그림이라 안 곱함) ② 기준점 = 손잡이 끝(손) → 손에서 안 떨어지는 대신 **내리찍는 프레임의 머리가 발밑보다 내려간다**(만렙 1.45배에서 약 0.37유닛). 머리를 땅에 붙이면 손잡이가 손에서 뜬다 ③ 범위 그림자는 **항상 보인다**(판정 가로 구간 그대로의 반투명 타원, 알파 0.22). 휘두를 때만 보이게 하면 화면은 깔끔한 대신 범위를 미리 못 읽는다 / 되돌릴 곳: `PlayerSkills.LateUpdate`·`SwingShadow*` 상수
- [9/18] **우주 맵 비행선 강화**(사용자: "배 탄 엘리트가 더 많이 · 쏟아지는 벽 스테이지 · 단일 대상 딜 시험 몹"). 갈림길 넷:
  ① **비행선 벽 = 13스테이지**(UFO 벽을 교체, 8대·3초 간격·체력 배율은 이웃 곡선 그대로) → 22는 우주 어려움에서만 나와 쉬움·보통 플레이어가 못 본다. 22 UFO 벽은 남김 / 22로 옮기려면 두 스테이지 칸을 맞바꾼다
  ② **8스테이지부터 비행선 실제 비율 3% → 25스테이지 12%**, 늘린 몫은 **엘리트 몫에서** 뺐고 다른 적 실제 비율은 유지(else-if 사슬을 역산) · 스폰 수는 쏟아지는 10마리를 절반 가중으로 상쇄해 줄임(25스테이지 142→96) / 다른 적 몫에서 빼면 우주의 다른 특징이 옅어진다 · 스폰 수를 안 줄이면 스테이지가 길고 빽빽해진다 / 되돌릴 곳: `Assets/Data/StageTable.asset` (git diff)
  ③ **사망 분출 몹이 스테이지 배율을 물려받게 함(비행선만)** — 새 옵션 `Enemy.deathSpawnInheritsMultipliers` · 분출 종류 블루베리·엘리트 → +라이더·방패·종이비행기 → 지금까진 기본 스탯이라 후반엔 "코앞에서 터지면 큰 피해"가 안 났다. 보스는 끔(분출 30마리에 보스 판 배율이 붙으면 보스전이 통째로 뛴다) / 되돌릴 곳: `Enemy_Airship.prefab`의 `deathSpawnInheritsMultipliers`·`deathSpawnPrefabs`
  ④ **군중제어 저항: 보스 0.5 → 0.15(15%만 받음) · 비행선 0.5** → "보스 아주 강하게 · 배 적당히 강하게"를 이 값으로 읽음. 감속·기절 **세기**와 넉백 **거리**에만 걸고 지속시간은 그대로 / 되돌릴 곳: `Enemy.BossCrowdControlScale` · `EnemyDef_Airship.crowdControlResistance`(새 칸)
  ⑤ **비행선 높이 무작위: 레인 위 2.2~6.0**(사용자: "하늘을 나는 배 · 공중 딜링 시험") — 새 옵션 `Enemy.spawnYJitter`. 휘두르기 판정 위쪽이 발 위 약 2.6이라 대부분은 대공 스킬로만 잡힌다 / 더 낮게 섞으려면 `spawnYOffset`을 내리고, 화면 위로 더 띄우려면 `spawnYJitter`를 키운다(우주 맵 화면 안 한계 약 +12) / 되돌릴 곳: `Enemy_Airship.prefab`의 `spawnYJitter`
- [9/18] **"회오리 무리" = 회오리 R0(분열 회오리·회오리 생성기)의 본체**로 읽고, 본체만 표적을 x·y 둘 다 쫓게 함(중력 없음, 표적이 없으면 원래대로 땅으로) → 미니는 소멸 자리에서 원래대로 떨어져 지상만 때림. 진화 전 회오리·R1 대회오리는 그대로 / 진화 전 회오리까지 공중 추적이면 `FireWhirlwind`의 `PathTier[0] >= 2` 조건을 뺀다 / 되돌릴 곳: `Whirlwind.HomeInY` · `PlayerSkills.FireWhirlwind`
- [9/18] **봇 레벨업·진화 선택 우선순위**(사용자: "2차 진화가 잘 안 만들어질 것 같아서") — "열쇠 스킬"을 게임 코드 정의로 읽음: 1차 진화 스킬의 열쇠 = `EvolutionRoutes.Stage2Prereq`, 진화 전 스킬의 열쇠 = `RoutePrereq`(루트 둘 다). **"보유한 스킬"에 패시브도 넣음**(패시브도 1차 루트 조건이 있다) · 열쇠 카드는 **신규 획득 카드**만 인정 · 진화 대상 선택은 "보유했지만 아직 진화 전인 2차 열쇠"를 먼저, 그 스킬의 루트는 열쇠가 요구하는 루트로 / 액티브만 뜻이었다면 `BotPilot.LevelUpPriorityPool`의 `pp.EquippedPassives` 루프를 지운다
- [9/18] **스킬 발동 순서 = 오래 기다린 스킬 먼저**(사용자 승인) — 동시에 준비되면 슬롯 순서로 갈림. 원인: 풀트리 파인애플 휘두르기 실제 쿨 0.33초가 전역 쿨 0.4초보다 짧아 W·E·R이 가능 횟수의 약 10%만 발동 / 되돌릴 곳: `PlayerSkills.Update`의 `castQueue` 루프
- [9/18] **휘두르기 쿨 1.5 → 4초, 피해 14 → 40**(사용자: "쿨 4초 · 데미지 올려봐") → 쿨에 비례한 DPS 유지값 37에 초반 보정 약 7%. **쓸어치기(R0) 진화 쿨은 코드 ×3이면 약 10초라 `Evo_Swing_R0_T1.baseCooldown` 7초로 덮음** — 지진파(R1)는 코드대로 약 6.2초 / 되돌릴 곳: `Prog_Swing`·`Evo_Swing_R0_T1`
- [9/19] **0~8회차 루프의 갈림길들은 전부 해소됐다**(측정 규모 축소 · campaign 생략 · 1회차 유지 · 2회차 미측정 · Unity 강제종료 보류 — 다섯 줄을 여기서 지웠다).
  이유: 8회차까지 다 돌았고 campaign 생략은 **사용자 지적으로 철회**돼 `balance-loop` 스킬에 "매 이터레이션에 campaign을 포함한다"로 박혔다.
  이터레이션별 가설·판정은 `BotRuns/iterations/NN.json`이 원본이라 여기 사본을 두지 않는다.
- [9/19] **`fly_3` 노드의 자리·가격을 내가 정했다**(선행 `fly_2` · 1460정수 · +30% 2레벨 · editorPos 1980,-70) → 사용자는 "트리에 대공 노드를 더"라는 **방향만** 줬고 트리 그래프 설계는 태리미 몫이다.
  가격을 45% 기준 트리가 못 사는 대역(최고가 290)에 일부러 올려서 **G2는 안 흔들고 G4만 움직이게** 했다 / 가격을 내리면 그 분리가 깨져 서열 측정이 같이 움직인다 / 되돌릴 곳: `Tools/SkillTree/inject-costs.js`의 `fly_3` + `node Tools/SkillTree/inject-costs.js` 재실행
- [9/19] **대공 게이트를 지운 뒤에도 봇의 대공 가중치를 남겼다** → 게이트가 사라져도 사거리·유도 차이는 남아서(지상에 묶인 회오리 vs 쫓아가는 저격·호밍) 가중치가 여전히 뜻이 있다고 봤다.
  (9/20에 실측으로 목록을 다시 뽑아 피뢰침을 뺐다 — 저격·호밍·독수리) / 뺀다면 `BotPilot.LevelUpPriorityPool`의 ⓪단계 블록을 지운다
- [9/21] **"1-5층은 지금대로 둬. 5-20만 바꾸자"를 5층 포함으로 읽음** → 두 문장이 5층에서 겹쳐 뒤 문장을 실행 지시로 봤다. 1~4층만 원본값 / 5층도 빼려면 `StageTable*`의 5층 `spawnInterval`·`spawnCount`·`ambushCount`를 원본으로
- [9/21] **쉬움 보스 −20%를 두 번 받아 한 번만 적용**(500→400) → 두 번째는 앞 지시의 반복으로 봤다 / 한 번 더면 320 — `EnemyDef_BossEasy.maxHealth`
- [9/21] **해변은 1~15층을 올리고 16~20층을 내려 쉬움·보통을 가른다** → 1~15층을 두 티어가 공유해 따로 못 움직인다. 20·21회차에 이 상쇄가 실제로 통했다(해변 보통 4.00 유지 → 2.33) / 되돌릴 곳: `StageTable_Coast` 16~20층 확률합 0.5
- [9/21] **21회차 판정을 마지막으로 22회차 변경은 에셋에 안 넣음** → 마감(19:00) 전에 측정이 못 끝나고, 사용자가 루프를 끊었다. 다음 후보는 `loop_state.json`의 `insights`(해변 9~12층 콩콩이부터)
- [9/19] **확률을 0.75로 둠**(대공 스킬이 없을 때 대공 카드를 고를 확률) → 1.0이면 모든 판이 같은 빌드가 되어 "대공 유무가 만드는 차이"를 측정할 수 없다 / 되돌릴 곳: `BotPilot.AntiAirPickChance`
- [9/18] **렉 2차 최적화에서 손대지 않은 것 셋** → ① **미사일 수 상한**(`GrowthStacks/20`이 상한 없이 늘어 판 끝 약 90발)은 **밸런스 손잡이**라 `balance` 스킬 기준으로 판단해야 해서 안 건드림 ② **`DamageNumber` 동시 개수 상한**(`HitParticle.MaxLive=2000` 같은 성격인데 없음) ③ **VFX 반납 지연 단축**(효과 길이의 4~7배로 남아 있음) → 셋 다 **1단계 최적화만으로 목표 배속에 닿으면 영구히 안 건드려도 된다.** 특히 ③은 실측에서 파티클 95개·drawCall 197로 **병목 근거가 없는데** 줄이면 연출이 잘릴 위험이 있고 그건 값이 아니라 캡처로만 검증된다 / 넣으면 프레임은 더 벌지만 ①은 스킬 파워가, ②는 후반 타격 피드백이, ③은 이펙트 꼬리가 달라진다 / 되돌릴 곳: `PlayerSkills.cs`의 `HomingMissileGrowth` 분기 · `Enemy.SpawnDamageNumber` · 각 `Despawn(vfx, N)` 인자
- [9/18] **표적 정렬을 "상위 k개 부분 선택"으로 바꾸려다 정렬 키 사전 계산으로 선회** → 계획 단계에선 부분 선택이 빠를 줄 알았는데, `k = TargetRank % N`이 미사일 90발이면 최대 89까지 가서 O(N·k)=26,700이 O(N log N)≈2,400보다 **오히려 느리다**. 대신 비교 함수 안의 `transform.position` 네이티브 접근을 N log N번 → N번으로 줄였다(실측 505.3 → 90.8 us/call) / 부분 선택을 쓰려면 `TargetRank`를 작은 값으로 클램프해야 하는데 그건 표적 분산이 줄어 밸런스가 바뀐다 / 되돌릴 곳: `HomingMissile.AcquireTarget`·`ByPriority`

**안 하고 남긴 것** (귀·눈으로 골라야 하는 것):
- 액션아이템 24 **보물 획득 사운드 교체** — 현재 `coins_gather_medium.wav`, 요청은 "밝고 기대감 있는 팡파레". 후보를 들어 봐야 골라진다. 자리: `Assets/Resources/SfxLibrary.asset`의 `treasureOpen`
- 액션아이템 23 **오브 이미지 해상도 통일** — "통일"의 기준이 불명(`Effect_BigOrb` 64×64 / `Effect_SmallOrb` 32×32 / 타 이펙트 48×48)
- **에디터 세이브 유실분**(도감·맵 클리어) — 태리미가 "그대로 둔다"로 결정(2026-09-03)
- **스테이지 난이도 재조정**(9/2 산탄 개편 영향) — 봇 밸런스 루프가 맡았다. 판단 기준은 `balance` 스킬

---

> 코드 구조·시스템 소유권·손잡이·코드 규칙은 여기 적지 않는다 — **해당 코드의 주석**과 `ARCHITECTURE.md`에 있다.
