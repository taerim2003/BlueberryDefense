# CLAUDE.md

Behavioral guidelines to reduce common LLM coding mistakes. Merge with project-specific instructions as needed.

**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- **해석이 갈리는 걸 *인지한 순간*이 묻는 시점이다 — 구현을 마친 뒤 각주로 달지 말 것.**
  바로 위 규칙은 "제시하라"까지만 말해서 **일단 다 만들고 끝에 단서를 붙이는 것**을 못 막는다. 그건 묻는 게 아니다.
  트리거 자명 — *"이건 해석이 갈릴 수 있다"는 생각이 드는 순간.* 그 자리에서 한 줄 묻는다.
  갈림길이 **기능 스펙이 아니라 손맛·연출**일 때 특히 그렇다 — 검증으로는 절대 안 갈리고, 틀리면 방향이 **정반대**가 된다.
  (세션49: "산탄 사격 재구현"을 사용자 의도와 정반대로 구현해 검증까지 마친 **뒤에** "해석이 갈릴 수 있습니다"라고 적었다.)
- **요구 목록의 한 줄이 *현상*인지 *목표*인지 갈라 읽을 것.** 버그 목록에 섞인 **느낌 서술**이 위험하다.
  트리거 자명 — *액션 아이템에 "~한 느낌"·"~하는 거임" 같은 서술이 있는 순간.* → **지금 값을 먼저 재 본다.**
  **값과 해석이 어긋나면 틀린 건 해석이다.**
  (세션49: *"산탄이 엄청 빠르게 나가서 …빵 나가고 없어지는 느낌"* 을 불만으로 읽었는데 **원하는 모습**이었다.
  `Scatter_Pellet`의 탄속이 5로 게임에서 가장 느린 축이라는 걸 **재 보고서도** 안 갈렸다.)
- **코드를 고치기 전에 "무엇을 어디에 손대는지"를 한 줄로 선언할 것.** 묻는 게 아니라 **선언**이다 — 답을 기다리지 말고 바로 진행하고, 틀렸을 때만 사용자가 끊게 한다.
  **파일·화면·오브젝트 이름을 대야 틀린 게 드러난다**("우측 하단에 붙일게요"는 안 드러나고 "`SkillTreeUI` 우측 하단"은 드러난다).
  사고는 늘 *갈림길로 안 보이는 갈림길*에서 났다 — "새로운 맵"(세션20) · "벽 벽 애들"(세션21) · "화면 우측 하단 원티드 포스터"(세션26, 맵 선택 화면에 만들었다가 통째로 옮김. 정답은 스킬트리 창).
  전부 **대상이 하나뿐인 줄 알았는데 여럿**이었다. 기능 스펙은 갈림길마다 물어놓고 배치만 추측한 게 사고였다.
  특히 **피드백 왕복 중에 잘 난다** — 일기 34편 집계상 계획된 작업은 14%, 즉흥 왕복은 44%.
  → **UI 코드를 고치기 전에 그 코드가 그리는 화면을 전부 열거할 것.** `LevelUpUI` 하나가 **3택 레벨업 · 2택 갈림길 · 진화 대상 선택** 셋을 그린다.
  열거하면 "어느 화면인가"가 저절로 질문이 된다 — 세션28에 안 세고 "선택지 UI"를 전부로 읽어 **씬의 카드 3장을 고쳐 저장했다가 통째로 되돌렸다**(반대 방향 사고: 하나인데 여럿으로 읽음).
- **"넣어줘"를 받으면 넣을 곳이 아니라 *이미 있는 곳*부터 센다.** 위 열거 규칙은 "이 코드가 뭘 그리나"만 묻고 "거기 이미 뭐가 걸려 있나"는 안 묻는다 — 그래서 **중복으로 얹게 된다.**
  세션31에 "UI에 애니메이션 넣어줘"를 전부 새로 넣을 뻔했는데, 사용자가 *"체력바 같은거 지금 들어가있지 않아?"* 라고 짚어줘서 grep했더니 HUD·스킬트리·레벨업에 **이미 DOTween 연출이 여럿**이었다.
  → 기능을 추가하기 전에 **그 기능의 기존 구현을 `grep`으로 먼저 센다**(연출이면 `DOTween|DOScale|Punch`, 저장이면 `PlayerPrefs`…). 있으면 **겹치는지·경합하는지**부터 보고할 것 — 실제로 두 곳(`TreasureRewardDecor`·진화 노드 무한 펄스)은 얹으면 싸우는 자리였다.
- **"A를 B로 바꿔줘"를 받으면 B를 짜기 전에 *A에 딸린 것*부터 센다.** 바로 위 규칙은 **더할 때**를 말하는데, **갈아끼울 때**는 반대쪽이 안 보인다 —
  명세·요청은 "무엇으로 바꿀지"만 말하고 **무엇이 사라지는지는 안 말한다.**
  세션34에 진화 16종을 명세대로 교체하다 기존 기능 **4개가 죽었고**(오브 제단·초과 피해 연쇄·체인 라이트닝·회오리 타겟팅) 전부 **구현 도중에** 하나씩 발견했다.
  매핑표에 "기존 진화" 열을 만들어 뒀는데도 거기 적은 건 **이름**("오브 제단")이라, 그게 프리팹·VFX·**다른 함수에 숨은 전용 분기**를 달고 있다는 건 안 보였다
  — `TryUseSkill`의 `OrbAltarCooldown` 분기를 못 봤으면 새 진화가 **조용히 15초 쿨로** 돌 뻔했다.
  → 교체 대상을 **`grep`해 호출부·전용 분기·프리팹 참조를 전부 열거**한 뒤 시작할 것. 죽는 게 있으면 **지울지 남길지 먼저 보고**한다.
- **사용자 아트를 반영할 땐 파일 수정 시각부터 확인할 것.** 여러 번 다시 그리는 워크플로에서 "내가 배선한 그림"과 "지금 디스크에 있는 그림"이 쉽게 어긋난다(세션26: 애니 작업 1시간 전의 낡은 스케치를 대기 프레임으로 쓰고 있었다).
  **그리고 배선 직전에 "한 파일 = 한 그림인가"를 볼 것** — Unity 기본값이 Multiple이라 **자동 슬라이스가 그림을 조각낸다.**
  서브 스프라이트 이름이 `_0`이 아니라 `_1`로 나오면 이미 쪼개진 것이고, 그대로 배선하면 **잔상·꼬리 조각만 날아간다**(세션35 `Effect_Arrow*`가 그랬다). 자세한 절차는 `unity-mcp` 스킬.
- **파일명 접미사의 뜻을 파일 종류마다 확인할 것.** `Icon_*R1/R2`는 **루트**(R1=루트0)인데,
  `Effect_Arrow*R1/R2`는 **애니메이션 프레임 번호**다 — **둘 다 1차 진화 화살**이고 R2는 태리미의 작화 도구가
  다음 프레임에 자동으로 붙인 이름이다(2026-08-26에 태리미가 확정. 세션35에 "티어"라고 적었던 건 **오독이었다**).
  같은 접미사가 **셋 다른 뜻**일 수 있다: 루트 · 티어 · 프레임 번호. **그림을 열어보는 것으로는 안 갈린다. 한 번 물을 것.**
- 선택지를 낼 땐 **손잡이 이름(`ambushCount` 같은 필드명)이 아니라 화면에서 벌어지는 일로** 먼저 한 줄 설명할 것. 사용자는 코드 필드로 생각하지 않는다 — 세션25에 "게릴라를 늘린다"를 서로 다른 뜻으로 쓰다 선택지 전체가 헛다리를 짚었다(정답은 내가 낸 3안 밖에 있었다).
- **한 값이 결과 둘을 동시에 움직이면, 고치기 전에 두 결과의 수식을 세운다.**
  트리거 자명 — *같은 파라미터를 고쳤는데 사용자가 **반대 방향 불만**을 말하는 순간*("얇다" 다음에 "너무 파고든다").
  그건 손잡이가 모자란 게 아니라 **내가 결과를 하나만 보고 있다는 신호**다. 한쪽만 보고 움직이면 왕복이 안 끝난다 —
  세션38에 폰트 아웃라인 하나로 **사용자를 네 번 왕복시켰다**(`_FaceDilate`·`_OutlineWidth`가 *바깥 번짐*과 *안쪽 침범*의 공통 입력이었다).
  두 수식을 세우자 그 자리에서 멈췄다.
- **"구조적 한계입니다"라고 쓰기 전에 전수 조사를 한 번 한다.** 트리거 자명 — *한계를 선언하려는 그 순간.*
  한계처럼 보이는 것이 실은 **표본 하나에 전체를 맞추고 있던 것**일 때가 있다
  (세션38: `WhitePurple` 텍스트 18개를 크기별로 세어 보니 **15pt 하나 때문에 27~48pt인 12개가 손해**를 보고 있었다).
- **래퍼·파사드를 만들면 전제를 *모든 공개 진입점*에 건다.** 트리거 자명 — *`static` 창구 클래스에 두 번째 공개 멤버를 추가하는 순간.*
  하나에만 걸어두면 **다른 진입점이 조용히 다른 상태를 본다** — 게다가 호출 순서가 우연히 맞아떨어지면 **가려져서 안 드러난다.**
  (세션39: `Loc.T()`에만 초기화를 넣고 `Loc.Locales`엔 빠뜨렸다. 설정 패널이 마침 `T()`를 먼저 불러 정상처럼 보였고,
  검증 로그의 `Locales.Count = 0`을 눈으로 훑다가 잡았다.) **진입점을 열거하고 전제를 표로 맞출 것.**
- **진단용 출력이 서로 다른 값을 같은 문자열로 뭉개지 않는지 볼 것.** 트리거 자명 — *숫자를 잘라서·줄여서 찍으려는 순간.*
  자리 절약이 **정반대 결론**을 만든다. 폭이 걱정되면 값을 줄이지 말고 **표본 수**를 줄인다.
  (세션41: 밝기를 `ToString("0.00").Substring(1)`로 찍어 흰색 1.00과 검정 0.00이 **둘 다 `.00`**이 됐고,
  "판 속이 전부 검정"이라는 결론이 나왔다. 앞서 잰 평균 밝기와 안 맞아서 겨우 걸렸다.)
  화면이 이상할 때 **원인을 눈으로 좁히지 않는 것**도 같은 갈래다 — 자세한 절차는 `unity-mcp` 스킬.
- **손으로 한 계산으로 가능성을 닫지 말 것.** "그건 해도 소용없다"는 판단은 **실측 한 번보다 싸지 않다.**
  세션38에 "폰트 padding을 늘려도 비율이라 소용없다"고 계산해 놓고 **그게 틀려서 정답을 두 턴 늦게** 시도했다.
- **"구조적으로 불가능하다"를 말하기 전에, 그 불가능이 *내가 덧붙인 전제* 때문인지 본다.**
  바로 위 규칙은 *계산 착오*를 말하는데, 이건 **전제를 좁게 잡아놓고 그걸 제약이라고 되돌려 보고**하는 경우다.
  트리거 자명 — *"~라서 안 됩니다"를 쓰려는 순간, 그 "~라서"가 **사용자가 준 조건인지 내가 얹은 조건인지** 갈라 본다.*
  특히 **사용자가 이미 예외를 허용해 둔 규칙**을 엄격하게만 읽으면 난다.
  (세션46: §5-1의 "판은 원본 크기로"를 *원본 크기만 허용*으로 좁혀 읽고 좌우 화살표·토글을 "불가"로 보고했다.
  사용자 규칙엔 *"써야 할 때는 쓰되 Simple + Preserve Aspect"* 라는 **축소 허용이 처음부터 있었다.**)
- If a simpler approach exists, say so. Push back when warranted.
- **대안으로 도구·기능을 꺼낼 땐, 꺼내는 그 자리에서 그 문서를 연다.** "X로도 됩니다"라고 **쓰기 전에** 여는 것이지, 사용자가 "X가 뭔데?"라고 물은 뒤가 아니다.
  세션29에 `/schedule`을 안 열고 대안으로 밀었는데 **클라우드 실행이라 로컬 파일을 못 읽었다** — 결론이 통째로 뒤집혔다.
- If something is unclear, stop. Name what's confusing. Ask.
- **외부 도구(MCP·API)에 한글을 넘길 땐 유니코드 이스케이프를 손으로 만들지 말고 한글을 그대로 쓸 것.** JSON에 직접 넣어도 문제없다.
  트리거 자명 — *툴 인자에 한글이 든 문자열을 넘기기 직전.* 세션36에 손으로 계산한 코드포인트가 **4번 중 4번 다른 글자가 됐고**
  (`핫뚜`→`핫뙐`→`핫뜬`, `만렙`→`만렉`, `곡선`→`공선`), 그게 **사용자가 보는 노션 카드에 그대로 박혔다.**
  같은 세션에 Edit 툴로 쓴 한글(`.asset`·`HANDOFF.md`)은 멀쩡했다 — 이스케이프 자체가 원인이다.
- **메모리·문서에는 규칙만 적고, 변하는 것(현재 값·구현 상태·프리팹 목록)은 "어디서 확인하는지"만 적을 것.** 상태를 박아두면 낡아서 다음 세션이 틀린 걸 사실로 읊는다. 세션27에 메모리 3건이 그렇게 낡아 있었고, 그중 하나는 **다른 메모리가 "틀렸다"고 기록해 둔 주장**을 그대로 갖고 있었다(둘 다 매 세션 주입된다). 일하는 방식 규칙(`feedback`)은 24일이 지나도 안 낡았다 — 차이는 그 시점 상태가 섞였는지뿐이다.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

## 5. 개발 환경 & 패키지

- **Unity 6** (6000.4.4f1), URP 2D
- **UI 시스템**: uGUI (미정 시 기본값. 필요 시 UI Toolkit으로 전환하고 여기 갱신)
- **빌트인 패키지**: 2D Animation, 2D Tilemap, 2D SpriteShape, URP
- **서드파티 플러그인**:
  - **Unity-MCP** (IvanMurzak, `com.ivanmurzak.unity.mcp`) — Claude Code가 Unity 에디터를 직접 조작하기 위한 MCP 브릿지. §6 참고.
  - VFX: `Assets/Vefects/Pixel Craft VFX URP` (에셋스토어, URP 2D용 임포트 완료 — `VFX_2D_...` 접두사 프리팹 사용)
  - 도트 그래픽: 캐릭터·몬스터·배경은 직접 그리기 (GDD §6 프로토타입 계획 참고)

---

## 5-1. 🔴 UI 이미지는 **무조건 Simple + Set Native Size** (사용자 결정, 예외 없음)

**칸 크기를 정해두고 거기 그림을 맞추지 말 것. 스프라이트 원본 크기가 칸 크기를 정하고, 텍스트·레이아웃을 그 안쪽에 맞춘다.**

- `Image Type` = **`Simple`**, 크기는 **`Set Native Size`**(코드로는 `img.SetNativeSize()`)로 원본 픽셀 그대로.
  ⚠️ `Set Native Size` 버튼은 **Type이 Simple일 때만** 인스펙터에 나타난다 — Sliced면 안 보인다.
- 🚫 **`Sliced`(9-slice)를 쓰지 않는다.** border를 **원본 픽셀 크기 그대로** 그리기 때문에,
  그림을 줄여 쓰면 테두리·**아래 그림자 띠**만 원본 크기로 남아 상대적으로 두꺼워진다
  (372×372 그림의 아래 그림자 66px = 원본 18% → 240 칸에선 27%). 늘려 쓰면 반대로 가운데가 뭉갠다.
- **`Preserve Aspect`는 켠다** — 사용자가 잡은 판·버튼이 전부 `Simple + NATIVE + PA`다.
- ⚠️ **`localScale`은 가급적 쓰지 않는다.** 다만 꼭 필요하면 써도 된다(사용자 결정) —
  **대신 그 Image는 무조건 `Simple` + `Preserve Aspect`여야 한다.** 비율이 깨지면 픽셀이 통째로 뭉갠다.
- **칸에 맞는 원본 크기 그림이 세트에 없으면 늘려 쓰지 말고 그 크기의 그림을 요청**한다.
  "늘려 쓸 큰 판을 그려 달라"는 **잘못된 요구**다 — 늘리는 한 같은 문제가 반복된다.
- ⚠️ **`UISkinApply.cs`의 `FitSlice`는 `img.type = Sliced`를 강제로 씌운다.** 이 규칙과 정면으로 충돌하므로
  **도구를 돌리기 전에 반드시 확인할 것** — 손으로 Simple로 바꿔 놓아도 도구가 되돌린다.
- 2026-08-25에 이 규칙을 어겨 캐릭터 선택·진화 창을 전부 다시 손봐야 했다. 회의록에도 여러 번 올라온 지적이다.
- **검증은 rect 계산이 아니라 플레이모드 캡처로 한다** — rect의 "안쪽에 들어감"은 그림자 두께·실제 글리프 높이·
  9-slice 늘어남을 **못 잡는다.** 한 화면 고칠 때마다 `ScreenCapture.CaptureScreenshot`으로 찍어 눈으로 볼 것.
- 🔴 **UI를 훑어 "이상한 곳"을 찾을 땐 *넘치는 것*만 세지 말고 *눌린 것*도 셀 것 — 검사 항목은 `칸 비율 vs 그림 비율`이다.**
  `Preserve Aspect`는 비율이 어긋나도 **조용히 맞춰 그린다**: 경고도 없고, rect도 정상이고, 글자도 안 넘친다.
  그래서 "글자가 칸을 넘는가 / 그래픽이 부모 밖으로 나가는가"만 보는 전수 조사에 **아예 안 걸린다.**
  트리거 자명 — *화면을 전수 조사해 결함 목록을 내려는 순간.* 각 Image의 `rect.width/height`와 `sprite.rect.width/height`의
  비를 나란히 찍어, 차가 크면 그 자체를 결함으로 볼 것.
  (세션47: 설정 창을 "전수 조사했다"고 보고한 **뒤에** 사용자가 *"화살표 버튼 동그라미가 이상하잖아"* 라고 짚어줬다.
  3.50 비율 판(`가로길쭉이` 361×103)이 1.07 비율 칸(44×41)에 들어가 **44×12 띠로 눌려** 있었다. 값으로 잡을 수 있었다.)
- 🔴 **비율이 맞아도 틀린다 — 그림이 *칸 전체를 채우지 않는* 경우가 있다.** 바로 위 규칙은 `칸 비율 vs 그림 비율`만 보는데,
  판 그림에는 **둘레의 투명 여백**과 **둥근 실루엣**이 있다. 비율은 통과하는데 **그림은 칸 안쪽 일부만** 채우므로,
  **rect를 기준으로 놓은 자식이 판 밖 허공에 간다.** 경고도 없고 비율 검사에도 안 걸린다.
  트리거 자명 — *판 스프라이트를 깐 칸 안에 글자·버튼을 배치하려는 순간.*
  → 실루엣을 **PNG 알파로 재서** 그 안에 배치한다. `베개같이생긴네모_색칠`은 373×195 중 채워진 면이 **x34~344 · y22~149**뿐이고,
  둥글어서 **위아래 끝일수록 쓸 수 있는 가로폭이 좁다**(측정 표는 `PauseMenu.BuildUI` 위 주석에 있다).
  (세션49: 일시정지 창의 제목이 판 **위** 허공에, 버튼·ESC 힌트가 판 **아래** 밖에 있었다. 버튼 비율 결함을 값으로
  먼저 잡아 고쳤는데, 캡처를 안 찍었으면 **그것만 고치고 끝냈을** 자리다.)
- 🔴 **색·알파·머티리얼도 값이 아니라 캡처로 판정할 것.** 위 규칙은 *rect*만 말해서 색에는 발동을 안 한다.
  `Image.color`가 노랑이어도 **그림이 검정이면 화면은 검정**이다(uGUI 기본 셰이더가 텍스처에 색을 **곱한다**).
  트리거 자명 — *`.color`를 바꿔 놓고 "됐다"고 말하려는 순간.* 찍어 보거나, 그림의 픽셀 밝기를 재라.

### 📐 판 그림별 용도와 글자 크기 (2026-08-25에 사용자가 직접 잡은 Title·Battle 씬에서 실측)

**새 화면을 만들 때 이 표에서 고른다. 칸 크기는 원본 그대로 쓰고, 글자를 거기 맞춘다.**

| 그림 | 원본 | 쓰이는 곳 · 글자 |
|---|---|---|
| `가로길쭉이_색칠` | 361×103 | 메인 메뉴 버튼(**42pt**) · 정수 표시(28pt Right) · 맵 이름표(26pt) |
| `가로안길쭉이_색칠` | 227×118 | 뒤로/선택 버튼(**32pt**) · 레벨 박스(26pt) |
| `베개같이생긴네모_색칠` | 373×195 | 제목 박스 — 스킬트리/캐릭터/맵/스테이지/레벨업 헤더(**40~42pt**) |
| `각진정사각형_색칠` | 203×221 | 캐릭터 카드 (이름 24pt) |
| `정사각형_색칠` | 372×372 | 승천 행 · 원티드 포스터 · 리롤 버튼(0.67배 PA, 28pt+18pt) |
| `개큰네모_색칠` | 721×289 | 스킬 설명 박스(이름 32pt Left · 설명 22pt) |
| `길쭉큰네모_색칠` | 900×210 | 레벨업 선택 버튼 3장 |
| `스킬아이콘하기좋은네모_색칠` | 143×141 | 아이콘 칸 |
| `동그라미_색칠` | 145×135 | 좌우 화살표 버튼(36pt) |

- **게이지는 3겹**: `OO바_색칠`(바탕) + `OO바_내용물`(채움) + `OO바_투명`(테두리를 맨 위에 덮음). 체력·경험치 둘 다.
  🔴 **가운데 채움 층만은 이 §5-1의 예외다 — `Image.type`이 반드시 `Filled`(Horizontal · Left)여야 한다.**
  `Simple`이면 **`fillAmount`가 통째로 무시되어** 바가 늘 꽉 차 보인다(경고도 없다).
  트리거 자명 — *게이지 채움 Image의 `type`을 만지는 순간.* 2026-08-26에 체력·경험치 바가 정확히 이래서 고장나 있었다.
  ⚠️ `preserveAspect`도 꺼야 한다 — 켜면 채움 폭이 그림 비율에 눌린다.
- **아이콘류만 확대 예외** — 원본이 24×29·32×48로 작아서 키워 쓰되 **`Preserve Aspect` 필수**.
- **딤 배경**: 스프라이트 없는 Image에 `#08050D` + 알파(전체 덮기 0.96 / 모달 0.55~0.85).
- **IDLE 모션은 [UIFloat.cs](Assets/Scripts/UIFloat.cs)를 붙이기만 하면 된다**(새로 만들지 말 것).
  호버 중엔 자동으로 멈춘다. ⚠️ `LayoutGroup` 아래에 붙이면 제자리를 레이아웃 뒤에 잡아야 해서
  `UIFloat`이 그 경우를 따로 처리한다 — 그 분기를 지우지 말 것.
- **버튼 연출은 `JuicyButton`**이 소유(`idleDim`으로 평소 0.92배·어둡게, 호버 1.06배). 값은 `JuicyTuning.cs`.
  `targetGraphic`은 `colorOnHover`를 켤 때만 쓰는 필드다 — **비어 있어도 정상**이다.

---

## 6. Unity-MCP — Claude가 씬을 직접 조작함

**이 프로젝트는 Claude Code가 Unity 에디터에 직접 연결되어 있다.** `ai-game-developer` MCP 서버(로컬, `http://localhost:23269`)를 통해 GameObject 생성·컴포넌트 부착/수정·프리팹 생성·씬 저장·플레이모드 진입까지 Claude가 직접 수행한다. **더 이상 "에디터 작업은 사용자가, 코드는 Claude가" 원칙이 아님** — 오브젝트 배치·참조 연결도 Claude가 MCP 툴로 처리한다.

- 연결 설정: `.mcp.json` (프로젝트 루트), Unity 쪽 설정은 `UserSettings/AI-Game-Developer-Config.json` — Unity 에디터의 `Window > AI Game Developer` 창에서 Local/Cloud 모드 확인 가능.
- 새 세션에서 Unity MCP 툴이 안 보이면: Claude Code 재시작해야 `.mcp.json` 변경이 반영됨.
- Unity 에디터가 열려 있어야 로컬 서버가 뜬다. `mcp__ai-game-developer__scene-list-opened`로 연결 확인.

### 🚫 씬·에셋·플레이모드를 건드리기 전에 `unity-mcp` 스킬을 열 것

모달 API 호출(→ Unity 정지·MCP 사망) · 사용자 미저장 편집 파괴 · 검증 4계층 · 플레이모드 측정 함정 ·
MCP 툴 우회법 · SO 독립 파일 규칙이 **전부 거기 있다. 안 열고 손대면 Unity를 멈추거나 사용자 작업물을 날린다.**

원본은 키트(`prototyping-kit/skills/unity-mcp/`)에 있고 `~/.claude/skills/`에 정션으로 연결돼 있다 — 모든 프로토타입이 같은 파일을 본다.

### 🪝 이 프로젝트의 안전장치 (배선)

`.claude/settings.json`의 `PreToolUse` 훅(`.claude/hooks/block-unity-hazards.ps1`)이 `mcp__ai-game-developer__*` 호출을 검사해
**모달 API · `scene-open` · `console-get-logs` · Additive 없는 `OpenScene`**을 차단하고 대안을 알려준다.
차단되면 stderr 메시지를 읽고 그대로 따를 것. 탈출구는 인자에 `HOOK-OK` — **사용자 승인을 받은 뒤에만.**

---

## 6-1. 노션 · 칸반보드 → `kanban` 스킬

노션 연동 정보(허브 링크·툴 이름 함정)와 칸반보드 "POC 할일"의 운영 원칙·조회 절차는 **`kanban` 스킬**에 있다.
세션 시작(`brief` 스킬)·종료(`/wrap`)와 노션에서 뭔가 찾을 때 그 스킬을 열 것.

---

## 7. 세션 시작 루틴

**세션 첫 턴에 `brief` 스킬을 실행한다.** 절차(칸반 조회 → HANDOFF 읽기 → **코드 대조** → 일기 → 브리핑)와
각 단계의 확인 항목이 전부 거기 있다. 전부 읽기 전용이라 **사용자가 안 쳐도 알아서 돈다** (`/wrap`과 반대).

- 이 프로젝트의 일기 경로: `d:\unity\prototyping-kit\journal\`
- 칸반 조회 절차는 `kanban` 스킬, 씬 구조는 `SCENE_MAP.md`, 코드 구조는 `ARCHITECTURE.md`

### 세션 내내 유효한 규칙 (스킬이 아니라 여기 있는 이유: 시작할 때만 지키는 게 아니다)

- `GDD.md`는 `grep`으로 필요한 섹션만 조각내어 읽을 것 (`cat GDD.md` 금지)
- **밸런스 수치는 문서가 아니라 에셋이 정답** — `StageTable*`·`EnemyDefinition`·`Prog_*`·`BalanceConstants.cs`
- ⚠️ **HANDOFF는 할 일의 목록이지 코드의 진실이 아니다.** 거기 적힌 "~해야 함"을 사용자에게 말하기 전에 **코드로 확인**할 것 — 이미 해결됐거나 필드명이 틀렸던 적이 여러 번 있다.
- ⚠️ **"코드에 스위치가 있다" ≠ "게임에 그 기능이 있다".** 스킬트리 노드·SO·씬 배열처럼 **에셋이 켜야 도는 것**은 코드만 보면 있는 것처럼 보인다.
  트리거 자명 — *코드의 `case`·필드를 근거로 현황을 보고하거나 선택지를 만들 때, 그 스위치를 켜는 **에셋을 `grep`으로 한 번** 확인한다.*
  (세션33: 리프레쉬 폐지의 파급으로 "스킬트리 노드 `refresh_bonus`가 죽는다"고 보고하고 **그 전제로 질문까지 했는데**, 현행 `MainSkillTree.asset`엔 그 노드가 **애초에 없었다** — `MainSkillTree_Backup.asset`에만 있었다.)
  바로 위 "밸런스 수치는 에셋이 정답"과 같은 함정인데, 그 규칙은 **값**만 말해서 **기능의 존재 여부**엔 적용이 안 됐다.
- ⚠️ **체크박스·상태 표시를 근거로 "완료"를 말하기 전에 그 항목의 *하위 목록*을 볼 것.**
  트리거 자명 — *카드·문서의 `[x]`를 근거로 진행 상황을 요약하려는 순간.*
  **체크박스는 그 줄만 말하지 아래 딸린 리스트는 말하지 않는다.**
  바로 위 두 규칙(HANDOFF의 "~해야 함"·"코드의 스위치")은 전부 **미완료 주장**만 의심해서 **완료 주장**엔 발동을 안 한다 — 반대쪽에서도 틀린다.
  (세션42: 「폴리싱 할일 리스트업」의 `- [x] UI 에셋` 하나를 보고 "UI 에셋 완료"로 요약했는데, 그건 **틀 교체**만 끝난 것이고
  본문의 UI 상세 리스트는 미체크가 훨씬 많았다. **그 오독 위에 출시 일정 전체를 세웠고**, 사용자가 *"아직 그래픽적인 것도 완벽하지 않은데"* 라고 해서야 드러나 일정을 다시 짰다.)

- ⚠️ **반대쪽도 틀린다 — 체크가 *비어 있는* 항목도 그대로 믿지 말 것.** 바로 위 규칙은 `[x]`(완료 주장)만 의심하는데,
  **미체크 항목이 이미 끝나 있거나 사용자가 "안 한다"고 정해둔 경우**가 그만큼 많다.
  트리거 자명 — *카드·문서의 미체크 항목을 다른 곳으로 옮기거나 할 일로 보고하려는 순간.* **에셋·파일로 1건 확인한다.**
  특히 **사용자가 항목 뒤에 덧붙여 놓은 메모가 곧 결정**이다 — 체크박스는 안 바뀌어 있다.
  (세션44: 「사운드」 카드의 "효과음 14종 고르기"·"git 포함 결정"이 미완으로 남아 있었지만
  `SfxLibrary.asset`엔 **이미 15개가 꽂혀 있었고** `Assets/Sound`는 **진작 `.gitignore`에** 있었다.
  「아트 리소스」는 사용자가 *"지금도 괜찮던데"*, *"그냥 기본 오브 크기 조정해서 써"* 라고 적어둔 걸
  미체크라는 이유로 살아 있는 할 일로 취급했다 — **6건**이 그랬고 사용자가 *"내가 안 하기로 결정한 거 다 제껴"* 라고 해서야 걸렀다.)

- ⚠️ **포인터도 "~해야 함"과 똑같이 낡는다 — `파일:라인`을 따라가기 전에 그 줄을 열어볼 것.**
  위 규칙들은 전부 **주장**(했다·안 했다)만 의심하고, 그 옆에 적힌 **"손댈 곳" 열은 사실로 읽는다** — 거기서 틀린다.
  포인터는 적을 당시엔 맞았어도 코드가 움직이고, 애초에 **다른 심볼을 잘못 짚어놨을 수도** 있다.
  트리거 자명 — *문서·카드의 `파일:라인`을 근거로 코드를 고치려는 순간.* **그 줄을 읽고, 이름이 뜻과 맞는지 본다.**
  (세션45: ⚡표가 "기본 오브 관통력↑ → `SkillProgression.cs:113` `Pierce`"라고 가리켰는데 그 줄은 **기본공격 화살의 관통**이었다.
  진짜 손잡이는 `BalanceConstants.OrbBaseTargets`였고, 게임 UI가 그 값을 **"관통 대상 +N"**으로 표시하고 있어 확정됐다.
  그대로 고쳤으면 **사용자가 요청한 것과 다른 스킬을 버프**할 뻔했다.)

- ⚠️ **문서에 적힌 *해법*도 똑같이 낡는다 — "이렇게 하면 된다"는 줄을 그대로 구현하기 전에 전제 하나를 실측할 것.**
  바로 위 규칙은 **포인터**(`파일:라인`)만 의심하고, 그 옆의 **"이렇게 해라"는 사실로 읽는다.**
  해법은 적을 당시에 **검증까지 된 게 아닐 수도 있다** — 관찰은 맞는데 처방은 추측이었던 경우가 있다.
  트리거 자명 — *문서·주석이 제시한 해법을 그대로 따라가려는 순간.*
  (세션47: HANDOFF에 "`_투명`은 순수 검정이라 색을 곱해도 안 물든다 → 노란 하이라이트는 `_색칠`을 **뒤에 까는 판**으로 해라"가 있었다.
  **관찰은 맞았는데 처방이 틀렸다** — `_색칠`도 바깥쪽이 검은 선이라 뒤에 깔아 키우면 그 검은 선만 삐져나온다.
  그대로 해 보고 한 번 헛돌았다. 정답은 `_투명` 선화를 **`Mask`로 쓰고 안쪽을 채우는 것**이었다.)
- ⚠️ **규칙 옆에 붙은 *예시*도 낡는다 — 규칙이 맞다고 예시까지 맞는 게 아니다.**
  위 규칙들은 **주장**(했다/안 했다) · **포인터**(`파일:라인`) · **해법**(이렇게 해라)까지 의심 대상에 넣었는데,
  **"이때 이랬다"는 구체 예시**는 여전히 사실로 읽는다. 예시는 **그때 한 번의 관찰**일 뿐이라 그 사이에 바뀐다.
  트리거 자명 — *문서의 예시를 근거로 배선·매핑을 정하려는 순간.* **예시가 가리키는 파일을 열거나, 한 번 물을 것.**
  (세션48: 바로 아래 "파일명 접미사" 규칙의 예시가 `Effect_Arrow*R1/R2`를 **티어**라고 적어놨는데,
  실제로는 **둘 다 1차 진화 화살의 1·2프레임**이었다 — 작화 도구가 R1로 저장하면 다음 프레임을 R2로 자동 명명한다.
  아이러니하게 그 규칙 자체가 *"한 번 물을 것"* 이었다. **규칙은 맞고 예시가 틀렸는데, 예시를 근거로 배선했다.**)
- ⚠️ **같은 사실의 사본을 두 곳에 두지 말 것.** 두면 반드시 한쪽만 낡는다 — 그리고 **낡은 쪽을 근거로 말하게 된다.**
  트리거 자명 — *이미 어딘가 있는 표·목록을 다른 문서에 또 적으려는 순간.* **원본 한 곳만 두고 나머지는 포인터만 남긴다.**
  (세션44: HANDOFF 안에 일정표가 두 군데 있었는데 사용자가 조건을 하나씩 줄 때마다 한쪽만 고쳐져 **하루에 세 번 어긋났다.**
  「태리미가 할 일」 절도 날짜 카드와 두 번째 진실이 돼서 결국 10블록을 통째로 지웠다 — 처음부터 한 곳에만 적었으면 없을 일이었다.)
---

## 8. grep 사용

**코드 파일 (.cs) 수정 시:**
- 수정 전 `grep`으로 관련 클래스/함수의 정확한 위치(라인) 먼저 파악
- 불필요한 전체 파일 cat 금지, 해당 라인만 Read

---

## 9. 세션 종료 — `/wrap`

세션을 마무리할 땐 **`/wrap`** 을 실행한다. 절차(이번 세션 정리 → 칸반 → HANDOFF → 일기 → 커밋)와
각 단계의 확인 항목이 전부 거기 있다.

**부수효과가 있어서 사용자가 칠 때만 도는 스킬이다** — Claude가 임의로 커밋하거나 노션 카드를 고치지 않는다.

- 이 프로젝트의 키트 경로: `d:\unity\prototyping-kit` (일기는 그 아래 `journal/`, 틀은 `templates/JOURNAL_ENTRY.md`)
- 저장소가 둘이다 — 키트를 고쳤으면 거기도 따로 커밋·푸시할 것.

## 10. 프로젝트 종료 루틴

플레이 빌드 배포 후 또는 프로토타입 중단 결정 후:

1. `RETROSPECTIVE.md` 작성 (틀: `templates\RETROSPECTIVE.md`)
   — **철저히 사용자 중심으로 작성.** "구현된 피쳐"·"기술 스택" 같은 사실 나열 섹션만 AI가 채우고, 나머지(What Went Well/Wrong·과정 개선안·게임 개선방안)는 작성 전 플레이테스트 여부/결과부터 묻고 섹션별로 사용자 입장을 질문해서 그 답변으로 채운다. AI가 짚고 싶은 이슈는 먼저 언급하고 사용자 동의를 받아 반영한다.
   — What Went Well/Wrong은 **워크플로 수준**으로. 코드 버그 목록 아님.
   — 과정 개선안 중 Kit에 반영할 것은 **[→ Kit]** 표시 후 실제 반영
2. 노션 등 외부 도구에 공유 (선택, 확정 후에)
3. 다음 프로토타입 SESSION_ZERO 작성 시 이 회고 먼저 읽을 것
